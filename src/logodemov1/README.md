# Logo demo v1 (`/logodemov1`)

Parallel to **`/client-showcase`**: alternate entry under **`src/logodemov1/`** with its own App Router segment **`src/app/logodemov1/`**.

- **Purpose:** Same marketing homepage shell with a **different header wordmark** (and nav/hash targets scoped to this URL).
- **Local:** [http://localhost:3000/logodemov1](http://localhost:3000/logodemov1)

Edit **`content.ts`** (`logoDemoV1.logoSrc`) to point at your demo asset in `public/assets/branding/`.
