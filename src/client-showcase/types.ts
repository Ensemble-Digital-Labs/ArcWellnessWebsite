/** Subset of fields used by simplified testimonial cards (matches `homeTestimonials` rows). */
export type HomeTestimonialItem = {
  id: string;
  imageSrc: string;
  imageAlt: string;
  quote: string;
  attribution: string;
  context: string;
};
