# EmSella imagery

Raster masters + WebP siblings for `/treatments/emsella`.

| File | Section |
|------|---------|
| `emsella-hero.webp` | Full-bleed page hero |

Runtime paths use `.webp` only (see `webp-assets.mdc`). PNG masters stay for editing.
Referenced from `src/content/pages/emsella.ts` via `emsellaAsset()`.
