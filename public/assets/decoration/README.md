# Decoration assets

Visual accents that are **not** core section photography or logos.

| Subfolder | Purpose |
|-----------|---------|
| [`background/`](./background/) | Abstract / ambient **background** decoration — `ambient-01` … `ambient-10`, plus `welcome-copy-stage-cream` for welcome copy phase (see `backgroundDecoration.ts`; **09** → Your Path intro, **10** asset-only). |
| *(root)* | `micro-statement-tibbixel-frame.svg` — vector frame for **`ArcMicroStatementSection`** copy block (CSS background). |

St. Louis landmark imagery stays under **`../location/st-louis/`** (`stlouisDecoration.ts`).
