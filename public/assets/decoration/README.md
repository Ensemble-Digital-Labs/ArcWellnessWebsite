# Decoration assets

Visual accents that are **not** core section photography or logos.

| Subfolder | Purpose |
|-----------|---------|
| [`background/`](./background/) | Abstract / ambient **background** decoration — `ambient-01` … `ambient-04` (see `backgroundDecoration.ts`). |

St. Louis landmark imagery stays under **`../location/st-louis/`** (`stlouisDecoration.ts`).
