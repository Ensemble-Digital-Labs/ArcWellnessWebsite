# Session log – 2026-06-22

### 1. New transparent gold logo — move to branding + site swap

**Ask:** Move `arc_wellness_logo_transparent_gold.png` into `public/assets/branding/` with the other logos and update the site to use it.

**Done:**

- Moved repo-root **`arc_wellness_logo_transparent_gold.png`** → **`public/assets/branding/arc-wellness-logo-transparent-gold.png`** (kebab-case, alongside v1–v4).
- **`src/content/site.ts`** — **`images.logo`** now points at the gold transparent wordmark (header, footer, inner headers, client-showcase components that consume **`images.logo`**).
- **`scripts/make-logo-transparent.cjs`** — **INPUT** path updated to the new production logo file.
- **`src/logodemov1/content.ts`** — comment updated to note main site uses transparent gold.


### 9. Rose-gold logo asset — organize into branding

**Ask:** Move **`ChatGPT Image Jun 22, 2026, 02_46_41 PM.png`** into the assets folder.


### 11. Header logo — smaller HD wordmark

**Ask:** Make the logo smaller after swapping in the HD transparent gold asset.

**Done:** **`ArcSiteHeader.tsx`** — reduced **`ARC_HEADER_LOGO_*`** heights and max-widths (~15–20%): mobile **`6.5–7rem`**, desktop container **`h-32` → `h-40`**, max-width caps **`480–520px`**.

### 12. Header logo — smaller + no shadow

**Ask:** A bit smaller; remove shadow on the wordmark.


### 13. Hero — full-bleed scroll zoom (no center frame)

**Ask:** Remove the small center image that zooms on scroll; zoom the background instead; remove the secondary back image layer for now.

**Done:** **`ScrollExpandHero.tsx`** — removed center **`mediaSrc`** frame, ambient underlay, and crossfading **`bgImageSrc`** entrance layer. Single full-bleed photo scales with scroll (**`bgScale`**). Intro copy repositioned below headline (mobile) / bottom-right (desktop). **`ArcMarketingHome`** passes **`images.heroMedia`** only. **`site.ts`** comments updated.



**Ask:** Use new **`arc_wellness_logo_HD_transparent_gold(1).png`** as the site logo.

**Done:** Moved to **`public/assets/branding/arc-wellness-logo-transparent-gold.png`** (replaces missing prior file). **`images.logo`** unchanged path — header, footer, and all **`images.logo`** consumers now serve the HD asset.



**Ask:** Make the hero intro paragraph (“Redefining how you look…”) a bit bigger.

**Done:** **`ScrollExpandHero.tsx`** — **`HERO_INTRO_TYPE`** bumped from **`text-sm` / `md:text-base`** to **`text-base` / `md:text-lg` / `lg:text-xl`**; ornament frame max-width widened slightly so lines breathe.


**Ask:** At ~752px width, “Begin your Journey” and “See How it Works” should be center-aligned (not left).

**Done:** **`ScrollExpandHero.tsx`** — CTA row uses **`max-md:items-center max-md:justify-center`**; **`sm:items-start`** limited to **`md:`**+ so tablet/mobile keeps centered button group while desktop stays left-aligned with headline.


**Ask:** Branding teal should be **`#83d0bb`**, not the previous **`#4ec4b0`**.

**Done:** **`globals.css`** — **`--arc-teal: #83d0bb`**, harmonized hover/ink/muted tokens, added **`--arc-teal-glow`**. Hero/showcase marquee shadows use **`var(--arc-teal-glow)`**. Hardcoded teal RGBA glows in **`src/`** updated to match. **`.cursor/rules/project-context.mdc`** hex note updated.


**Ask:** Change the hero scroll/marquee bar color from rose gold to branding teal.

**Done:** **`ScrollExpandHero.tsx`** — bar **`bg-arc-teal`**, white label type, teal glow shadow, white/15 border. **`ShowcaseConcernMarquee.tsx`** aligned for client-showcase parity.


**Ask:** Update sliding concern bar: remove “Issues” from brain fog; split hormonal imbalance and weight gain into separate items.

**Done:** **`ScrollExpandHero.tsx`** **`HERO_KEYWORD_MARQUEE_ITEMS`** — “Hormonal Imbalance”, “Weight Gain”, “Brain Fog & Focus” (6 items total). **`client-showcase/mock-page-content.ts`** marquee list aligned.


**Ask:** Change hero title from “Where Aesthetics, Wellness, and Longevity Converge.” to “Where Wellness, Longevity and Aesthetics Converge.”

**Done:** Updated **`ArcMarketingHome.tsx`** title prop; **`ScrollExpandHero.tsx`** default keyword emphasis order; **`client-showcase/mock-page-content.ts`** for parity.


**Ask:** Replace hero intro paragraph (“Rooted in vitality…”) with new tagline about redefining how you look, feel and function.

**Done:** Updated **`ScrollExpandHero`** **`intro`** in **`src/components/arc/ArcMarketingHome.tsx`** to: “Redefining how you look, feel and function through every decade — with intentional, science backed care designed for the life you are building.”


| Path | Notes |
|------|-------|
| `public/assets/branding/arc-wellness-logo-transparent-gold.png` | New production wordmark |
| `src/content/site.ts` | `images.logo` |
| `scripts/make-logo-transparent.cjs` | INPUT path |
