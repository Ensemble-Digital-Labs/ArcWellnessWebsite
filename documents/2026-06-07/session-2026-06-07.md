# Session log – 2026-06-07

### 1. Hero background image — organize asset and wire homepage

**Ask:** Use newly added `ChatGPT Image Jun 7, 2026, 09_54_44 AM.png` as the hero back/background image on the homepage; rename and organize under `public/assets`.

**Done:** Moved file from repo root to `public/assets/hero/arc-wellness-entrance-glass-doors.png` (kebab-case, matches existing hero naming). Updated `images.heroBg` in `src/content/site.ts` so `ArcMarketingHome` → `ScrollExpandHero` `bgImageSrc` serves the new open-glass-doors entrance photo. Center scroll-expand frame (`heroMedia` / reception interior) and underlay (`ambient-03.png`) unchanged.

## Paths / artifacts

| Path | Notes |
|------|--------|
| `public/assets/hero/arc-wellness-entrance-glass-doors.png` | New hero full-bleed background |
| `src/content/site.ts` | `heroBg` path updated |

### 2. Hero background — remove dark overlay (preview)

**Ask:** Remove the dark layer on top of the new hero background to see how it looks.

**Done:** Removed `bg-black/35` overlay from the full-bleed `bgImageSrc` layer in `ScrollExpandHero.tsx`. Center frame overlay and ambient underlay unchanged.

### 4. Hero inner frame — new reception image

**Ask:** Add new image from folder, rename/organize in assets, use as hero center (inner) image.

**Done:** Moved `ChatGPT Image Jun 7, 2026, 03_58_07 PM.png` → `public/assets/hero/arc-wellness-reception-mint-logo-wall.png`. Updated `images.heroMedia` (and services array entry) in `src/content/site.ts`. Full-bleed background (`heroBg`) unchanged.

### 5. Hero inner frame — remove dark overlay

**Ask:** No dark overlay on the center zooming image.

**Done:** Removed the animated `bg-black/45` layer over `mediaSrc` in `ScrollExpandHero.tsx`.

### 6. Ambient light backgrounds — organize assets

**Ask:** New ambient “light” version background images added to folder — organize into assets.

**Done:** Moved `ambient-01-light.png` … `ambient-04-light.png` from repo root → `public/assets/decoration/background/`. Added `BACKGROUND_DECORATION_LIGHT_IMAGES` in `src/content/backgroundDecoration.ts` and README row. Original `ambient-01` … `ambient-04` unchanged; not wired to hero yet.

### 7. Switch ambient 01–04 backgrounds to light plates site-wide

**Ask:** Wherever `ambient-01` … `ambient-04` are used as backgrounds, use the `-light` versions.

**Done:** Updated `BACKGROUND_DECORATION_IMAGES[0]`…`[3]` to light paths; added `HERO_AMBIENT_BG`, `FOOTER_AMBIENT_BG`, and `BACKGROUND_DECORATION_DARK_IMAGES` (rollback). Wired hero underlay, footer, concerns USP cells, founder/about/insights ambients, client-showcase header/footer/services/testimonials via shared exports. Original dark PNGs kept on disk.

### 8. Concerns USP bar — black text on light ambient

**Ask:** Change white stat bar text (12+, FDA, 0, Free) to black for readability on light marble background.

**Done:** Updated USP value/label classes in `ArcConcernsPinnedSection.tsx` to `text-arc-charcoal` with light text-shadow (removed dark drop-shadow).

### 9. Concerns USP bar — thicker black dividers

**Ask:** Thick black border lines on the stat bar.

**Done:** Grid uses `border-t-2` / `border-b-2` `border-arc-charcoal`; cell dividers `border-r-2` / `border-b-2` (2×2 mobile, four columns desktop).

### 10. Concerns panels — rounded corners

**Ask:** Round borders on the five concern boxes.

**Done:** Added `rounded-xl` (+ focus ring offset) to each concern panel button in `ArcConcernsPinnedSection.tsx`.

### 11. Hero text — maximum legibility on bright entrance photo

**Ask:** Improve hero headline + intro visibility; text color only, best contrast on busy background.

**Done:** Unified hero copy to warm off-white (`#faf8f4`) with layered dark text-shadow (headline script, serif connectors, intro paragraph). CTAs, images, overlays unchanged.

### 12. Hero text — stronger contrast + keyword emphasis

**Ask:** Text still not standing out; “Aesthetics, Wellness, Longevity” need to pop.

**Done:** Switched hero type to near-black with white outline shadow (better on sunlit walls). Script keywords: larger scale, `arc-teal-ink`, heavier white stroke. Intro `<strong>` matches teal accent.

### 13. Hero headline — fix awkward line breaks

**Ask:** Weird line breaking — “Longevity” orphaned on its own line.

**Done:** Replaced flex-wrap keyword row with block/inline flow; non-breaking tie keeps “Wellness, and Longevity” together; slightly wider max-width for the headline stack.

### 14. Hero keywords — forced one line, scales to fit

**Ask:** “Aesthetics, Wellness, and Longevity” on one line; responsive; not cut off.

**Done:** `HeroKeywordOneLine` wrapper with `whitespace-nowrap`, non-breaking commas/“and”, and ResizeObserver fit that shrinks font until the phrase fits the available width (floor 11px). Fluid clamp sets the starting size on larger viewports.

### 15. Hero keywords — restore large size, keep one line

**Ask:** As big as before, but still one line.

**Done:** Restored large keyword `em` scale (1.92em–2.62em) and full responsive base (`text-2xl` → `lg:text-4xl`). Auto-fit only shrinks on narrow viewports when the line would overflow.

### 16. Hero intro frame ornaments — visibility on bright bg

**Ask:** Decorative corner borders on intro text block need to be visible.

**Done:** `HeroIntroCornerOrnament` — charcoal stroke (`#141414`), thicker lines, higher opacities, white + dark drop-shadow halo (matches intro copy legibility).

### 17. Hero mobile — disable scroll-expand animation

**Ask:** Animation should not affect mobile hero view (keep good stacked layout).

**Done:** On `<768px`: skip GSAP ScrollTrigger pin/scrub; lock progress at 0; static bg + fixed-size center frame; normal document scroll. Desktop scroll-expand unchanged.


**Ask:** Change light rose gold to dark rose gold; change white text to black for hero visibility on bright entrance photo.

**Done:** In `ScrollExpandHero.tsx` only — connectors (`Where`, `and`, `Converge.`) and intro frame ornaments use `text-arc-rose-gold-ink`; headline keywords and body line use `text-arc-charcoal`; intro paragraph charcoal with rose-gold-ink on `<strong>`; hero CTAs use local `HERO_GLASS_CTA_CLASS` with dark labels. Rest of site unchanged.

### 18. Hero mobile — center alignment

**Ask:** On mobile, hero content should be middle/center aligned (headline, CTAs, intro).

**Done:** `ScrollExpandHero.tsx` — below `md` (768px): headline stack, keyword line, closing line, intro copy, and intro block container use `text-center` / `items-center`; `md+` keeps existing left alignment and desktop scroll-expand behavior unchanged.

### 19. Hero hydration mismatch fix

**Ask:** Console hydration error — server HTML didn’t match client in `ScrollExpandHero`.

**Done:** `isMobileState` was initialized with `window.innerWidth` on the client but `false` on the server, so mobile got different markup on first paint. Now viewport detection runs only after hydration (`hasHydrated` + `useLayoutEffect`); render branches use `mobileLayout = hasHydrated && isMobileState`. Unified hero background markup (single `motion.div`, no div swap) avoids hydration mismatches. GSAP pin is **not** gated on `hasHydrated` — it registers in the same effect pass as downstream sections so pin spacing for concerns → welcome stays correct; extra `ScrollTrigger.refresh()` after hero setup.

### 20. Welcome copy phase — light cream background

**Ask:** “Wellness. Made Personal.” section should use the lighter copy-stage background, not the dark version.

**Done:** Confirmed asset `welcome-copy-stage-cream.png` (light cream / fabric / portrait); dark art kept as `about-copy-stage--previous.png` for rollback. `ArcWelcomeSplitSection` immersive copy phase: removed `bg-black/[0.12]` overlay → `bg-arc-cream/25`; updated copy typography for light plate (charcoal body, `text-arc-teal-ink` on “Made Personal”, teal CTA). Exported `WELCOME_COPY_STAGE_BG` from `backgroundDecoration.ts`.

### 21. Testimonials — lighter full-bleed background + light-surface typography

**Ask:** User added a new lighter testimonial background for the testimonials section.

**Done:** Moved `ChatGPT Image Jun 7, 2026, 06_58_13 PM.png` → `public/assets/sections/testimonials/testimonials-background.png`; archived prior bronze plate as `testimonials-background--previous.png`. Added `TESTIMONIALS_SECTION_BACKGROUND_SRC` (+ rollback export) in `backgroundDecoration.ts` with `?v=20260607-light` cache bust. **`ArcTestimonialsSection.tsx`**: wired shared export; replaced `bg-black/25` with cream gradient wash; heading + sphere hint → charcoal; testimonial cards → solid white panels with charcoal quote/attribution and teal top accent on active card (replacing dark glass + white type). Added `public/assets/sections/testimonials/README.md`.

### 22. Whole-Body Care slider — light editorial spa assets + light UI

**Ask:** User added new light-theme editorial spa images; organize into assets and use in Whole-Body Care. Inside and Out section.

**Done:** Moved five `ChatGPT Image Jun 7, 2026, 07_18_* PM` exports from repo root → `public/assets/sections/editorial-spa/editorial-spa-01.png` … `05.png` (1:1 slide order); archived dark set as `editorial-spa-01--previous.png` … `05--previous.png`. **`editorialSpaImages.ts`**: `EDITORIAL_SPA_ASSETS_VERSION = "20260607-light"`, cache-bust query strings, rollback export. **`servicesShowcaseSlides.ts`** unchanged (slides 1–5 already use `EDITORIAL_SPA_IMAGES[0–4]`; slide 6 still `patient-experience-01`). **`ArcServicesShowcaseSlider.tsx`**: cream shell, light gradient overlay, charcoal typography, frosted nav/controls; **`globals.css`**: `.arc-slide-nav--light` nav label + progress track tokens. Updated `public/assets/sections/editorial-spa/README.md`.

### 23. Insights page masthead — charcoal header text

**Ask:** On the Insights page, change the header text color to black.

**Done:** **`ArcInsightsFeedSection.tsx`** — masthead `h1` and subtitle switched from white + drop shadows to `text-arc-charcoal` / `text-arc-charcoal/85`; header divider updated to `border-arc-charcoal/10` for the light marble ambient plate.

### 24. Insights feed area — white surface + charcoal type

**Ask:** Section below the masthead: black background → white; update text colors accordingly; colors only.

**Done:** **`ArcInsightsFeedSection.tsx`** — feed wrapper `bg-white`; filter tabs, underline, borders, card titles, meta lines, and empty state copy → charcoal tokens; focus ring offsets adjusted for white surface. Layout, images, and hover behavior unchanged.

### 25. Crafted by your Concern — inverted panel copy chips

**Ask:** Concern title chips (e.g. Low Energy & Burnout, Hormonal Imbalance) → white box / black text; expanded subtext blurb → black box / white text.

**Done:** **`ArcConcernsPinnedSection.tsx`** — title label `bg-white text-arc-charcoal`; active blurb container `bg-arc-charcoal text-white/92` (replacing cream/white blurb chip).

### 26. Crafted by your Concern — mobile layout fix

**Ask:** Section doesn’t look good on mobile (squished five-column row, truncated titles, blurb overlay covering cards).

**Done:** **`ArcConcernsPinnedSection.tsx`** — on mobile/native scroll: skip GSAP pin; horizontal snap carousel (~82vw cards); full-width title chip; blurb moved below carousel with dot nav; USP grid always visible; section uses natural scroll height instead of locked `100dvh`. Desktop pinned expand behavior unchanged.
