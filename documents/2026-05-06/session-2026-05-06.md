# Session log - 2026-05-06

### 1. Reduce lag in hero ticker animation

**Ask:** User reported the bottom keyword bar in `ScrollExpandHero` was lagging while animated.

**Done:** Reviewed `src/components/arc/ScrollExpandHero.tsx` and `src/app/globals.css` animation setup. Identified frequent parent re-renders from scroll progress updates as likely source of marquee jank. Applied a targeted fix by wrapping `HeroKeywordMarquee` with `memo` and adding the `memo` import so the marquee subtree does not re-render on every hero scroll update. Ran lint diagnostics for the edited file; no issues found.

### 2. Tighten vertical spacing around footer separators

**Ask:** User requested less space above and below the divider line in the footer, then asked for progressively tighter spacing across multiple follow-ups.

**Done:** Updated both separator dividers in `src/components/arc/ArcFooter.tsx` in incremental passes:
- `my-14 md:my-16` -> `my-10 md:my-12`
- `my-10 md:my-12` -> `my-8 md:my-10`
- `my-8 md:my-10` -> `my-6 md:my-8`

Kept all other footer structure and styling intact. Ran lint diagnostics after each edit; no issues found.

## Paths / artifacts

- `src/components/arc/ScrollExpandHero.tsx`
- `src/components/arc/ArcFooter.tsx`

### 3. Left-align patient experience rating block

**Ask:** User requested that the "5.0 Patient experience" group start from the left edge rather than appearing centered.

**Done:** Updated `src/components/arc/ArcFooter.tsx` so the rating cluster aligns left at all breakpoints:
- `StarRatingBlock` root alignment changed from centered to left (`items-center text-center` -> `items-start text-left`).
- Parent grid cell alignment changed from conditional centered/left to always left (`justify-center md:justify-start` -> `justify-start`).

Validated with lint diagnostics; no issues found.

### 4. Reposition "Patient experience" label

**Ask:** User reported the "Patient experience" text was out of position after the previous alignment change.

**Done:** Refined `StarRatingBlock` layout in `src/components/arc/ArcFooter.tsx` to keep the unit in a consistent vertical stack at all breakpoints by removing responsive row switching (`sm:flex-row sm:items-baseline ...`). This keeps the label anchored directly under the `5.0` + stars cluster on the left, avoiding cross-breakpoint drift. Lint diagnostics passed.

### 5. Move label above rating value

**Ask:** User requested "Patient experience" to appear on top of the 5-star/5.0 rating.

**Done:** Reordered elements inside `StarRatingBlock` in `src/components/arc/ArcFooter.tsx` so the "Patient experience" label renders before the numeric/star row. Preserved the same typography and alignment. Lint diagnostics passed.

### 6. Fill hero end-state whitespace with ambient background

**Ask:** User reported white space around the hero zoom image near the end of the zoom animation and requested `ambient-03.png` behind it to fill gaps.

**Done:** Updated `src/components/arc/ScrollExpandHero.tsx` by adding a persistent full-bleed ambient background layer (`/assets/decoration/background/ambient-03.png`) behind the existing fading hero background image. Added a subtle dark overlay for legibility and raised the animated background layer to `z-[1]` so it fades over the ambient plate. This removes white gaps during end-state zoom while preserving current layout and motion. Lint diagnostics passed.

### 7. Fade in hero ticker after load stabilization

**Ask:** User requested the bottom hero ticker bar to fade in after loading to prevent initial visual jitter.

**Done:** Updated `HeroKeywordMarquee` in `src/components/arc/ScrollExpandHero.tsx` so the moving row starts hidden (`opacity-0`) and transitions to visible (`opacity-100`) with `transition-opacity duration-500 ease-out` once `marqueeOn` flips true. Kept existing delayed animation start logic and `animate-arc-marquee` behavior unchanged. Lint diagnostics passed.

### 8. Fade in entire ticker bar (background + text)

**Ask:** User clarified they wanted the entire bar to fade in, including the teal bar container itself, not only the words.

**Done:** Moved the opacity transition from the inner moving text row to the outer ticker container in `src/components/arc/ScrollExpandHero.tsx`. The wrapper now transitions from `opacity-0` to `opacity-100` when `marqueeOn` becomes true, so both the bar background and text reveal together. Lint diagnostics passed.

### 9. Smooth the ticker fade-in motion

**Ask:** User requested a smoother fade-in animation for the ticker bar.

**Done:** Tuned the outer ticker container transition in `src/components/arc/ScrollExpandHero.tsx` from a short ease-out fade to a longer and softer curve: `duration-900` with `ease-[cubic-bezier(0.22,1,0.36,1)]`, plus `will-change-opacity` for cleaner compositing. Lint diagnostics passed.

### 10. Reduce hero CTA size while keeping CTA readability

**Ask:** User requested the two hero CTAs ("Begin your Journey" and "See How it Works") be smaller but still clearly visible as CTA controls.

**Done:** Added an `xs` size variant to `ArcTextUnderlineCta` in `src/components/arc/ArcTextUnderlineCta.tsx` with slightly reduced type scale (`text-xs sm:text-sm`) and uppercase weight retained for CTA clarity. Updated both hero CTA instances in `src/components/arc/ScrollExpandHero.tsx` from `size="sm"` to `size="xs"`. This reduces visual footprint without removing emphasis or interaction affordance. Lint diagnostics passed.

### 11. Shorten scroll lock duration for services WebGL section

**Ask:** User requested the pinned scroll lock in the services showcase (`#services`, `WebGLShowcase`) to be shorter.

**Done:** Introduced configurable pin duration scaling for pinned sections:
- `src/lib/arcSectionPins.ts`: added `pinDistanceMultiplier` option to `useArcFullscreenPin` and applied it to `endDist()` with a safety clamp.
- `src/components/arc/PinnedSection.tsx`: added pass-through prop `pinDistanceMultiplier`.
- `src/components/arc/WholeBodySection.tsx`: set `pinDistanceMultiplier={0.72}` for `#services` only.

Result: the services section unlocks sooner while preserving its existing visual/interaction behavior. Lint diagnostics passed.

### 12. Add per-step backgrounds in the Path section

**Ask:** User requested each journey step in `#path` to have a background that visually references that step.

**Done:** Updated `src/components/arc/YourPathSection.tsx` to add a subtle per-step ambient backdrop behind each sticky step content area in the immersive layout:
- Added `next/image` import.
- Introduced `PATH_STEP_BACKGROUNDS` with five existing ambient assets (`ambient-01` through `ambient-05`).
- Rendered an absolute background image layer per step (selected by index) plus a teal-muted gradient overlay for legibility.

Kept the existing card content, spacing, and sticky behavior intact. Lint diagnostics passed.

### 13. Expand step backgrounds to full section

**Ask:** User clarified that each path step background should fill the full step section (treat each step as its own section), not just the inner content block.

**Done:** Refactored background rendering to the `ArcStickyTabs.Item` section layer:
- `src/components/arc/ArcStickyTabs.tsx`: extended `ArcStickyTabsItemProps` with optional `backgroundSrc` and `backgroundOverlayClassName`; render full-bleed absolute background + overlay at the section root.
- `src/components/arc/YourPathSection.tsx`: pass `backgroundSrc` per step to `ArcStickyTabs.Item`; removed prior inner-content background layer.

Result: each step now gets a true full-section background fill behind header/content. Lint diagnostics passed.

### 14. Remove gray cast and inner background container from path steps

**Ask:** User reported a gray overlay on the step backgrounds and a weird inner container effect.

**Done:** Removed both visual sources:
- `src/components/arc/YourPathSection.tsx`: set per-step `backgroundOverlayClassName="bg-transparent"` so no gray tint is applied.
- `src/components/arc/ArcStickyTabs.tsx`: removed `bg-inherit` from the step content wrapper (`relative z-0 min-h-0`) to avoid a centered background block appearance.

Result: step backgrounds show cleanly without the cast, and the inner section no longer looks like a separate tinted container. Lint diagnostics passed.

### 15. Prevent step card from peeking during scroll transitions

**Ask:** User reported step content becoming visible in an awkward way while scrolling over step transitions.

**Done:** Adjusted immersive step content positioning in `src/components/arc/YourPathSection.tsx` so cards stay lower and centered within each step:
- Changed container alignment from `justify-start` to `justify-center`.
- Increased top offset from small values (`pt-3/5/6`) to larger values (`pt-12/14/16` across breakpoints).

This reduces card peeking into sticky transition zones and keeps each step visually contained. Lint diagnostics passed.

### 16. Keep path hint and first step header visually connected

**Ask:** User reported the transition between the `#path` intro hint strip and the first sticky step header was splitting apart while scrolling, and requested they stay together.

**Done:** Updated `src/components/arc/YourPathSection.tsx` to shorten the sticky lead lock track for this section by passing `leadStickyLockTrackClassName="min-h-[100dvh]"` to `ArcStickyTabs`. This reduces the extra hold distance from the default and makes the handoff between the hint strip and the first step header feel continuous instead of separated. Ran lint diagnostics for the edited file; no issues found.

### 17. Make path step headers stick at viewport top

**Ask:** User shared a `sticky-section-tabs` reference implementation and requested the two sticky regions stop splitting while scrolling, noting nav spacing was not a priority.

**Done:** Updated `src/components/arc/YourPathSection.tsx` to override this section's step sticky offset by passing `stickyHeaderContainerClassName="top-0 shadow-[0_8px_30px_rgba(0,0,0,0.06)]"` into `ArcStickyTabs`. This makes the step header (`1 Listen`, etc.) stick to the viewport top (reference-style behavior) instead of waiting for the larger nav-offset sticky top, removing the visible split band during scroll transitions. Lint diagnostics passed.

### 18. Remove all path step designs after intro hint

**Ask:** User requested removing all step design content that appears after "Keep scrolling to follow your path" in the `#path` section, while keeping testimonials and other sections below intact.

**Done:** Edited `src/components/arc/YourPathSection.tsx` to remove the immersive step list entirely by making `ArcStickyTabs` render only the lead sticky intro/hint (no `ArcStickyTabs.Item` children). Also removed the reduced-motion step card grid and replaced it with a small single informational card so the section still closes cleanly without the multi-step UI. No other sections/routes were changed. Lint diagnostics passed.

### 19. Remove `ArcStickyTabs` from `#path` and delete unused component

**Ask:** User requested removing the `ArcStickyTabs` component as well since it is no longer used for the path section.

**Done:** Refactored `src/components/arc/YourPathSection.tsx` to stop using `ArcStickyTabs` entirely. Rebuilt the immersive path intro as a direct `PinnedSection` with the same background image/scrim, centered headline + CTA, and bottom "Keep scrolling to follow your path" strip. Removed the `ArcStickyTabs` import and added local `next/image` usage for the intro background. Then deleted `src/components/arc/ArcStickyTabs.tsx` because it had no remaining code references. Verified lint diagnostics for the edited section file and checked source tree references (`src/`) to confirm no `ArcStickyTabs` usage remains.

### 20. Make `#path` hint strip span edge-to-edge

**Ask:** User requested the bottom hint strip (`"KEEP SCROLLING TO FOLLOW YOUR PATH"`) in `#path` fill from the left edge to the right edge instead of staying inset.

**Done:** Updated the hint strip container in `src/components/arc/YourPathSection.tsx` to break out of the inner centered width by replacing the constrained `w-full` behavior with a full-bleed attempt using viewport breakout classes. After user feedback that right-edge fill was still off, refined the implementation to a padding-cancel approach using responsive negative margins (`-mx-5 sm:-mx-8`) so the strip aligns flush with section edges while preserving the existing parent layout and spacing rhythm. Lint diagnostics passed after both edits.

### 21. Convert `#path` hint into a true bottom bar

**Ask:** User clarified the hint should not look like a small box; it should be a left-to-right bar that fills the bottom part of the current section.

**Done:** Refined `src/components/arc/YourPathSection.tsx` so the hint reads as a docked bottom band:
- Removed bottom padding from the parent section wrapper (`pb-0`) so no gap appears below the bar.
- Increased the hint strip vertical presence with `min-h` and center alignment (`justify-center`) to create a clearer bar region.
- Moved safe-area bottom spacing onto the bar itself via responsive `pb-[max(...)]`.
- Slightly deepened the gradient to strengthen the strip as a continuous bottom band.

Result: the "Keep scrolling to follow your path" area now spans left-to-right and visually fills the lower portion of the section instead of appearing as a floating box. Lint diagnostics passed.

### 22. Force full-width edge-to-edge path hint bar

**Ask:** User reported the element still appeared as a box and reiterated that the bar must stretch fully from the left edge to the right edge.

**Done:** Reworked layout in `src/components/arc/YourPathSection.tsx` so the hint bar is no longer a child of the centered `max-w-7xl` content wrapper. Moved it to be a direct child of `PinnedSection` and positioned it with `absolute inset-x-0 bottom-0 w-full`, ensuring true section-wide edge-to-edge rendering. Added bottom padding reserve to the main inner wrapper so headline/CTA content does not overlap the docked bar. Lint diagnostics passed.

### 23. Build five full-page "Your Path" step sections

**Ask:** User requested creating five explicit steps (`I. Listen` through `V. Revise`) with provided copy, treating each as its own full-page section, and applying a split-screen design inspired by their `ScrollAdventure` demo.

**Done:** Refactored `src/components/arc/YourPathSection.tsx` to keep the existing intro/pinned hero + bottom hint bar, then added a new sequence of five full-height sections driven by `PATH_STEPS` data. Each step now renders as:
- Full viewport section (`min-h-[100dvh]`)
- Split layout (`md:grid-cols-2`) with alternating image/content side by step
- Exact user-provided title, step metadata, and body copy
- Ambient background imagery from existing project assets (`BACKGROUND_DECORATION_IMAGES`) to stay on-brand and avoid introducing external placeholders

Also removed the old reduced-motion alternate card block and replaced that logic with the new direct section sequence so the requested structure is consistently rendered. Lint diagnostics passed for the updated file.

### 24. Add per-step fullscreen lock + scroll-scrub animation

**Ask:** User requested each step to lock fullscreen while scrolling and animate during the lock.

**Done:** Updated `src/components/arc/YourPathSection.tsx` step sections to use the existing `useArcFullscreenPin` hook directly per step section (same `#main` scroll model used across the site). Each step now pins for one viewport of scroll distance and scrubs panel transforms via pin progress:
- Left panel translates upward from a positive Y offset toward `0%`
- Right panel translates downward from a negative Y offset toward `0%`
- Both sides use GPU-friendly `translate3d(...)` and `will-change-transform`

Result: each step behaves like a locked chapter with motion progression tied to scroll rather than static sections. Lint diagnostics passed.

### 25. Animate arrow cue in path indicator bar

**Ask:** User requested an arrow animation on the bottom "Keep scrolling to follow your path" indicator bar.

**Done:** Added an inline right-arrow glyph to the indicator label in `src/components/arc/YourPathSection.tsx` and applied a custom animation class. In `src/app/globals.css`, added `@keyframes arc-indicator-arrow` plus `.animate-arc-indicator-arrow` for subtle horizontal pulse movement and opacity change, and disabled it under `prefers-reduced-motion`. Lint diagnostics passed.

### 26. Switch indicator cue to dual downward arrows

**Ask:** User requested down-arrow animation on both sides of the indicator text.

**Done:** Updated `src/components/arc/YourPathSection.tsx` indicator label to render `↓` on both sides of "Keep scrolling to follow your path", each using the existing animation utility class. Updated `src/app/globals.css` keyframes for `arc-indicator-arrow` to animate vertically downward (`translateY`) instead of horizontally right. Kept reduced-motion behavior unchanged (`animation: none`). Lint diagnostics passed.

### 27. Organize new image asset and set testimonials full-screen background

**Ask:** User requested taking a newly added image file, renaming/organizing it into assets, and using it as the full-screen background for the testimonials section.

**Done:** Moved the uploaded image from repo root into a structured section asset path as `public/assets/sections/testimonials/testimonials-background.png` (and removed the original root-level file). Updated `src/components/arc/ArcTestimonialsSection.tsx` to render this asset as an absolute full-bleed background layer (`next/image` with `fill`) behind the testimonials content, plus a cream overlay for readability. Promoted both content columns to `relative z-[1]` so interactive content stays above the background plate. Lint diagnostics passed.

### 28. Increase testimonials heading/hint contrast over photo background

**Ask:** User requested text color changes in testimonials section after applying the new full-screen background image.

**Done:** Updated `src/components/arc/ArcTestimonialsSection.tsx` text styling for the two user-targeted elements:
- Sphere interaction hint copy changed from charcoal to `text-white/90` and given a subtle shadow.
- `Testimonials` heading changed from charcoal to white with a slightly stronger shadow.

This improves readability against the photo background while keeping the card content styling unchanged. Lint diagnostics passed.

### 29. Apply new testimonial card design style

**Ask:** User requested replacing the current testimonial card look with their provided card design reference.

**Done:** Restyled the selected testimonial card in `src/components/arc/ArcTestimonialsSection.tsx` to match the requested card language:
- Switched to a compact white card shell (`max-w-[22rem]`, rounded-2xl, subtle gray border/shadow).
- Added a large top image pane (`h-72`, rounded-xl, inset with side margins) using the selected testimonial image.
- Reworked metadata + quote into a tighter lower text block (`px-4 py-2`) with cleaner hierarchy.
- Removed prior decorative top accent bar and large quote-style typography to align with the new reference aesthetic.

Kept existing selection logic and data wiring intact. Lint diagnostics passed.

### 30. Keep testimonial card permanently spread/fanned

**Ask:** User requested the card to stay always spread out like the stacked-card reference (not only on hover).

**Done:** Updated `src/components/arc/ArcTestimonialsSection.tsx` to render a permanent three-layer fan:
- Added two decorative backing cards behind the main card with opposite rotations and horizontal offsets.
- Kept the current selected testimonial content in the top/front card.
- Preserved existing data binding and interactions while changing only card composition/layout style.

Result: testimonials now present as an always-spread stacked card look that matches the reference direction. Lint diagnostics passed.

### 31. Animate testimonial card transitions on selection change

**Ask:** User requested animation when the testimonial card changes.

**Done:** Added Framer Motion card transition behavior in `src/components/arc/ArcTestimonialsSection.tsx` using `AnimatePresence` + `motion.figure`, keyed by selected testimonial id. New selection now transitions with a short premium motion (`fade + subtle vertical slide + slight scale`) and respects reduced-motion by falling back to opacity-only changes. Kept stacked/fanned card visual design and selection logic unchanged. Lint diagnostics passed.

### 32. Auto-advance testimonials without user interaction

**Ask:** User requested testimonials to switch automatically to the next card even when the user does nothing.

**Done:** Added autoplay selection logic in `src/components/arc/ArcTestimonialsSection.tsx` using `useEffect` + `setInterval`. The selected testimonial now advances every ~4.2 seconds in a loop across all items. Existing manual selection from sphere interactions remains intact and compatible (manual picks become the new current point in the rotation). Lint diagnostics passed.

### 33. Pause autoplay while user interacts with sphere

**Ask:** User requested that cards should not auto-switch during sphere interaction.

**Done:** Updated `src/components/arc/ArcTestimonialsSection.tsx` to track active interaction around the sphere region and temporarily pause autoplay. Added interaction hold logic with small resume delays for mouse/touch activity and explicit hold after image selection. Autoplay timer now only runs when interaction is idle, preventing unwanted switches while the user drags/taps the sphere. Lint diagnostics passed.

### 34. Hero CTAs as glass buttons

**Ask:** User requested the two hero links ("Begin your Journey" → `#book`, "See How it Works" → `#path`) use a glass-button treatment instead of underline CTAs.

**Done:** Updated `src/components/arc/ScrollExpandHero.tsx` to replace `ArcTextUnderlineCta` with `next/link` pills using frosted glass styling (`backdrop-blur`, translucent white fill, light border, inset highlight, hover lift). Tightened horizontal gap between the two buttons on larger breakpoints. Removed unused `ArcTextUnderlineCta` import from this file. Lint diagnostics passed.

### 35. Add script emphasis to invest section headline

**Ask:** User requested the `#book` heading ("Invest in You. Live Fully. Age Intentionally.") use the same emphasis treatment style as the `#path` heading.

**Done:** Updated `src/components/arc/InvestCTASection.tsx` to use `TitleEmphasis` with `ARC_HEADLINE_TITLE_EMPHASIS_CLASS` for key words in the headline (`Live`, `Age`) while preserving existing sizing/spacing classes and CTA layout. Imported the shared emphasis utilities from `@/components/arc/TitleEmphasis`. Lint diagnostics passed.

### 36. Invest `#book` CTAs — glass + member pill (reference)

**Ask:** User requested `#book` CTAs match homepage glass buttons and provided reference (glass “Reserve a call” + solid terracotta “Become a member” with arrow).

**Done:** Added `src/lib/arcGlassCta.ts` exporting shared `arcGlassCtaClass` (same frosted pill as hero). Updated `ScrollExpandHero.tsx` to import it. Replaced invest underline CTAs in `InvestCTASection.tsx` with `next/link`: primary extends glass with sans sentence-case label; secondary uses solid terracotta (`#bc6c5c`) pill, white label, trailing arrow. Responsive row layout with full-width stack on narrow viewports. Lint diagnostics passed.

### 37. Path step photography assets

**Ask:** User added `listen.png`, `measure.png`, `author.png`, `practice.png`, `revise.png` and asked to organize them under assets and use them for the five `#path` step panels.

**Done:** Copied files into `public/assets/sections/your-path/steps/` and removed duplicates from repo root. Exported `PATH_STEP_IMAGE_SRC` from `src/content/backgroundDecoration.ts` and wired `PATH_STEPS` in `YourPathSection.tsx` to those URLs with updated `imageAlt` strings. Updated `public/assets/sections/your-path/README.md` to document the `steps/` folder.

### 38. FAQ section after testimonials (pinned + scroll animation)

**Ask:** User requested a full-screen FAQ block below testimonials with page-lock behavior and scroll-driven animation.

**Done:** Extended `PinnedSection` with optional `onProgress` passthrough to `useArcFullscreenPin`. Added `homeFaq` content in `src/content/homepage.ts` and new client component `src/components/arc/ArcFaqSection.tsx` (`id="faq"`): full-viewport pinned section, two-column layout (intro + accordion `details`/`summary` with `lucide-react` chevron), scroll-scrubbed fade/slide on the container and staggered reveal per FAQ row. Wired `<ArcFaqSection items={homeFaq} />` in `src/app/page.tsx` between testimonials and invest CTA. Respects `prefers-reduced-motion` by showing final state. Lint diagnostics passed.

### 39. FAQ full-page design — tabs + Plus accordion + watermark

**Ask:** User requested FAQ align with full-page ARC design and provided reference patterns (cream two-column + dark tabbed accordion with Plus icons).

**Done:** Restructured content as `homeFaqCategories` + `homeFaqByCategory` in `homepage.ts` (three tabs: Getting started, Scheduling & results, Memberships). Rebuilt `ArcFaqSection.tsx`: kept pinned scroll-scrub wrapper; right column adds subtle **ARC WELLNESS** watermark, **pill category tabs** with teal gradient selection + `layoutId` transition, white card with **Framer Motion** list cross-fade on tab change, and **Plus** toggles with height animation (reduced-motion fallback). Updated `page.tsx` props. Lint diagnostics passed.

### 40. FAQ clearance under fixed header / logo

**Ask:** User reported FAQ tabs and card overlapping the fixed ARC header logo.

**Done:** In `ArcFaqSection.tsx`, replaced vertical `justify-center` with `justify-start` so content is not pulled up under the fixed bar; added responsive top padding aligned to header logo heights (`h-32`–`lg:h-48`) plus `lg:pt-[13rem]` buffer; increased `scroll-mt-*` for `#faq` anchors; set sticky column to `lg:top-48` / `xl:top-52`; shifted the decorative watermark lower (`top-10`+) so it sits in the content band. Lint diagnostics passed.

### 41. FAQ scroll — match global fade-in (remove pin jump)

**Ask:** User reported a jump when scrolling into FAQ and wanted the same fade-on-scroll behavior as other sections, not a hard transition or overlap with the header.

**Done:** Removed `PinnedSection` / pin-progress opacity and translate from `ArcFaqSection.tsx`. The section is now a normal full-height `<section>` with the inner flex wrapper marked `data-scroll-section` so `initArcScrollReveal` in `scrollReveal.ts` drives the same GSAP opacity + `y` reveal as the rest of the site. Kept layout (tabs, accordion, watermark, header padding/sticky intro). Per-row pin-based reveal was removed from the accordion (rows are always visible once the section reveals). Lint diagnostics passed.

## Paths / artifacts (FAQ update)

| Path | Notes |
|------|--------|
| `src/components/arc/ArcFaqSection.tsx` | Pin removed; `data-scroll-section`; simplified accordion rows |

### 42. Your Path intro — scrubbed fade-up like micro statement

**Ask:** User wanted the same scroll-driven fade-in used on the pinned `ArcMicroStatementSection` applied to the `#path` intro block inside `PinnedSection` (headline + “Start Your Journey”).

**Done:** In `YourPathSection.tsx` (`YourPathIntroSection`), added `pinProgress` state via `PinnedSection`’s `onProgress`, plus `prefers-reduced-motion` handling so copy stays fully visible when `useArcFullscreenPin` is skipped. Reused the same `fadeUp` timing as `ArcMicroStatementSection` (`headline` at 0.08 / 2.35, CTA at 0.26 / 2.2) with inline `style` on the `h2` and a wrapper `div` around `ArcTextUnderlineCta`. Verified with `npm run build`.

### 43. Your Path step panels — same scrubbed fade-up

**Ask:** User asked to apply the same fade-in behavior to each of the five path steps below the intro.

**Done:** In `YourPathSection.tsx`, extracted shared `pathPinFadeUp` + `usePathPinScrubProgress` (replacing duplicated intro logic). `YourPathIntroSection` now uses those helpers. `YourPathStepSection` uses the hook with `useArcFullscreenPin`’s `onProgress`, and applies staggered fades to title / step meta / body only (`0.08/2.35`, `0.16/2.05`, `0.24/2.0`); step photography stays static aside from the light column parallax. Parallax `%` transforms on the two grid columns key off scrub `p` so reduced-motion users (`p === 1`) land at neutral parallax with full-opacity text. Verified with `npm run build`.

### 44. Path intro — “keep scrolling” bar scrub fade

**Ask:** User wanted the bottom gradient hint bar on `#path` to use the same pin-scrub fade-up as the rest of the intro.

**Done:** In `YourPathIntroSection`, added `scrollHintMotion = pathPinFadeUp(p, 0.34, 2.05)` on the `role="note"` bar (after headline + CTA stagger). Reduced-motion path still yields `p === 1` via existing hook.

### 45. Testimonials — shorter pin duration

**Ask:** User asked to reduce scroll lock duration for `#testimonials`.

**Done:** Set `pinDistanceMultiplier={0.5}` on `PinnedSection` in `ArcTestimonialsSection.tsx` (default was `1` = one viewport of pinned scroll via `useArcFullscreenPin`).

### 46. Invest / `#book` — shorter pin duration

**Ask:** User asked to reduce scroll lock for the invest CTA section (`#book`).

**Done:** Set `pinDistanceMultiplier={0.5}` on `PinnedSection` in `InvestCTASection.tsx`, matching testimonials.

### 47. Invest / `#book` — pin-scrub fade like micro statement

**Ask:** User wanted the invest band to use the same scroll-scrubbed fade-in as `ArcMicroStatementSection`.

**Done:** Added `src/lib/arcPinReveal.ts` with shared `pathPinFadeUp` + `usePathPinScrubProgress` (moved from `YourPathSection.tsx`). `InvestCTASection` is now a client component: `onProgress={setPinProgress}` on `PinnedSection`, staggered `pathPinFadeUp` on headline (`0.08/2.35`), optional supporting line (`0.16/2.05`), and CTA row (`0.26/2.2`) — same timing as micro headline / USP / link. Background image + charcoal scrim stay static. Verified with `npm run build`.

### 48. Testimonials — pin-scrub fade (match invest / micro)

**Ask:** User asked to apply the same scrubbed fade-in to the testimonials section.

**Done:** In `ArcTestimonialsSection.tsx`, wired `usePathPinScrubProgress` + `onProgress` on `PinnedSection`. Staggered `pathPinFadeUp` on sphere wrapper (`0.08/2.35`), hint line (`0.16/2.05`), “Testimonials” title wrapper (`0.08/2.35`), and testimonial card stack (`0.26/2.2`). Background image unchanged. Verified with `npm run build`.

### 49. Client showcase — separate folder and route

**Ask:** User wanted a simplified client-facing preview clearly separated from ongoing full-site work, with all simpler-version work living in its own folder for organization.

**Done:** Added `src/client-showcase/README.md` documenting the split (main work stays in `src/components/arc/`, preview-only code in `src/client-showcase/`). Added App Router segment `src/app/client-showcase/` with a layout banner + metadata (“Client preview”) linking to `/`, and a placeholder `page.tsx` explaining the intent. Updated root `README.md` folder table and dev URL note. Full homepage at `/` is unchanged.

## Paths / artifacts (this entry)

| Path | Notes |
|------|-------|
| `src/client-showcase/README.md` | Conventions and URL |
| `src/app/client-showcase/layout.tsx` | Banner + metadata |
| `src/app/client-showcase/page.tsx` | Placeholder until sections are composed |
| `README.md` | Documented `/client-showcase` |

### 50. Client showcase — design reference folder

**Ask:** User asked where the extra folder for **design** of the simplified version is.

**Done:** Clarified that earlier work only added **`src/client-showcase/`** (code). Created **`design/client-showcase/README.md`** at repo root for mockups, exports, and reference imagery for the lite preview; noted distinction from **`design/reference/`** (main site). Linked from `src/client-showcase/README.md` and root `README.md` folder table.

### 51. Client showcase — first full simplified homepage

**Ask:** User asked to start the simplified version of the website.

**Done:** Implemented a static, scroll-normal homepage at **`/client-showcase`** that mirrors **`/`** section order without `ArcScrollShell`, Locomotive, or pinned/scrub GSAP. Added **`src/client-showcase/`** modules: `content.ts` (concerns/path/USP + `showcaseInternalHref`), `types.ts`, `ClientShowcaseHome.tsx`, and **`components/`** (`SimpleShowcaseHeader`, hero, micro statement with poster still, concerns grid + USP chips, welcome split, founder + mosaic grid, services card grid, path intro + steps, four testimonial cards, invest CTA, `SimpleFooter` without pin/marquee loop). **`src/app/client-showcase/page.tsx`** now renders `ClientShowcaseHome` only (placeholder copy removed). Reused `@/content/homepage`, `servicesShowcaseSlides`, and `site` images. Verified with **`npm run build`**.

## Paths / artifacts (entry 51)

| Path | Notes |
|------|-------|
| `src/client-showcase/*` | New showcase implementation |
| `src/app/client-showcase/page.tsx` | Thin route entry |

### 52. Client showcase nav — two-tier reference layout

**Ask:** User wanted the nav bar improved to match a supplied reference (dark green announcement rail, cream main bar, centered serif links, pill CTA, EN/FR, patient portal).

**Done:** Rebuilt `SimpleShowcaseHeader.tsx` with tier 1 (forest `#1B3022`, uppercase announcement items, phone + patient portal + arrow) and tier 2 (cream `#F5F2E9`, brand dot + logo + “Est. 2019” subtitle, centered Playfair links: Philosophy, Modalities, The Method, Journal, Visit). Right: EN/FR toggles (visual state only) and forest pill “Book a consultation” with arrow. Mobile: compact book pill + drawer listing all links including Concerns + Invest. Added `id="journal"` on `SimpleTestimonials` for the Journal anchor. Build verified.

### 53. Client showcase — Vida-style simpler marketing flow

**Ask:** User asked for a simpler site patterned after [Vida Revival](https://vidarevival.com/).

**Done:** Reworked `/client-showcase` composition: **utility header** (white strip: phone, `hello@arcwellness.com`, book appointment, patient portal; cream bar: logo + sans nav About / What we treat / Services / Your path / Resources / Contact + consultation CTA). Added **`VidaStyleHero`** (stacked *aesthetics / wellness / longevity*, “Where science meets serenity.”, schedule CTA on hero photo), **`ShowcaseFocusStrip`** (icon row linking to `#concerns`), **`ShowcasePillars`** (two-column “Elevating your care”), **`SimplePathCompact`** (3-step summary replaces long `SimplePath` on this route). Removed micro-statement block and old split hero from this page flow; services grid now shows **4** slides with updated heading. Build verified.

### 54. Client showcase hero — single caps word + Vida-style subline

**Ask:** User wanted the hero to match a reference screenshot (one large all-caps sans headline, “WHERE SCIENCE MEETS” + italic *serenity*, rectangular white CTA).

**Done:** Updated `VidaStyleHero.tsx`: dominant **single-word** headline (default `REFINEMENT`, overridable via `headlineWord` prop), wide letter-spacing sans treatment; subline as uppercase sans + nested italic Playfair *serenity*; removed extra body paragraph; CTA is **rounded-md** white block with tight uppercase tracking. Softer gradient scrim for photography. Build verified.

### 55. Client showcase — full rebuild to ARC homepage mock

**Ask:** User asked to build the simplified site to match their full-page ARC mock (utility + main header, left hero on clinic interior, who we are two-column, five whole-body cards with highlighted center card, horizontal process timeline, split pre-footer, light footer).

**Done:** Added **`design-tokens.ts`**, **`mock-page-content.ts`**, and mock components: **`ArcDesignHeader`**, **`ArcDesignHero`** (initial ARC mock version), **`WhoWeAreDesign`**, **`WholeBodyFiveCards`**, **`ProcessTimelineDesign`**, **`PatientsAndShopBands`**, **`SplitPrefooterCTA`**, **`ArcDesignFooter`**. **`ClientShowcaseHome.tsx`** composes this stack. Phone placeholder **(949) 555-ARCW** per mock. Removed the extra “Client preview” strip from **`src/app/client-showcase/layout.tsx`**. Build verified.

### 56. Client showcase hero — Arcwell editorial layout

**Ask:** User wanted the hero replaced with a reference layout (grid on photo, corner mastheads, quote, serif headline with italic *beautifully*, white pill CTA, studio line with play).

**Done:** Rebuilt **`ArcDesignHero.tsx`**: light scrim + CSS grid overlay; top-left **N° / LONGEVITY BY DESIGN**; top-right **VOL. VII** + italic quote; main **“A quieter way to age *beautifully*, on purpose.”** with ARC Presidio body copy; bottom **“Begin your arc”** white pill + outlined play + studio line. Added **`editorialHeroCopy`** + **`editorialHeroImage`** (`images.heroBg`) in **`mock-page-content.ts`**; **`ClientShowcaseHome`** uses the exterior hero image. Build verified.

### 57. Main homepage — hide micro statement for design review

**Ask:** User wanted **`ArcMicroStatementSection`** commented out on the real homepage while showing the actual design.

**Done:** In **`src/app/page.tsx`**, commented out **`ArcMicroStatementSection`** usage and its **`ArcMicroStatementSection`** import; commented **`homeMicro1`** import; removed unused **`videos`** import. Left a short note in JSX for restoring the block. Verified with **`npm run build`**.

### 58. Your Path — rose-gold CTA and step titles

**Ask:** User pointed at **`#path`**: “Start Your Journey” underline CTA and step headings (I. Listen … V. Revise) still used teal ink in the DOM; align with the site’s rose-gold accent treatment.

**Done:** In **`src/components/arc/YourPathSection.tsx`**, set **`ArcTextUnderlineCta`** to **`accent="roseGoldInk"`** for the intro link; changed step title **`p`** from **`text-arc-teal-ink`** to **`text-arc-rose-gold-ink`**. Section backgrounds (`arc-teal-muted` panels) unchanged. **`npm run build`** succeeded.

### 59. Concerns pinned section — new headline + subheading

**Ask:** User specified replacement copy for **`ArcConcernsPinnedSection`**: heading **Crafted By your Concern**; subheading **Thoughtfully mapped to guide your journey to aesthetics, wellness and longevity**.

**Done:** In **`src/components/arc/ArcConcernsPinnedSection.tsx`**, replaced the **`h2`** line with **Crafted** (charcoal) + **`TitleEmphasis`** **By your Concern** (same rose-gold ink treatment as before). Added a centered **`p`** under the headline for the subheading (`text-arc-charcoal/72`, responsive type scale). **`npm run build`** succeeded.

### 60. Hero keyword marquee bar — rose gold

**Ask:** User wanted the bottom **`ScrollExpandHero`** ticker bar (concern keywords) recolored off teal to match the rose-gold direction.

**Done:** In **`src/components/arc/ScrollExpandHero.tsx`**, marquee container **`bg-arc-teal`** → **`bg-arc-rose-gold-ink`**; top border and shadow tuned; added a light rose wash in the upward shadow via **`color-mix`** with **`--arc-rose-gold`**. Label and bullet separators use **`text-arc-cream`** / **`text-arc-cream/85`** instead of pure white for a warmer read on the ink field. Comment updated. **`npm run build`** succeeded.

### 61. Hero keyword bar — match luminous rose-gold (`text-arc-rose-gold` token)

**Ask:** User wanted the bottom marquee bar to use the same color as the hero headline keyword emphasis (**`text-arc-rose-gold`** / **`--arc-rose-gold`**), not the ink bar.

**Done:** In **`ScrollExpandHero`**, marquee wrapper **`bg-arc-rose-gold`**; border **`border-arc-charcoal/10`**; shadow uses charcoal softness plus **`var(--arc-rose-gold-glow)`**. Labels and middots switched to **`text-arc-charcoal`** / **`text-arc-charcoal/50`** for contrast on the light rose field. **`npm run build`** succeeded.

### 62. Root URL — simplified client showcase; full site at `/full`

**Ask:** User wanted to “go back to” the simplified marketing version.

**Done:** **`src/app/page.tsx`** renders **`ClientShowcaseHome`**; full homepage extracted to **`MarketingHomeFull`** + **`/full`**. **`/client-showcase`** unchanged.

### 63. Client showcase hero — tighter vertical rhythm

**Ask:** User asked to tighten the hero on **`/client-showcase`** (same component as **`/`**).

**Done:** In **`ArcDesignHero.tsx`**: lower **`min-height`** cap (**880px** / **920px** sm); reduced masthead and bottom padding/gaps; headline **`clamp`** slightly smaller + **`leading-[1.06]`**; body spacing **`mt-4`/`sm:mt-5`**; bottom CTA band tighter; pill padding slightly reduced. **`npm run build`** succeeded.

### 64. Client showcase hero — second tightening pass

**Ask:** User asked for **more tight** on the showcase hero.

**Done:** Further reduced **`min-height`** (**720px** / **780px** sm caps, **85–88dvh**); mastheads smaller type + tighter **`pt/pb/gaps`**; center column **`min-h-0`** + **`py-2`/`py-3`**; headline **`clamp`** max **2.85rem**, **`leading-[1.04]`**; body **`mt-3`**, **`13px`** mobile, **`leading-snug`**; bottom band **`gap-2`**, **`pb-4`**; CTA pill **`py-2.5`** / **`px-5`**; play control **`size-9`**/**`size-10`**. **`npm run build`** succeeded.

### 65. Client showcase hero — larger `h1`

**Ask:** User wanted the editorial hero headline (**“A quieter way to age beautifully, on purpose.”**) **bigger**.

**Done:** In **`ArcDesignHero.tsx`**, widened copy column **`max-w-xl` → `max-w-2xl`**; headline **`clamp`** raised (**~1.85–3.65rem** base, **`lg:`** up to **3.9rem** max), **`leading-[1.06]`** / **`lg:leading-[1.05]`**. **`npm run build`** succeeded.

### 66. Client showcase hero — headline larger again

**Ask:** User asked **bigger** for the editorial **`h1`** (follow-up).

**Done:** **`max-w-2xl` → `max-w-3xl`**; headline **`clamp`** increased (**~2.05–4.25rem** default, **`lg`** up to **4.65rem**, **`xl`** up to **4.85rem**); slightly tighter **`leading`** for large sizes. **`npm run build`** succeeded.

### 67. Client showcase hero — tighter + studio/video right

**Ask:** User asked to tighten the hero more and move **Begin your arc** vs **studio / video** so the video control sits on the **right**.

**Done:** In **`ArcDesignHero.tsx`**: lowered **`min-height`** (**78dvh / 640px**, **82dvh / 700px** sm); reduced masthead and center **`py`** / body **`margin`**; bottom bar **`w-full`** row with CTA **`shrink-0`** and studio link **`ml-auto`**, copy **`text-right`** with play icon **after** the line so the control hugs the right edge; constrained studio **`max-width`** on very narrow screens. **`npm run build`** succeeded.

### 68. Client showcase hero — Begin your arc right-aligned with studio

**Ask:** User asked for **Begin your arc** (pill CTA) **also on the right**, not only the studio line.

**Done:** Wrapped pill + studio links in an inner **`flex … justify-end`** group inside an outer **`justify-end`** bar so both controls sit on the **right**; **`flex-wrap`** keeps narrow layouts sane. **`npm run build`** succeeded.

### 69. Client showcase hero — responsive height across viewports

**Ask:** User wanted hero **height responsive for all screen sizes**.

**Done:** Replaced fixed **`min(dvh, px)`** pairs with shared **`SHOWCASE_HERO_MIN_H`**: **`min-h-[min(92dvh,56rem)]`** on **`section`** and inner flex wrapper — height tracks **92% of dynamic viewport** while **capping at 56rem** on very tall displays; short / landscape viewports shrink naturally without **`min-height` / `max-height`** clashes. **`npm run build`** succeeded.

### 70. Brand logo file — rename to v1

**Ask:** User asked to rename **`arc-wellness-logo.png`** to **`arc-wellness-logo-v1`** (normalized filename **`arc-wellness-logo-v1.png`**).

**Done:** **`git mv`** **`public/assets/branding/arc-wellness-logo.png`** → **`arc-wellness-logo-v1.png`**. Updated **`images.logo`** in **`src/content/site.ts`** and **`scripts/make-logo-transparent.cjs`** (**INPUT** path + comment). **`npm run build`** succeeded.

### 71. Logo v2 — transparent asset + header uses v2

**Ask:** User supplied **`arc_wellness_logo_transparent.png`**; rename to **Arc Wellness logo v2**, place under assets, and use for **`SiteHeader`** logo (**`images.logo`**).

**Done:** Moved repo-root **`arc_wellness_logo_transparent.png`** → **`public/assets/branding/arc-wellness-logo-v2.png`**. **`src/content/site.ts`** **`images.logo`** → **`/assets/branding/arc-wellness-logo-v2.png`** (header + footer + showcase headers that consume **`images.logo`**). **`scripts/make-logo-transparent.cjs`** **INPUT** updated to **`arc-wellness-logo-v2.png`**. **`npm run build`** succeeded.

### 72. Header logo — stronger color / legibility (CSS)

**Ask:** User wanted **`arc-header-logo`** (**transparent v2** wordmark) **more visible in terms of color**.

**Done:** In **`src/app/globals.css`**, expanded **`.arc-header-logo`** **`filter`**: **`brightness`**, **`contrast`**, **`saturate`** for punch; stronger dark **`drop-shadow`** stack; brighter rim glow + **`arc-teal`** bloom via **`color-mix`**. Added **`@media (prefers-contrast: more)`** alternate stack. **`npm run build`** succeeded.

### 73. Logo demo route `/logodemov1` (parallel to client-showcase)

**Ask:** User asked to add another folder/route like **`/client-showcase`**, named **`/logodemov1`**, for an alternate-header-wordmark version of the site.

**Done:** Added **`src/logodemov1/`** with **`content.ts`** (**`LOGO_DEMO_V1_BASE`**, **`logoDemoV1`** pointing at **`arc-wellness-logo-v1.png`** vs main **`v2`**, plus **`logoDemoV1InternalHref`**), **`README.md`**, and App Router **`src/app/logodemov1/layout.tsx`** + **`page.tsx`**. Extracted shared homepage markup into **`src/components/arc/ArcMarketingHome.tsx`**; **`src/app/page.tsx`** now renders **`ArcMarketingHome`** with default **`SiteHeader`**. Extended **`ArcSiteHeader`** with optional **`logoSrc`**, **`logoAlt`**, **`homeHref`**, **`sectionBasePath`** (builds menu links as **`/logodemov1#…`** when set). Demo page passes those props and **`sectionBasePath`** into **`ArcMarketingHome`** so the welcome CTA **`/#services`** becomes **`/logodemov1#services`**. Verified with **`npm run build`**.

## Paths / artifacts (entry 73)

| Path | Notes |
|------|-------|
| `src/logodemov1/content.ts` | Base path + demo logo URL |
| `src/app/logodemov1/*` | Route + metadata |
| `src/components/arc/ArcMarketingHome.tsx` | Shared homepage body |
| `src/components/arc/ArcSiteHeader.tsx` | Optional branding + nav base |
| `src/app/page.tsx` | Uses **`ArcMarketingHome`** |

### 74. Logo v3 — bold transparent asset organized under branding

**Ask:** User asked to rename **`arc_wellness_logo_bold_transparent.png`** as **logo v3** and organize it under assets.

**Done:** Moved repo-root **`arc_wellness_logo_bold_transparent.png`** → **`public/assets/branding/arc-wellness-logo-v3.png`** (kebab-case to match **v1** / **v2**). **`images.logo`** in **`src/content/site.ts`** left on **v2** unless the site should switch to **v3**.

| Path | Notes |
|------|-------|
| `public/assets/branding/arc-wellness-logo-v3.png` | Bold transparent wordmark (**v3**) |

### 75. Main site header logo → v3

**Ask:** User set the **main** site wordmark to **`arc-wellness-logo-v3.png`** (production **`images.logo`**).

**Done:** **`src/content/site.ts`** **`images.logo`** → **`/assets/branding/arc-wellness-logo-v3.png`** (header via **`ArcSiteHeader`** default **`logoSrc`**, plus any consumer of **`images.logo`** e.g. client-showcase headers/footers). **`scripts/make-logo-transparent.cjs`** **INPUT** already pointed at **v3**. **`npm run build`** succeeded.

### 76. Founder immersive section — frosted top blend at scroll start

**Ask:** At the **start** of the founder scroll animation, add a **blur top bar** that blends the transition when scrolling into the section.

**Done:** **`FounderEditorialHero.tsx`**: exported **`FOUNDER_HERO_REVEAL_END`** (aligned with ambient → hero reveal). **`ArcFounderIntroSection.tsx`** (**`FounderImmersiveScrollBody`**): **`motion.div`** absolutely positioned **`z-[12]`** above the editorial hero with **`backdrop-blur`**, cream **`bg-gradient-to-b`**, feathered **`mask-image`**, opacity scrubbed via **`useTransform(progress, …)`** — full at **`progress === 0`**, eased to **0** by **`FOUNDER_HERO_REVEAL_END`**. **`npm run build`** succeeded.

### 79. Hero — rose-gold “Where”, “Converge.”, frame intro, ornament strokes

**Ask:** Apply the same **rose-gold** styling used on connector words to **Where**, **Converge.**, the **HeroIntroOrnamentFrame** paragraph, and related chrome.

**Done:** **`ScrollExpandHero`**: **`heroLeadWordClass`** and **`heroTitleClosingLineClass`** use **`HERO_TITLE_CONNECTOR_ROSE_CLASS`**; shared **`heroTitleLineLayoutClass`**; intro **`<p>`** uses **`HERO_TITLE_CONNECTOR_ROSE_CLASS`**; **`HeroIntroCornerOrnament`** **`stroke-arc-rose-gold`**. **`npm run build`** succeeded.

### 80. Invest `#book` pinned section — founder-style frosted top blend (first pin phase)

**Ask:** Same **blur top bar** as the founder immersive opening, on **`#book`** (**`InvestCTASection`**) for the **first phase** of pin animation.

**Done:** **`InvestCTASection`**: **`INVEST_PIN_TOP_BLEND_END`** (**`0.3`**) scrubs **`investTopBarOpacity`** from pin progress **`p`** (**`1 − min(1, p / 0.3)`**). **`motion.div`** after **`bg-arc-charcoal/45`**, **`z-[8]`**, reuses founder gradient / **`backdrop-blur`** / mask classes (**`investPinnedTopBlendLayerClass`**). **`npm run build`** succeeded.

### 81. Welcome immersive collage — “Made personal” white (opening)

**Ask:** On the **focal collage** tile, make **“Made personal”** (and the period) **white** like **“Wellness.”** in the **initial** collage phase.

**Done:** **`ArcWelcomeSplitSection`** (**`WelcomeImmersiveScrollBody`** focal overlay): **`TitleEmphasis`** + trailing period from **`text-arc-rose-gold`** to **`text-white`** with the same shadow stack as **“Wellness.”**; copy phase **`<h2>`** below unchanged (rose-gold emphasis). **`npm run build`** succeeded.

### 82. Logo v4 — med spa transparent asset under branding

**Ask:** Rename **`arc_wellness_med_spa_logo_transparent.png`** as **logo v4** and place under assets.

**Done:** Moved repo-root **`arc_wellness_med_spa_logo_transparent.png`** → **`public/assets/branding/arc-wellness-logo-v4.png`**. Site **`images.logo`** unchanged unless switched to **v4** explicitly (**`/assets/branding/arc-wellness-logo-v4.png`**).

### 83. Logo demo route `/logodemov2` (v2 wordmark)

**Ask:** Add **`logodemov2`** like **`logodemov1`**, using **`arc-wellness-logo-v2.png`** in the header.

**Done:** **`src/logodemov2/`** — **`content.ts`** (**`LOGO_DEMO_V2_BASE`**, **`logoDemoV2`**, **`logoDemoV2InternalHref`**), **`README.md`**. **`src/app/logodemov2/layout.tsx`** + **`page.tsx`** render **`ArcMarketingHome`** with **`SiteHeader`** props + **`sectionBasePath`**. Comments in **`ArcSiteHeader.tsx`** / **`ArcMarketingHome.tsx`** updated. **`npm run build`** succeeded (**`/logodemov2`** in route list).

### 84. Logo demo route `/logodemov3` (v4 / med spa wordmark)

**Ask:** Add **`/logodemov3`** using **`arc-wellness-logo-v4.png`**.

**Done:** **`src/logodemov3/`** (**`content.ts`**, **`README.md`**) + **`src/app/logodemov3/layout.tsx`** + **`page.tsx`**. **`logoDemoV3.logoSrc`** → **`/assets/branding/arc-wellness-logo-v4.png`**. **`ArcSiteHeader`** comment lists **`/logodemov3`**. **`npm run build`** succeeded.

### 85. Client showcase — viewport fade-in per section

**Ask:** On **`/client-showcase`**, add simpler animation: sections fade in as they enter the viewport, one block at a time.

**Done:** Added **`ShowcaseSectionReveal`** (**`src/client-showcase/components/ShowcaseSectionReveal.tsx`**) — client component using Framer Motion **`whileInView`** with **`opacity`** only (no **`translateY`**) so **`sticky`** on **`ArcDesignHeader`** is not broken by ancestor transforms; **`viewport.once: true`**, **`amount: 0.18`**; **`useReducedMotion`** → plain wrapper, no animation. Wrapped each major block in **`ClientShowcaseHome.tsx`**. **`npm run build`** succeeded.

### 86. Client showcase — slower section fades (Influx-style reference)

**Ask:** Fades felt too fast / invisible; slow them down; **[Influx Marketing](https://www.influxmarketing.com/)** as reference for deliberate scroll reveals.

**Done:** **`ShowcaseSectionReveal`**: **`duration`** **0.55 → 2.15s**, calmer **`ease`** **`[0.33, 0, 0.2, 1]`**; **`viewport.amount`** **0.18 → 0.08** plus **`margin: "0px 0px 18% 0px"`** so the fade starts slightly earlier as the section approaches. **`npm run build`** succeeded.

### 87. Client showcase — staggered section fade pacing

**Ask:** Keep section fades, but add stagger by section index (**`+0.15s`** each block) so reveal pacing is more perceptible.

**Done:** Added optional **`delayIndex`** prop to **`ShowcaseSectionReveal`** and wired transition delay as **`delay: delayIndex * 0.15`**. Updated **`ClientShowcaseHome`** to pass sequential indices **`0..7`** across header → hero → about → services → path → bands → prefooter → footer. Existing slow fade settings and reduced-motion behavior preserved. **`npm run build`** succeeded.

### 88. Client showcase — clearer “Who we are” fade (per-column reveal)

**Ask:** Fade on **`#about`** was hard to see; section is very tall (~stacked copy + image), so a single **`ShowcaseSectionReveal`** wrapper used a coarse intersection target.

**Done:** Added **`WhoWeAreRevealGrid`** (**client**): copy column and image each use **`whileInView`** with **`amount: 0.38`**, **`duration: 2.05s`**, image column **`delay: 0.22s`**; reduced-motion skips animation. **`WhoWeAreDesign`** renders the section shell + grid; **`ClientShowcaseHome`** no longer wraps Who we are in **`ShowcaseSectionReveal`** (avoids double-fade). **`npm run build`** succeeded.

### 89. Client showcase — Influx-style “partial” content reveal (blur + lift + stagger)

**Ask:** Section content should fade in more like **[Influx Marketing](https://www.influxmarketing.com/)** — partially / in pieces, not only a flat opacity tick.

**Done:** **`ShowcaseSectionReveal`**: new **`mode`** — **`stickySafe`** (opacity only, for header / sticky) vs **`rich`** (opacity + **`y`** + **`blur(10px)` → **`blur(0)`**). **`ClientShowcaseHome`**: header uses **`stickySafe`**; other wrapped sections default **`rich`**. **`WhoWeAreRevealGrid`**: columns use the same blur-lift; **`introDelaySec`** on **`WhoWeAreDesign`**. **`WholeBodyFiveCards`**: **`use client`**, viewport **`motion.section`** with staggered **`blurLift`** on title then cards (**`introDelaySec={0.45}`** from home); removed outer **`ShowcaseSectionReveal`** on services to avoid double motion. Reduced-motion paths unchanged (static layout). **`npm run build`** succeeded.

### 90. Client showcase — faster fade / blur-lift timing

**Ask:** Make animation fade in faster.

**Done:** Tightened timing across client-showcase motion: **`ShowcaseSectionReveal`** delay step **`0.15 → 0.1`**, sticky-safe duration **`2.15 → 1.1`**, rich duration **`2.25 → 1.2`**, and gentler start values (**`y 28 → 18`**, **`blur 10px → 6px`**). **`WhoWeAreRevealGrid`** sped up (**`COL_DURATION 2.05 → 1.15`**, second column delay **`0.22 → 0.12`**, start **`y 24 → 16`**, **`blur 8px → 5px`**). **`WholeBodyFiveCards`** sped up card reveal (**`duration 1.7 → 1.05`**, **`y 22 → 14`**, **`blur 8px → 5px`**, stagger **`0.14/0.12 → 0.08/0.05`**). **`npm run build`** succeeded.

### 91. Client showcase hero — fit in viewport across screen sizes

**Ask:** Hero should fully fit in the viewport responsively; bottom part was hidden on larger screens.

**Done:** Updated **`ArcDesignHero`** height sizing to account for sticky header height per breakpoint using responsive **`calc(100dvh - offset)`** values and clamp caps. Replaced old **`min-h-[min(92dvh,56rem)]`** with: **`h-[min(calc(100dvh-8.75rem),52rem)] sm:h-[min(calc(100dvh-7.75rem),54rem)] lg:h-[min(calc(100dvh-6.75rem),56rem)]`** so the bottom CTA row remains visible at large viewport widths while still scaling cleanly. **`npm run build`** succeeded.

### 92. Services reveal — match “Who we are” feel

**Ask:** Make the section after “Who we are” use the same fade/reveal animation feel as “Who we are”.

**Done:** Tuned **`WholeBodyFiveCards`** motion constants to align with **`WhoWeAreRevealGrid`** pacing/style: reveal duration set to **`1.15s`**, hidden offset adjusted to **`y:16`** with **`blur(5px)`**, and section/card stagger changed to **`0.12`** with a slightly later child start (**`0.08 + introDelaySec`**). Result: services cards now enter with a closer match to the who-we-are blur-lift cadence. **`npm run build`** succeeded.

### 93. Hero left text blocks — shift slightly right

**Ask:** Move the left-side text blocks in the `/client-showcase` hero a bit to the right.

**Done:** In **`ArcDesignHero`**, increased horizontal padding for the top metadata row and center headline/body row: **`px-4/sm:px-8/lg:px-10`** → **`px-6/sm:px-10/lg:px-12`**. This nudges all left hero copy inward while preserving hierarchy and overall composition. **`npm run build`** succeeded.

### 94. Hero left text blocks — shift further right

**Ask:** Move the hero left text more to the right.

**Done:** Increased the same padding again in **`ArcDesignHero`** for both top metadata and headline/body rows: **`px-6/sm:px-10/lg:px-12`** → **`px-8/sm:px-12/lg:px-16`**. **`npm run build`** succeeded.

### 95. Hero mobile viewport fit — avoid clipped bottom

**Ask:** On smaller screens, the bottom of the hero was still hidden within one viewport.

**Done:** Adjusted hero height formula in **`ArcDesignHero`** to use **`svh`** (stable viewport height with browser UI) and larger mobile/tablet header offsets: **`100dvh-8.75/7.75rem`** → **`100svh-11.25/9.5rem`** (desktop offset unchanged). New class: **`h-[min(calc(100svh-11.25rem),52rem)] sm:h-[min(calc(100svh-9.5rem),54rem)] lg:h-[min(calc(100svh-6.75rem),56rem)]`**. **`npm run build`** succeeded.

### 96. Hero bottom CTA row — move up

**Ask:** Move the two hero bottom actions (“Begin your arc” and “Inside the studio”) up a bit.

**Done:** Increased bottom padding on the hero bottom action row in **`ArcDesignHero`**: **`pb-3 sm:pb-4`** → **`pb-5 sm:pb-6 lg:pb-7`** (top padding unchanged). This lifts the CTA/link cluster upward while keeping alignment and spacing behavior intact. **`npm run build`** succeeded.

### 97. Hero bottom CTA row — move further up

**Ask:** Move the hero bottom action row up more.

**Done:** Increased bottom padding again in **`ArcDesignHero`** for the bottom action row: **`pb-5 sm:pb-6 lg:pb-7`** → **`pb-8 sm:pb-9 lg:pb-10`**. **`npm run build`** succeeded.

### 98. Header logo — much larger on all breakpoints

**Ask:** Make the logo in the client-showcase header way bigger on all screen sizes.

**Done:** Increased main-nav logo slot in **`ArcDesignHeader`** from **`h-9 w-8rem / sm:h-10 w-9rem`** to **`h-12 w-12rem / sm:h-14 w-14rem / lg:h-16 w-16rem`**, updated image **`sizes`** accordingly, and slightly tightened nav row vertical padding to keep balance. Because taller header changes first-fold space, adjusted **`ArcDesignHero`** viewport offset to maintain full hero fit in one screen: **`100svh-11.25/9.5/6.75rem`** → **`100svh-12.75/11/8.5rem`** (mobile/sm/lg). **`npm run build`** succeeded.

### 99. Header logo overflow only (keep navbar size + click area)

**Ask:** Keep navbar size unchanged; make logo visually larger only (transparent overflow allowed); keep clickable area as navbar logo slot.

**Done:** Restored original main-nav row sizing/padding and original logo link slot dimensions in **`ArcDesignHeader`**. Implemented larger logo as an absolutely positioned child span inside the link with **`pointer-events-none`**, so the artwork overflows visually while click target remains the original slot. Reverted **`ArcDesignHero`** viewport offsets back to pre-logo-enlarge values since nav height is unchanged. **`npm run build`** succeeded.

### 100. Main navbar background — ambient image

**Ask:** Use **`ambient-02.png`** as navbar background so the bar looks nicer.

**Done:** Updated the client-showcase main nav container in **`ArcDesignHeader`** to use **`/assets/decoration/background/ambient-02.png`** as a background image with a subtle white overlay for readability: **`linear-gradient(rgba(255,255,255,0.92), rgba(255,255,255,0.92)), url(...)`** plus **`background-size: cover`** and centered positioning. **`npm run build`** succeeded.

### 101. Main navbar — remove overlay, use text color contrast

**Ask:** Remove the overlay on ambient navbar background and change text color instead.

**Done:** In **`ArcDesignHeader`** main nav, removed gradient overlay and kept raw **`ambient-02.png`** background only. Updated nav link text to **white** with subtle text-shadow for readability, increased chevron visibility, changed desktop CTA to white-on-dark translucent treatment, and updated mobile menu toggle button to white contrast styles on the image background. **`npm run build`** succeeded.

### 102. Header logo overflow — increase size again

**Ask:** Make the logo bigger.

**Done:** Increased visual overflow logo dimensions in **`ArcDesignHeader`** while keeping navbar size/click area unchanged: **`h-16 w-14rem / sm:h-4.5rem w-16rem / lg:h-20 w-18rem`** → **`h-20 w-17rem / sm:h-5.5rem w-20rem / lg:h-24 w-22rem`**. Updated image **`sizes`** to match larger render targets. **`npm run build`** succeeded.

### 103. Main nav thicker + logo larger

**Ask:** Make the main nav bar a bit thicker and make the logo bigger.

**Done:** In **`ArcDesignHeader`**, increased main-nav vertical padding (**`py-3 / lg:py-4`** → **`py-3.5 / lg:py-5`**) for a thicker bar. Increased overflow logo render size to **`h-24 w-20rem / sm:h-28 w-24rem / lg:h-32 w-28rem`** and updated image **`sizes`** accordingly. Since nav height changed, adjusted **`ArcDesignHero`** viewport offset to preserve first-fold fit: **`100svh-11.25/9.5/6.75rem`** → **`100svh-11.75/10/7.25rem`**. **`npm run build`** succeeded.

### 104. Main nav thicker again

**Ask:** Make the nav bar thicker more.

**Done:** Increased main-nav vertical padding again in **`ArcDesignHeader`** to **`py-[1.125rem]`** on base and **`lg:py-6`** on desktop. Updated **`ArcDesignHero`** viewport offsets to maintain first-screen composition with taller header: **`100svh-11.75/10/7.25rem`** → **`100svh-12.25/10.5/7.75rem`**. **`npm run build`** succeeded.

### 105. Main nav thicker — follow-up

**Ask:** More thickness.

**Done:** **`ArcDesignHeader`** main row padding **`py-[1.125rem] lg:py-6`** → **`py-5 sm:py-[1.375rem] lg:py-7`**. **`ArcDesignHero`** offsets **`100svh-12.25/10.5/7.75rem`** → **`100svh-12.75/11/8.25rem`**. **`npm run build`** succeeded.

### 106. Client showcase — sticky nav follows scroll

**Ask:** Navbars should follow scrolling (stay with the page / viewport while scrolling).

**Done:** **`ArcDesignHeader`** already uses **`sticky top-0`**, but it was wrapped in **`ShowcaseSectionReveal`**’s **`motion.div`**, which breaks sticky positioning for descendants. Removed the reveal wrapper in **`ClientShowcaseHome`** so **`ArcDesignHeader`** is a direct child of **`#showcase-top`**; both utility + main nav rows now stick correctly on scroll. **`npm run build`** succeeded.

### 107. Client showcase — infinite concern keyword marquee under hero

**Ask:** Aesthetic infinite sliding loop below the hero with given concern keywords (bulleted list).

**Done:** Added **`showcaseConcernMarqueeKeywords`** in **`mock-page-content.ts`**. New **`ShowcaseConcernMarquee`** component: beige band, uppercase tracking, bullet + champagne **`✦`** separators, duplicated track + existing **`arc-marquee`** keyframes via new **`animate-showcase-concern-marquee`** (**52s** linear) in **`globals.css`** with reduced-motion off. Composed in **`ClientShowcaseHome`** immediately after **`ArcDesignHero`**. **`npm run build`** succeeded.

### 108. Showcase concern marquee — fix motion, thicker bar, rose-gold, larger type

**Ask:** Slider not animating; bar thicker and text bigger; bar using rose-gold like main site.

**Done:** Rebuilt **`ShowcaseConcernMarquee`** to mirror **`ScrollExpandHero`** ticker structure: **`animate-arc-marquee`** on the scrolling row, explicit **`style={{ width: \"max-content\", animationDuration: \"48s\" }}`**, and **`shrink-0`** / nested **`inline-flex`** spans so **`translate3d(-50%)`** loops correctly. Bar uses **`bg-arc-rose-gold`** + rose-glow shadow like production hero keyword strip; labels use larger serif uppercase charcoal (**`text-base sm:text-lg md:text-xl`**) with hero-style text-shadow; separators use **`·`**. Increased vertical padding (**`py-4 sm:py-5 md:py-6`**). Removed unused **`animate-showcase-concern-marquee`** from **`globals.css`**. **`npm run build`** succeeded.

### 109. Rose-gold emphasis — footer + main nav polish

**Ask:** Use gold rose color to emphasize text across the simplified (client showcase) site.

**Done:** Confirmed **`showcaseRoseClass`** / **`arc-rose-gold`** usage across showcase sections from prior pass. **`ArcDesignFooter`**: added missing **`import { cn } from \"@/lib/utils\"`**; applied **`showcaseRoseClass.ink`** to “Stay connected” and primary footer CTA; phone + legal links use rose ink / hover rose. **`ArcDesignHeader`**: desktop nav links **`hover:text-arc-rose-gold`**; “Book now” uses rose border + rose text on dark overlay; mobile menu links rose-tint hover; mobile “Book now” rose border/text. **`npm run build`** succeeded.

| Path | Notes |
|------|--------|
| `src/client-showcase/components/ArcDesignFooter.tsx` | `cn` import, rose on CTA / labels / links |
| `src/client-showcase/components/ArcDesignHeader.tsx` | Rose hovers + Book now treatment |

### 110. Client showcase — dark “science meets beauty” band after marquee

**Ask:** Add a section like the provided mock (dark two-column: eyebrow + headline + CTAs | three value cards) after the sliding keyword bar and above Who we are.

**Done:** Added **`showcaseScienceBeautyBand`** copy in **`mock-page-content.ts`**. New **`ShowcaseScienceBeautyBand`**: `#0a0a0a` panel, rose-gold eyebrow rule + label, serif headline with italic rose emphasis on “lasting”, grey subhead, outlined rose “Book a consultation” + “View our programs” with play control (links **`/#book`** and **`/#services`**). Right column: three bordered cards with italic index, **`Doctor`-Led**-style titles (lead word in **`showcaseRoseClass.bright`**). Composed in **`ClientShowcaseHome`** between **`ShowcaseConcernMarquee`** and **`WhoWeAreDesign`**, wrapped in **`ShowcaseSectionReveal`** **`delayIndex={2}`**. **`npm run build`** succeeded.

| Path | Notes |
|------|--------|
| `src/client-showcase/components/ShowcaseScienceBeautyBand.tsx` | New section UI |
| `src/client-showcase/mock-page-content.ts` | `showcaseScienceBeautyBand` |
| `src/client-showcase/ClientShowcaseHome.tsx` | Composition + reveal |

### 111. Science-beauty band — in-section scroll reveal

**Ask:** The dark science/beauty band should fade in with the same reveal behavior as other sections.

**Done:** **`ShowcaseScienceBeautyBand`** is now a **client** component with Framer Motion: **`whileInView`** on **`motion.section`**, same viewport/margin/easing as **`ShowcaseSectionReveal` “rich”** (`opacity` + `y` + `blur`), plus **`staggerChildren`** so the left column then each value card steps in. **`prefers-reduced-motion`**: static section, no animation. Removed outer **`ShowcaseSectionReveal`** wrapper from **`ClientShowcaseHome`** to avoid double animation and match **`WholeBodyFiveCards`** (section owns its reveal). **`npm run build`** succeeded.

| Path | Notes |
|------|--------|
| `src/client-showcase/components/ShowcaseScienceBeautyBand.tsx` | `motion.section` + staggered `blurLift` |
| `src/client-showcase/ClientShowcaseHome.tsx` | Direct compose, no wrapper |

### 112. About / Who we are — affordability & fineness copy

**Ask:** Replace **`WhoWeAreRevealGrid`** (#about) content with Section 3 theme (“Balance of Affordability with Fineness”), headline “Wellness, Made Personal,” and supplied body about medical-grade care, pricing, and “Indulgent, yes. Exclusive? Never.”

**Done:** Updated **`whoWeAreCopy`** in **`mock-page-content.ts`**: eyebrow **`Balance of affordability & fineness`**, headline parts **`Wellness, Made `** / rose **`Personal`** / **`.`**, two paragraphs from the provided copy (typographic apostrophes). No component changes. **`npm run build`** succeeded.

| Path | Notes |
|------|--------|
| `src/client-showcase/mock-page-content.ts` | `whoWeAreCopy` |

### 113. About section — visual polish (WhoWeAreRevealGrid)

**Ask:** Make the #about / Who we are block look nicer.

**Done:** **`WhoWeAreDesign`**: section uses **`showcaseDesign.beige`** (matches whole-body band), increased vertical rhythm **`py-20 sm:py-24 lg:py-28`**. **`WhoWeAreRevealGrid`**: rose vertical rule + **`text-balance`** + tighter tracking for long eyebrow; headline larger clamp, **`font-normal`** serif, slight negative tracking, emphasized segment **`italic` + `font-medium`**; first body paragraph as clean sans; closing paragraph(s) as editorial **callout** (serif, light panel, rose left border); CTA soft lift shadow; grid **`items-start`** / **`lg:items-center`**, wider gaps; image **`rounded-2xl`**, softer shadow + subtle ring. **`npm run build`** succeeded.

| Path | Notes |
|------|--------|
| `src/client-showcase/components/WhoWeAreDesign.tsx` | Beige surface, padding |
| `src/client-showcase/components/WhoWeAreRevealGrid.tsx` | Typography + callout + image frame |

### 114. #services / WholeBodyFiveCards — visuals + motion

**Ask:** Improve the whole-body services section visually and add more animation; improve card design.

**Done:** **`WholeBodyFiveCards`**: semantic **`section`** + inner **`motion.div`** with **`whileInView`** so stagger runs correctly; headline block uses stronger **blur/lift** reveal; grid uses nested **stagger** + per-card **`cardReveal`** (opacity, **y**, **scale**, blur); cards **`whileHover`** lift (**`-10px`**). Card UI: **`rounded-2xl`**, layered shadow + hover shadow, highlighted card **rose ring**; image **gradient scrim**, **italic index** (01–05), **`group-hover` image scale** + slight brightness; icon in **rose-tint** circle with hover scale; bullet dots with soft rose halo; **Learn more** arrow nudges on hover. Section padding aligned with About (**`py-20 sm:py-24 lg:py-28`**). Added **`wholeBodySectionSubline`** in **`mock-page-content.ts`** + shared **`WholeBodyHeadline`**. **`npm run build`** succeeded.

| Path | Notes |
|------|--------|
| `src/client-showcase/components/WholeBodyFiveCards.tsx` | Layout, motion, cards |
| `src/client-showcase/mock-page-content.ts` | `wholeBodySectionSubline` |

### 115. #services — five-pillar row (reference mock)

**Ask:** Recreate the whole-body section like the provided “quietly orchestrated” mock: split header, mono labels, N° badges, forest highlight column, “Most chosen,” quotes, EXPLORE →.

**Done:** **`design-tokens`**: **`pillarForest`**, **`pillarPeach`**, **`pillarSurface`**. **`mock-page-content`**: new **`WholeBodyCardData`** (`pillarLabel`, `quote`, `mostChosen`; removed icon/summary), **`wholeBodySectionHeading`**, five pillars with reference-aligned titles/lists/quotes (card 04 therapeutic line softened for clinical tone). **`WholeBodyFiveCards`**: cream **`pillarSurface`**; **two-column** header (serif title / sans supporting, right-aligned on lg); **teal dot**; **five columns** on lg with **vertical dividers**, stacked cards on small screens; each pillar: **mono label + rule**, **N°** pill on image, **Most chosen** peach pill on pillar 03, **serif title**, dash bullets, **italic quote**, **EXPLORE →**; **forest** column with white type + ring. Motion: outer stagger (header → dot → grid), **card stagger** + hover lift. **`npm run build`** succeeded.

| Path | Notes |
|------|--------|
| `src/client-showcase/design-tokens.ts` | Pillar palette |
| `src/client-showcase/mock-page-content.ts` | `wholeBodyCards`, `wholeBodySectionHeading` |
| `src/client-showcase/components/WholeBodyFiveCards.tsx` | Layout + motion |

### 116. Science-beauty band — scroll reveal fix

**Ask:** Dark “Medical aesthetics & wellness” band still wasn’t fading in on scroll like other sections.

**Done:** **`staggerChildren` on `motion.section` did not apply** because **non-motion wrapper `div`s** sat between the stagger parent and **`motion.div` / `motion.article` children**, so child variants never staggered reliably. Switched to semantic **`<section>`** + single **`motion.div`** that **is** the grid (`grid lg:grid-cols-2` on the same node) so **direct children** are **`motion.div` (copy)** + three **`motion.article`**s. Renamed stagger variant to **`staggerRoot`**, slightly increased **`viewport.amount`** (**`0.18`**) and blur/lift (**`y: 22`**, **`blur(8px)`**). **`npm run build`** succeeded.

| Path | Notes |
|------|--------|
| `src/client-showcase/components/ShowcaseScienceBeautyBand.tsx` | Motion tree flattening |

### 117. #services — ambient background plate

**Ask:** Use the nice ambient background from assets on the whole-body / services section.

**Done:** **`WholeBodyFiveCards`**: **`relative overflow-hidden`** section; **`ServicesAmbientBackdrop`** with **`next/image`** **`ambient-04.png`** (same family as **`ArcFooter`**), **`object-cover`** / **`object-[center_25%]`**, modest opacity; cream **linear-gradient wash** (RGB aligned to **`pillarSurface`**) so pillars and type stay readable. Content in **`relative z-[1]`** wrapper (both reduced-motion and motion trees). **`npm run build`** succeeded.

| Path | Notes |
|------|--------|
| `src/client-showcase/components/WholeBodyFiveCards.tsx` | `SERVICES_AMBIENT_BG`, backdrop |

### 118. Client showcase — remove Patients + Shop bands

**Ask:** Remove **`#patients`** and **`#shop`** sections from the showcase page.

**Done:** Removed **`PatientsAndShopBands`** from **`ClientShowcaseHome`** and deleted **`PatientsAndShopBands.tsx`**. Dropped **Patients** and **Shop** from **`ArcDesignHeader`** **`MAIN_LINKS`**. Renumbered **`ShowcaseSectionReveal`** **`delayIndex`** for prefooter (**`5`**) and footer (**`6`**). **`npm run build`** succeeded.

| Path | Notes |
|------|--------|
| `src/client-showcase/ClientShowcaseHome.tsx` | Composition |
| `src/client-showcase/components/ArcDesignHeader.tsx` | Nav |
| `src/client-showcase/components/PatientsAndShopBands.tsx` | **Removed** |

### 119. Unified book CTAs (rose outline)

**Ask:** Use the **`ShowcaseScienceBeautyBand`** outlined rose-gold button style for utility **Book here**, About CTA, timeline CTA, pre-footer **Book now**, footer **Book here**, and align header **Book now** / mobile book with the same system.

**Done:** Added **`showcase-book-cta.ts`** with **`showcaseBookCtaClass("light" | "dark", className?)`** — border-2, uppercase, **`0.16em`** tracking; **dark** = **`showcaseRoseClass.bright`** + rose border/hover tint; **light** = **`showcaseRoseClass.ink`** + ink border/hover tint. Wired into **`ShowcaseScienceBeautyBand`**, **`ArcDesignHeader`** (utility compact padding, desktop, mobile), **`WhoWeAreRevealGrid`**, **`ProcessTimelineDesign`**, **`SplitPrefooterCTA`**, **`ArcDesignFooter`**. Removed sage fill **`style`** on those links. **`npm run build`** succeeded.

| Path | Notes |
|------|--------|
| `src/client-showcase/showcase-book-cta.ts` | New helper |
| Multiple `components/*` | CTA class swaps |

### 120. `#path` — vertical forest timeline (reference art)

**Ask:** Build “Your path to feeling and living at your best” like the reference: dark forest, rose-gold timeline (roman **i–v**), step meta, descriptions; use image where appropriate.

**Done:** Rebuilt **`ProcessTimelineDesign`**: **`pillarForest`** base + **`PATH_SECTION_INTRO_BACKGROUND_SRC`** (`path-intro-background.png`) at low opacity + **`mix-blend-soft-light`** + gradient veil for depth; headline white + **`showcaseRoseClass.bright`** on Path/Best; **`showcasePathSteps`** from **`content.ts`** (Listen → Revise); each row flex layout with rose-gold circle (lowercase roman), connector segment, serif title + mono **`stepMeta**`, sans description; subtle **`border-t`** dividers; bottom centered **`showcaseBookCtaClass("dark")`** “Start your journey”. Removed horizontal **`processTimelineSteps`** card strip from this component ( **`processTimelineSteps`** remains in **`mock-page-content`** if needed elsewhere). **`npm run build`** succeeded.

| Path | Notes |
|------|--------|
| `src/client-showcase/components/ProcessTimelineDesign.tsx` | Full rewrite |

### 121. `#book` — membership cohort full-bleed (reference art)

**Ask:** Recreate the Arcwell-style band: full-bleed dark clinical photo, left eyebrow + headline + body, bottom-right **’26**, cohort line, ghost **Reserve a call** + solid rose **Become a member →**.

**Done:** Copied user PNG to **`public/assets/hero/membership-cohort-hero.png`**; **`site.ts`** **`membershipCohortHero`**. **`showcaseMembershipBandCopy`** in **`mock-page-content.ts`**. Rebuilt **`SplitPrefooterCTA`**: single full-width **`Image`** + layered gradients; left stack (rose-gold eyebrow, white serif headline, sans body); bottom cluster right-aligned large year + uppercase cohort line + **`rounded-full`** ghost white border + **`bg-arc-rose-gold`** primary with **`ArrowRight`**; **`ClientShowcaseHome`** passes **`images.membershipCohortHero`**. Both CTAs → **`/#contact`** for now (comment in component). **`npm run build`** succeeded.

| Path | Notes |
|------|--------|
| `public/assets/hero/membership-cohort-hero.png` | User asset |
| `src/content/site.ts` | `membershipCohortHero` |
| `src/client-showcase/mock-page-content.ts` | `showcaseMembershipBandCopy` |
| `src/client-showcase/components/SplitPrefooterCTA.tsx` | Layout |
| `src/client-showcase/ClientShowcaseHome.tsx` | Image prop |

### 122. `#book` background — biometric consultation room

**Ask:** Use **`biometric-consultation-room.png`** for the membership band background.

**Done:** **`images.membershipCohortHero`** → **`/assets/sections/who-we-are/biometric-consultation-room.png`**. **`SplitPrefooterCTA`** image **`object-position`** set to **`object-center`** (fits interior framing). **`npm run build`** implied via quick verify.

### 123. `#book` — typography (reference layout)

**Ask:** Match text design: rose rule + eyebrow, three-line headline with italic **one**, muted gray body, darker overlay.

**Done:** **`showcaseMembershipBandCopy`**: split **`headlineLine1` / `headlineLine2` / `headlineItalicWord` / `headlineAfterItalic`**. **`SplitPrefooterCTA`**: flex row with **`h-px`** rose rule + **`uppercase`** eyebrow (**`text-arc-rose-gold`**, wide tracking); headline **`#f5f5f5`** serif blocks + **`<em>`** on **one**; body **`#cccccc`** sans, relaxed leading; added **`bg-black/45`** base scrim + tuned gradients.

### 124. Client showcase `#book` — tighten section vertically

**Ask:** Reduce vertical height / padding for the membership band (`#book`) so the section reads shorter.

**Done:** In **`SplitPrefooterCTA.tsx`**: lowered **`min-h`** on the photo shell and inner flex wrapper from **`~92svh / 52rem`** down to **`~76svh / 42rem`** (with proportional **`sm`** / **`lg`** caps); reduced section padding (**`pt`/`pb`**); tightened margins between headline, body, and bottom cohort row (**`mt-*`**); reduced gap between cohort stack and CTAs; slightly lowered the decorative year **`clamp`** max so the bottom block occupies less vertical space. Verified with **`npm run build`**.

| Path | Notes |
|------|--------|
| `src/client-showcase/components/SplitPrefooterCTA.tsx` | Heights, padding, gaps, year scale |

### 125. Client showcase `#book` — headline line spacing

**Ask:** Increase vertical spacing between the membership band headline lines (“The next decade” / “starts with” / “one quiet hour.”).

**Done:** **`SplitPrefooterCTA.tsx`**: **`h2#membership-band-heading`** is now **`flex flex-col`** with responsive **`gap-2.5` / `sm:gap-3` / `lg:gap-4`** between the three **`span.block`** rows; removed the tight **`mt-0.5`** on the third row so spacing comes from the shared gap.

**Follow-up:** User asked for **more** line spacing — gaps increased to **`gap-4` / `sm:gap-5` / `lg:gap-6`**.

| Path | Notes |
|------|--------|
| `src/client-showcase/components/SplitPrefooterCTA.tsx` | Headline **`flex` + `gap`** |

### 126. `#services` — rose-gold section title + pillar card titles

**Ask:** Apply rose-gold treatment to **Whole-body care, quietly orchestrated.** (`WholeBodySectionHeader` **`h2`**) and each pillar **`h3`** (**`ServicePillarCard`**).

**Done:** **`WholeBodyFiveCards.tsx`**: imported **`showcaseRoseClass`**; **`h2`** and pillar **`h3`** use **`showcaseRoseClass.bright`** (dark-surface rose per design tokens) instead of **`text-white`** + neutral shadow. **`npm run build`** succeeded.

| Path | Notes |
|------|--------|
| `src/client-showcase/components/WholeBodyFiveCards.tsx` | **`showcaseRoseClass.bright`** on section headline + card titles |

**Follow-up:** Rose-gold also applied to **italic quote** and **Explore →** link on **all** pillars (including highlighted forest column — previously white).

### 127. `#services` section headline — dark rose gold

**Ask:** **Whole-body care, quietly orchestrated.** should use **dark** rose gold (not luminous **`bright`**).

**Done:** **`design-tokens.ts`**: added **`showcaseRoseClass.deep`** — **`text-arc-rose-gold-ink`** with shadows tuned for dark ambient imagery (no **`--arc-rose-gold-glow`** champagne halo). **`WholeBodyFiveCards`** **`WholeBodySectionHeader`** **`h2`** now uses **`showcaseRoseClass.deep`**. **`npm run build`** succeeded.

### 128. `#services` pillar bullets — bolder + larger

**Ask:** Bullet labels (e.g. Skin, Hormones) **bold** and **bigger** on every pillar card.

**Done:** **`WholeBodyFiveCards`** **`ServicePillarCard`**: list uses **`text-[13px] font-bold sm:text-[15px]`**, **`space-y-2.5`**, slightly stronger white opacity; **`li`** rows **`items-center`**, wider dash rule (**`w-3.5`**). **`npm run build`** succeeded.

### 129. Client showcase — `#testimonials` + `#faq` after `#path`

**Ask:** After **`#path`**, add **Testimonials** and **FAQ** sections aligned with the rest of **`/client-showcase`**.

**Done:** **`mock-page-content.ts`**: **`showcaseTestimonials`** (four cards), **`showcaseTestimonialsHeading`**, **`showcaseFaqItems`**, **`showcaseFaqHeading`**. New **`ShowcaseTestimonialsSection`** (`id="testimonials"`) — **`showcaseDesign.beige`**, **`max-w-[1400px]`**, rose rule + eyebrow, serif title with **`showcaseRoseClass.ink`**, 2×2 card grid (white panels, rose attribution). New **`ShowcaseFaqSection`** (`id="faq"`) — **`pillarSurface`** background, two-column layout (sticky intro on lg), native **`<details>`** accordion with **`Plus`** + **`group-open:rotate-45`**. **`ClientShowcaseHome`**: inserted both inside **`ShowcaseSectionReveal`** with staggered **`delayIndex`**; shifted **`SplitPrefooterCTA`** / footer delays to **7** / **8**. **`ArcDesignHeader`**: **`MAIN_LINKS`** now includes **Your path**, **Testimonials**, **FAQ** (removed duplicate Conditions→services link). **`npm run build`** succeeded.

| Path | Notes |
|------|--------|
| `src/client-showcase/mock-page-content.ts` | Testimonials + FAQ copy |
| `src/client-showcase/components/ShowcaseTestimonialsSection.tsx` | New |
| `src/client-showcase/components/ShowcaseFaqSection.tsx` | New |
| `src/client-showcase/ClientShowcaseHome.tsx` | Compose + reveal indices |
| `src/client-showcase/components/ArcDesignHeader.tsx` | Nav hashes |

### 130. `#testimonials` — carousel + expandable cards (reference UI)

**Ask:** Replace the testimonials grid with the supplied carousel / card / modal interaction pattern; align with client-showcase styling.

**Done:** Added **`ShowcaseTestimonialsCarousel.tsx`** (`use client`): horizontal **`overflow-x`** strip with staggered **`motion`** entry, **`ArrowLeft`/`ArrowRight`** controls (**`bg-arc-charcoal`** / cream icons), tall gradient cards (**`showcaseDesign.beige`** → cream, subtle **`ambient-01`** plate via **`showcaseTestimonialCardAmbient`** in **`mock-page-content.ts`**), circular portraits, excerpt → full quote on click. Expanded view **`createPortal`** to **`document.body`** (avoids parent **`motion`** transform breaking **`fixed`**), backdrop **`bg-arc-charcoal/55`**, serif modal body + **`Quote`** icon (**`text-arc-rose-gold-ink`**), scroll lock + Escape / outside click / overlay click to close. **`prefers-reduced-motion`**: no 3D hover tilt. **`ShowcaseTestimonialsSection`** now renders the carousel only (header unchanged). **`npm run build`** succeeded.

| Path | Notes |
|------|--------|
| `src/client-showcase/components/ShowcaseTestimonialsCarousel.tsx` | New |
| `src/client-showcase/mock-page-content.ts` | **`showcaseTestimonialCardAmbient`** |
| `src/client-showcase/components/ShowcaseTestimonialsSection.tsx` | Compose carousel |

**Follow-up:** Testimonial carousel **loops** — prev/next wrap with **`scrollToIndex` modulo**; **auto-advance** every **6s** (off when reduced motion or only one item); **pause** on strip **hover**. Arrow controls **`justify-center`**. Step width measured from **`data-carousel-track`** first card + gap.

**Follow-up:** Removed **looping** — no auto-advance interval; prev/next **clamp** at first/last slide; arrows **`disabled`** at ends.

### 131. `#services` section title — bolder + larger

**Ask:** **Whole-body care, quietly orchestrated.** should be **bold** and **a bit bigger**.

**Done:** **`WholeBodySectionHeader`** **`h2`**: **`font-normal` → `font-semibold`**, **`text-[clamp(...)]`** increased (**`1.95rem / 4vw / 3.15rem`**), **`leading-[1.1]`**, **`max-w-xl` → `max-w-2xl`** for the wider headline line. **`npm run build`** succeeded.

**Follow-up:** Section title rose treatment **`deep` → `bright`** (luminous **`text-arc-rose-gold`** + glow, not ink).

### 132. `#testimonials` / `#faq` eyebrow rows — more visible

**Ask:** **Patient voices** and **Questions** eyebrow strips easier to see.

**Done:** **`ShowcaseTestimonialsSection`** + **`ShowcaseFaqSection`**: rule **`h-px` → `h-0.5`**, **`rounded-full`**, longer (**`w-10 sm:w-12`**), **`bg-arc-rose-gold-ink/90`**; label **`text-[11px] font-bold sm:text-xs`**, **`text-arc-rose-gold-ink`**, slightly relaxed tracking. **`npm run build`** succeeded.

### 133. Testimonial carousel cards — ambient restored; no cream overlay

**Ask:** Keep **`ambient-01`** as before; avoid a white / cream overlay on top of it.

**Done:** **`showcaseTestimonialCardAmbient`** restored in **`mock-page-content.ts`**. Collapsed cards: full-bleed **`ambient-01`** (**no** beige linear-gradient wash). Legibility via **dark-only** gradient (**`from-black/40 via-black/15 to-black/55`**), not white. Type on photo: **`text-white`**, context **`text-arc-rose-gold`** + shadows; portrait ring **`border-white/45`**; card rim **`border-white/18`**. **`npm run build`** succeeded.

### 134. Client-showcase hero — quote + studio link to white

**Ask:** Set the top-right quote (“To age is a privilege…”) and the **Inside the studio** link row (including duration) to **white** instead of rose-gold / tinted white.

**Done:** In **`ArcDesignHero.tsx`**: quote span replaced **`showcaseRoseClass.bright`** with **`text-white`** plus a light **`text-shadow`** for readability on photography; **Inside the studio** row uses **`text-white`** on the link wrapper and video line/duration; play-circle **`border-white/85`** / **`text-white`** instead of rose-gold. Headline italic accent unchanged (**`showcaseRoseClass.bright`**). **`npm run build`** succeeded.

## Paths / artifacts

| Path | Notes |
|---|---|
| `src/client-showcase/components/ArcDesignHero.tsx` | Quote + studio CTA white styling |
