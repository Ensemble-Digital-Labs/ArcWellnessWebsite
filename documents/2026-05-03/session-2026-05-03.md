# Session log – 2026-05-03

### 1. Title keyword emphasis (signature font)

**Ask:** Use a handwritten / signature-style font for emphasizing keywords in titles.

**Done:** Loaded **Birthstone** via `next/font/google` with CSS variable `--font-birthstone` on `<html>` in `src/app/layout.tsx`. Added global utility `.font-title-emphasis` in `src/app/globals.css` (slightly larger `1.12em`, normal weight, `not-italic` on the span). Added `TitleEmphasis` in `src/components/arc/TitleEmphasis.tsx` wrapping children in that class. Production `npm run build` succeeded.

**Usage:** Inside any existing `font-serif` heading, wrap the keyword(s):

```tsx
import { TitleEmphasis } from "@/components/arc/TitleEmphasis";

<h2 className="font-serif text-3xl font-semibold ...">
  Care That&apos;s <TitleEmphasis>Intentional</TitleEmphasis>. Results That Last.
</h2>
```

| Path | Notes |
|------|--------|
| `src/app/layout.tsx` | `Birthstone`, `weight: "400"`, `birthstone.variable` on `<html>` |
| `src/app/globals.css` | `.font-title-emphasis` |
| `src/components/arc/TitleEmphasis.tsx` | Reusable span |

**Follow-ups:** Swap Birthstone for another Google script in `layout.tsx` if you want a different voice; keep emphasis to short phrases for readability.

### 2. Hero title keywords in Birthstone

**Ask:** Apply the emphasis font to the hero keywords (“Aesthetics,” “Wellness,” “Longevity”) in “Where Aesthetics, Wellness, and Longevity Converge.”

**Done:** `ScrollExpandHero` now splits the second line of the title with `emphasizeTitleWords()` (whole-word regex), wrapping matches in `TitleEmphasis`. Defaults: `Aesthetics`, `Wellness`, `Longevity`. New optional prop `titleKeywords` (pass `[]` to disable). `page.tsx` unchanged.

| Path | Notes |
|------|--------|
| `src/components/arc/ScrollExpandHero.tsx` | `DEFAULT_HERO_TITLE_KEYWORDS`, `emphasizeTitleWords`, `restWithEmphasis` |

### 3. Hero keywords — larger + accent color

**Ask:** Hero script keywords larger than surrounding serif and use an emphasized color (not flat white).

**Done:** Added `HERO_TITLE_KEYWORD_EMPHASIS_CLASS` on hero `TitleEmphasis` spans: responsive `em` scale (~1.36–1.64× parent), `text-arc-teal`, dual-layer `text-shadow` for legibility on photos. `emphasizeTitleWords` takes optional `emphasisClassName`. If `textBlend` is true, uses warm off-white + shadow instead of teal (difference-blend–safer).

| Path | Notes |
|------|--------|
| `src/components/arc/ScrollExpandHero.tsx` | `HERO_TITLE_KEYWORD_EMPHASIS_CLASS`, third arg to `emphasizeTitleWords` |

### 4. Hero keywords — larger vs serif

**Ask:** Teal script keywords should read clearly bigger than white serif words.

**Done:** Increased `HERO_TITLE_KEYWORD_EMPHASIS_CLASS` scale to ~`1.55em`–`2.05em` (was ~`1.36em`–`1.64em`); matched `textBlend` branch proportionally; slightly stronger teal glow in shadow.

### 5. Hero keyword size not applying (cascade fix)

**Ask:** Keyword size still looked unchanged in the browser.

**Done:** `.font-title-emphasis` in `globals.css` had `font-size: 1.12em` in plain CSS, which loaded after Tailwind and overrode hero `text-[*em]` utilities (`tailwind-merge` does not merge custom classes with utilities). Removed size/letter-spacing from `.font-title-emphasis`; moved default `text-[1.12em] tracking-tight` into `TitleEmphasis` so hero classes merge and win.

### 6. Hero title line — baseline alignment for big script

**Ask:** Teal keywords should stay aligned with white serif words on the same line as they scale up.

**Done:** Second hero line `motion.span` is now `flex flex-wrap items-baseline justify-center` with `max-w` and `leading-snug` so wrapped lines breathe. Keyword spans use `shrink-0` (no `inline-block` / `align-middle`). `emphasizeTitleWords` skips empty string chunks between matches.

### 7. Site header — remove utility bar + main nav

**Ask:** Remove the teal top strip (specials, phone, book here, socials) and the white nav row (About, Services, …, Book Now); user will replace with a better nav later.

**Done:** `SiteHeader` is now a single white bar with logo → home only. Exported `SITE_HEADER_OFFSET` (`4.75rem`) for layout sync. `ArcScrollShell` scroll content + reduced-motion wrapper use `paddingTop: SITE_HEADER_OFFSET`. `ScrollExpandHero` inner column uses `minHeight: calc(100dvh - SITE_HEADER_OFFSET)` instead of hard-coded `7.25rem`.

### 8. Logo — `arc-wellness-logo.png` with real alpha

**Ask:** Use `arc-wellness-logo.png` and make the background transparent.

**Done:** Source PNG was RGB (no alpha). Added devDependency **sharp** and `scripts/make-logo-transparent.cjs`: estimates background from corner average (~cream), soft-thresholds distance to alpha, writes RGBA PNG back to `public/assets/branding/arc-wellness-logo.png`. Verified IHDR color type 6. `images.logo` points at that path; `SiteHeader` drops `mix-blend-multiply` and `unoptimized`. `package.json` script `logo:transparent` re-runs the processor if the source art is replaced.

### 9. Fullscreen GSAP nav (Webflow-style)

**Ask:** Integrate pasted fullscreen menu (GSAP, overlay, ambient shapes, menu links) for ARC.

**Done:** New `ArcSiteHeader` (`src/components/arc/ArcSiteHeader.tsx`): centered logo + **Menu** pill (top-right), dim overlay, right drawer with staggered backdrop strips + link stagger, hover-driven ambient SVG shapes per `data-shape`, Escape + overlay click close, `#main` scroll lock when open, `prefers-reduced-motion` instant toggle, `inert` when closed. Uses **power3** easing (no **CustomEase** — Club plugin). `NAV_LINKS` → `/#about`, `/#services`, `/#path`, `/#book`, `/#contact`. `YourPathSection` gained `id="path"`. `SiteHeader.tsx` re-exports `ArcSiteHeader` as `SiteHeader` + `SITE_HEADER_OFFSET`. Global rule `.arc-bg-shape .arc-shape-el` in `globals.css`.

### 10. Fullscreen nav — remove “Navigate” label

**Ask:** Remove the uppercase **NAVIGATE** `<p>` above the link list in the slide-out nav (`ArcSiteHeader`).

**Done:** Deleted that paragraph in `src/components/arc/ArcSiteHeader.tsx`; link `<ul>` remains first content in the column. `npm run build` succeeded.

### 11. Nav preview — interactive hover (Motion parallax + UI)

**Ask:** Upgrade the laptop nav preview to behave like an interactive hover links demo (Motion values / springs, floating image, letter stagger, arrow).

**Done:** In `ArcSiteHeader.tsx`, **lg+** preview column now uses a **floating card** (`next/image` in a rounded, shadowed frame) positioned with **`useMotionValue` + `useSpring` + `useTransform`** so **cursor movement over each nav link** drives **top/left** parallax (same normalization as the reference snippet). **`AnimatePresence`** + spring on index change replaces the old GSAP preview fly-in. **`navLinkHovered`** tilts/scales the card slightly when any link is hovered. **`prefers-reduced-motion`:** full-bleed crossfade only, no parallax; link row uses plain text + CSS arrow. Nav rows: bottom border, **`lucide-react` `ArrowRight`** slide-in via variants, **per-letter** spring stagger. Imports: `framer-motion`, `ArrowRight`. Removed `previewFlyDesktopRef` GSAP effect.

### 12. Nav — drop left column; preview on hovered row only

**Ask:** Remove the split layout’s left preview panel; show the image on the nav row being hovered (interactive hover style).

**Done:** Removed the left **`lg:`** preview column entirely; drawer is single-column (`max-w` ~28rem / ~32rem sm). Extracted **`ArcNavMenuLinkRow`**: each **`Link`** owns **`useMotionValue` / springs / transforms** for pointer-driven **`top`/`left`** on an absolutely positioned **`next/image`** card that **springs in only while that row is hovered** (`navLinkPreviewVariants`). Removed shared preview index / shared parallax state / **`AnimatePresence`** block from header. Reduced-motion path unchanged (no floating image).

### 13. Video assets — folder layout + `site.ts`

**Ask:** Organize newly added video files under assets.

**Done:** Six **`.mov`** files were at repo root; moved into **`public/assets/videos/`** with short names: **`hero/spa-background-oriental-therapy.mov`**, **`environment/spa-room-massage-bed.mov`**, **`treatments/`** (`face-care-cosmetologist`, `face-massage-relaxing`, `face-head-massage`), **`lifestyle/body-care-composition.mov`**. Extended **`public/assets/README.md`** (video table + usage). Exported **`videos`** from **`src/content/site.ts`** pointing at `/assets/videos/...` paths. `npm run build` succeeded.

### 14. Post-hero “chapter intro” — pin + scrub animations

**Ask:** Match hero-style scroll choreography (image pan, moving text) on the first section after the hero; use pinned sections as a base for future chapter designs.

**Done:** Added **`ScrollChapterIntroSection`** (`src/components/arc/ScrollChapterIntroSection.tsx`): same GSAP **`ScrollTrigger`** pattern as **`ScrollExpandHero`** (`#main` scroller, **pin**, **`scrub: 0.85`**, **`onUpdate` → progress**). Progress drives: horizontal **pan** on a **`next/image`** panel (`images.whoWeAre`), **teal vertical rule** scale-in, **headline + body** opacity and **`translateY` drift**. Decorative **“01”** watermark; gradient overlays for legibility; mobile uses a **bottom image strip** + top content padding. **`prefers-reduced-motion`:** static layout, progress forced to **1**, no scrub hook. **`page.tsx`:** replaced **`PinnedSection`** block with **`ScrollChapterIntroSection`** (`id="chapters"`, headline **Care, chapter by chapter**, existing body copy). Removed **`data-scroll-section`** from this block (scrub owns motion). `npm run build` succeeded.

### 15. Design intent — scroll “chapters” (user confirmation)

**Ask:** Record what they want for section-by-section scroll animation going forward.

**Done:** Added **`documents/scroll-chapters-design-intent.md`** — pinned sections, hero-like **scrubbed** motion (image pan, moving type), one viewport beat per chapter, GSAP + `#main` pattern, reduced-motion note, pointer to existing hero + chapter intro components. Session log cross-reference here.

### 16. Ultra-wide layout cap (2560 ≈ 1440 look)

**Ask:** At 2560px viewport, keep the same composition as at 1440px.

**Done:** **`src/lib/arc-layout.ts`:** **`ARC_SITE_MAX_WIDTH_PX = 1440`**, **`ARC_SITE_FRAME_CLASS`** (centered **`max-w-[min(100%,1440px)]`**, **`[transform:translateZ(0)]`** so **`position:fixed`** header/nav stay inside the rail), **`ARC_PAGE_RAIL_MAX`** aligned to the same cap (removed **`min-[2200px]`** narrower rail). **`layout.tsx`:** wraps **`{children}`** in the site frame. **`ScrollExpandHero`:** hero background image no longer **`w-screen`/`h-screen`** — uses **`w-full min-h-[100dvh] object-cover`** so it doesn’t bleed past the frame. `npm run build` succeeded.

### 17. Revert global 1440 “pill” frame (ultra-wide gutters)

**Ask:** Layout looked weird (large side gutters / letterboxed column at 2560).

**Done:** Removed **`ARC_SITE_FRAME_CLASS`** and **`layout.tsx`** wrapper so the site is **full-width** again. Kept **`ARC_PAGE_RAIL_MAX`** / **`ARC_SITE_MAX_WIDTH_PX`** for **inner** rails only (hero column, header inner). Comments in **`arc-layout.ts`** document not using a global capped shell. Hero bg still **`w-full min-h-[100dvh] object-cover`** (full bleed when parent is viewport-wide).

### 18. Site section content inventory (for deepening copy)

**Ask:** List each section’s current context/copy; site feels shallow for next content pass.

**Done:** Added **`documents/site-section-content-inventory.md`** — table-style breakdown of global chrome, hero, chapter intro, who we are, whole-body cards, patient path steps, invest CTA, footer (including placeholder links/phone/address); **depth gaps** per block; **file map** for where to edit. No runtime code changes.

### 19. Homepage IA — reference site vs fewer “concentrated” ARC sections

**Ask:** Reference a dense med-spa homepage (Avani-style screenshots); want similar storytelling with **fewer** sections, **more concentrated** blocks, and some sections that are **only USP + catchphrase**.

**Done:** Added **`documents/homepage-section-model-reference.md`** — lists reference **patterns** (trust row, welcome split, micro headlines, etc.), proposes an **8–9 section** ARC flow including **two micro “catchphrase + USP only”** strips, shorter pillars/path, trust strip when data exists; maps to **current components** to replace/trim; **dev spec** for micro-sections; notes keeping **ARC teal/cream** not reference gold. No app code changes.

### 20. Second section layout + header logo position

**Ask:** Implement post-hero section like reference (split copy / photo); **pin + scroll animation** per section; **avoid logo top-center** — use space better.

**Done:** **`ArcSiteHeader`:** rail uses **`justify-between`** — logo **top-left** (smaller max widths), **Menu** top-right (no `absolute` to rail center). **`ScrollExpandHero`:** intro block gets **extra left padding** so it clears the left logo. **`ScrollChapterIntroSection`:** rebuilt as **split narrative** — full-bleed background image with **scrubbed pan/scale**, **dual gradient** (vertical on mobile, horizontal sweep on `md+`), left **copy column** (`ARC_PAGE_RAIL_MAX`) with vertical rule, headline/body scrub, **“Continue to who we are”** link → **`/#about`**; right flex spacer so photography reads on the open side; pin end **`~1.12×`** viewport for slightly longer locked beat; **`scrub: 0.75`**. `npm run build` succeeded.

### 21. Implement concentrated homepage IA (micro strips + welcome + trust)

**Ask:** Implement the **`homepage-section-model-reference`** plan: fewer sections, concentrated copy, **micro-sections** (catchphrase + one USP line), **3** pillars and **3** path steps, invest supporting line; wire new components into the home page.

**Done:** **`src/app/page.tsx`:** Flow is **Hero** → **`ArcMicroStatementSection`** (`homeMicro1`) → **`ArcWelcomeSplitSection`** (`id="about"`, `homeWelcome` + `images.whoWeAre`) → **`ArcTrustStripSection`** → **`ArcMicroStatementSection`** (`homeMicro2`) → **`WholeBodySection`** → **`YourPathSection`** → **`InvestCTASection`** → **`ArcFooter`**. Removed **`ScrollChapterIntroSection`** and **`WhoWeAreSection`** from the route (components remain in repo for reference). **`src/content/homepage.ts`:** added **`headlineEmphasisWord`** for welcome headline split. **`WholeBodySection`:** **3** pillar cards, **`lg:grid-cols-3`**. **`YourPathSection`:** **3** steps (icons: consult, treat, sustain), flex card widths adjusted for three columns. **`InvestCTASection`:** optional **`supportingLine`** (fed **`homeInvestSupport`**). **`ArcWelcomeSplitSection`:** safe headline split when emphasis substring missing (`indexOf`). **`ArcMicroStatementSection`:** no extra space before **`headlineAfter`** when it starts with punctuation (e.g. em dash). `npm run build` succeeded.

| Path | Notes |
|------|--------|
| `src/app/page.tsx` | New section order + content imports |
| `src/content/homepage.ts` | `headlineEmphasisWord` on `homeWelcome` |
| `src/components/arc/WholeBodySection.tsx` | 3 cards |
| `src/components/arc/YourPathSection.tsx` | 3 steps, layout classes |
| `src/components/arc/InvestCTASection.tsx` | `supportingLine`, `cn` |
| `src/components/arc/ArcWelcomeSplitSection.tsx` | `indexOf` emphasis guard |
| `src/components/arc/ArcMicroStatementSection.tsx` | punctuation-aware `headlineAfter` spacing |

**Follow-ups:** Remove or repurpose unused **`ScrollChapterIntroSection`** / **`WhoWeAreSection`** if you want less dead code; refresh **`documents/site-section-content-inventory.md`** to match the new stack.

### 22. First micro strip — full viewport pin + scroll scrub

**Ask:** The first **`ArcMicroStatementSection`** (post-hero “Intentional care…” block) should read as a **full-screen** section with **scroll-driven animation**, not a short static band.

**Done:** **`ArcMicroStatementSection`** is now a **client** component with optional **`pinnedScrollMotion`**: **`ScrollTrigger`** on **`#main`** matches hero/chapter pattern (**pin**, **`+=` one viewport**, **`scrub: 0.8`**, Locomotive ready + fallback timeout). Layout uses **`min-h-[100dvh]`** + centered column; eyebrow / headline / USP / link get staggered **fade + translateY** from scroll **progress**; subtle **radial teal wash** scales with progress. **`prefers-reduced-motion`:** no pin hook, **`p = 1`**, static full-height presentation. Compact instances keep **`data-scroll-section`**; pinned variant omits it (scrub owns motion). **`page.tsx`:** first micro passes **`pinnedScrollMotion`**; second micro unchanged. `npm run build` succeeded.

### 23. Whole-body section — WebGL glass slider + real assets

**Ask:** Upgrade the services / whole-body area using the pasted **Three.js + GSAP** shader slider (glass transitions, nav, progress); **use local editorial + patient-experience images** instead of CodePen URLs.

**Done:** Added **`src/content/servicesShowcaseSlides.ts`** — six slides with ARC copy and paths from **`EDITORIAL_SPA_IMAGES`** (01–05) + **`PATIENT_EXPERIENCE_IMAGES`** [0]. Added **`src/components/arc/arcServicesShowcaseShaders.ts`** (vertex + fragment GL strings) and **`ArcServicesShowcaseSlider.tsx`** (**`three`** + **`gsap`** from npm; **container-sized** renderer + **ResizeObserver**; **dispose** textures/renderer/scene on unmount; **`prefers-reduced-motion`** → static crossfade + same nav copy). **`WholeBodySection`** imports the client slider and **`SERVICES_SHOWCASE_SLIDES`** above the three pillar cards. Next.js 16 disallows **`dynamic(..., { ssr: false })`** inside this server component, so the slider is a **direct client import** (WebGL still runs only in `useEffect`). **`npm run build`** succeeded.

| Path | Notes |
|------|--------|
| `src/content/servicesShowcaseSlides.ts` | Slide titles/descriptions + `imageSrc` |
| `src/components/arc/arcServicesShowcaseShaders.ts` | Shader source strings |
| `src/components/arc/ArcServicesShowcaseSlider.tsx` | WebGL + reduced-motion fallback |
| `src/components/arc/WholeBodySection.tsx` | Slider under section H2, above cards |

**Follow-ups:** Optional lazy `dynamic()` from a tiny **`use client`** wrapper if bundle size becomes an issue; tune auto-slide interval / glass uniforms for brand.

### 24. Services showcase nav — match reference (line above label)

**Ask:** Bottom **`slides-navigation`** should match the reference: one horizontal row, thin line **above** each **uppercase** label, **serif** feel, **active** item brighter (text + line), **inactive** dimmed; not the previous bordered “chip” controls.

**Done:** Added **`.arc-slide-nav-*`** rules in **`src/app/globals.css`**: line stack (`h-px` base + progress fill), **`.arc-slide-nav-label`** with **serif**, **uppercase**, **letter-spacing**, **2-line clamp**; **`data-active="true"`** drives stronger white on line + label. **`ArcServicesShowcaseSlider`:** nav uses **`justify-between`**, **`flex-nowrap`**, **gradient** bar (no heavy card chrome); **`createSlidesNavigation`** / **`updateNavigationState`** set **`data-active`** + **`aria-current`**. **Reduced-motion** variant uses the same structure. Slightly increased **content `padding-bottom`** for the taller bar; **overflow-x** scroll (hidden scrollbar) for very narrow widths. `npm run build` succeeded.

### 25. Services showcase — reference parity (CodePen DOM + side arrows)

**Ask:** Match the reference slider: **side chevrons + counters** (`slideNumber` / `slideTotal`), bottom **`slidesNav`** with **`slide-progress-line`** / **`slide-progress-fill`** / **`slide-nav-title`**, warm progress on the active line, **`bg-black/35`** bar.

**Done:** **`ArcServicesShowcaseSlider`:** **`ChevronLeft` / `ChevronRight`** (`lucide-react`) vertically centered; left cluster **prev + `#slideNumber`**, right **next + `#slideTotal`**; **`#slidesNav`** on `<nav>`. **`createSlidesNavigation`** builds **`slide-progress-line` > `slide-progress-fill[data-progress-fill]`** + **`slide-nav-title`**, toggles **`.active`**. **`globals.css`:** progress fill uses **warm off-white / gold** gradient; track on **`.slide-progress-line`**. **`autoSlideSpeed`** **5000** ms. **`ServicesShowcaseReducedMotion`:** same arrows, counters, nav markup (no animated fill). Content uses **horizontal padding** so copy clears arrows. `npm run build` succeeded.

### 26. Whole-body section — remove three pillar cards

**Ask:** Remove the three **`article`** pillar cards (“Rejuvenate…”, “Strengthen…”, “Restore…”).

**Done:** **`WholeBodySection`** now only renders the section **H2** + **`ArcServicesShowcaseSlider`** (removed **`cards`** grid, **`Image`**, **`Link`**, **`imageUrls`** prop). **`page.tsx`:** `<WholeBodySection />` with no props. Section **`PinnedSection`** classes trimmed (**no** `min-h-[100dvh]` / heavy vertical centering). `npm run build` succeeded.

### 27. Services — section heading inside slider; full-bleed copy layer

**Ask:** **`slide-content`** should **fill the whole slider**; the page **H2** (“Whole-Body Care…”) should live **on the slider**, not above it.

**Done:** **`WholeBodySection`** no longer renders the charcoal **H2**—only the slider. **`ArcServicesShowcaseSlider`:** **`slide-content`** is **`absolute inset-0`** with **`flex flex-col justify-between`**: top = same **H2** copy (**`data-scroll-section`**, white + shadow), bottom = eyebrow + per-slide title (**`h3`**, `titleRef` for GSAP) + description; outer layer **`pointer-events-none`**, copy block **`pointer-events-auto`**. **Reduced-motion** variant matches. `npm run build` succeeded.

### 28. Services slider — full-bleed width

**Ask:** Slider still did not **fill the full width** of the section (cream margins from **`max-w-7xl`** + horizontal padding).

**Done:** **`WholeBodySection`:** removed **`max-w-7xl`** wrapper and horizontal **`px-*`**; section is **`w-full`**, **`bg-arc-cream`**, vertical padding only. **`ArcServicesShowcaseSlider`:** **`max-w-none`**, **`rounded-none`** (edge-to-edge strip), loading skeleton matches. `npm run build` succeeded.

### 29. Services slider — full viewport height (canvas fill)

**Ask:** WebGL **`canvas`** should **fill the section vertically** (was ~`min(72vh,820px)`; canvas height ~437px in inspector).

**Done:** Slider **`section`** height **`h-[100dvh]`** (floor **`min-h-[320px]`**) for WebGL, reduced-motion, and loading placeholder; **`WholeBodySection`** **`py-0`** so no cream band steals vertical space. **`ResizeObserver`** still syncs renderer to **`root.clientHeight`**. `npm run build` succeeded.

### 30. Services — remove cream strip above slider

**Ask:** Shift content up / remove **cream “white space”** above the services block (inspector: **`section#services`** **`bg-arc-cream`**).

**Done:** **`WholeBodySection`:** **`bg-arc-charcoal`** + **`overflow-hidden`** so the wrapper matches the slider (no cream halo). **`page.tsx`:** second **`ArcMicroStatementSection`** (above services) gets **`className`** with **tighter bottom padding** (`pb-10`–`md:pb-14`) vs default **`py-24`/`py-32`**, keeping top spacing (`pt-24` … `md:pt-32`). `npm run build` succeeded.

### 31. Services — “Whole-Body Care…” below header logo

**Ask:** Section **H2** overlapped the **ARC** wordmark in the fixed header; move title **down** so it reads **below** the logo.

**Done:** **`slide-content`** (WebGL + reduced-motion) uses **responsive `padding-top`** = site header logo row (`h-32` / `sm:h-40` / `md:h-44` / `lg:h-48` as `8`–`12rem`) **+ 1.5rem** + **`env(safe-area-inset-top)`** so the H2 clears the mark. `npm run build` succeeded.

### 32. Services bottom nav — ARC teal branding

**Ask:** **`#slidesNav`** strip should use **brand teal / green** instead of neutral white/black.

**Done:** **`globals.css`:** progress **track** / **fill**, **inactive** / **active** labels, and **focus** ring use **`var(--arc-teal)`** / **`--arc-teal-hover`** via **`color-mix`**. **`ArcServicesShowcaseSlider`:** `<nav>` **`border-arc-teal/40`**, **`bg-gradient-to-t`** from **`from-black/55`** **`via-arc-teal/15`** **`to-arc-teal/25`**. `npm run build` succeeded.

### 33. Services bottom nav — stronger visibility

**Ask:** Increase **visibility** of the teal strip / controls.

**Done:** **`globals.css`:** progress track **2px** tall, **brighter** teal mixes + **glow** on active track; fill **stronger gradient** + **dual box-shadow**; inactive labels **brighter white/teal** + **text-shadow**; active labels **`#f6fffc`** **`font-weight: 600`** + teal glow; focus ring **more opaque**. Nav strip: **`z-20`**, **`border-t-2 border-arc-teal/70`**, **richer gradient** (`via-arc-teal/35` **`to-arc-teal/45`**), **`shadow-[0_-10px_36px_rgba(118,179,168,0.28)]`**, slightly **more vertical padding**; label font sizes **nudged up** at sm/md. `npm run build` succeeded.

### 34. Services nav strip — less green background

**Ask:** **`#slidesNav`** background **too green** — dial back.

**Done:** Nav **`className`:** **neutral dark gradient** (`from-black/85` **`via-black/70`** **`to-black/78`**), **`border-t border-white/12`** (no heavy teal border), **neutral shadow** (`rgba(0,0,0,0.5)`). Teal stays on **progress lines / labels** via **`globals.css`** only. `npm run build` succeeded.

### 35. Slide timer line — distinct countdown color

**Ask:** The **time / progress** line should use a **different color** (contrast) so the **countdown** is obvious vs the rest of the bar and when the **slide changes**.

**Done:** **`globals.css`:** **`.slide-progress-line`** = **neutral white** at low opacity (static track for all slides; active tab slightly brighter + **inset** hairline). **`.slide-progress-fill`** = **warm gold / ivory → amber** gradient + **gold-tinted glow** (separate from **arc-teal** label chrome). `npm run build` succeeded.

### 36. First micro strip — full-bleed video background

**Ask:** **`ArcMicroStatementSection`** (pinned “Intentional care…”) should use a **full video background** to lift the section visually.

**Done:** **`ArcMicroStatementSection`:** optional **`pinnedBackgroundVideoSrc`** + **`pinnedBackgroundPosterSrc`**. Pinned + video: **`<video>`** `object-cover` / `muted` / `loop` / `playsInline` / `autoPlay`, **`IntersectionObserver`** (root **`#main`**) to **play/pause** when off-screen; **cream + vignette + radial teal** overlays for readability. **`prefers-reduced-motion`:** **no video** — **poster** as static fill or **cream**. Section **`bg-arc-charcoal`** when video on (else cream). **`page.tsx`:** first micro uses **`videos.microStatement`** (see **`site.ts`**) + **`images.heroMedia`** poster; **`videos`** import from **`site.ts`**. `npm run build` succeeded.

### 37. Micro strip video — use full **`videos`** catalog (not one-off path)

**Ask:** The pinned micro section’s video background should draw from the **many clips in `public/assets/videos`**, not a single ad-hoc file.

**Done:** **`src/content/site.ts`:** JSDoc lists every **`videos`** key (`heroBackground`, **`spaRoom`**, **`treatments.*`**, **`bodyCareComposition`**) for copying into **`microStatement`** or props. Added **`HERO_SPA_ORIENTAL`** constant so **`heroBackground`** and **`microStatement`** share one path (swap **`microStatement`** to another entry string when you want a different mood). **`page.tsx`** wires **`pinnedBackgroundVideoSrc={videos.microStatement}`**. `npm run build` succeeded.

| Path | Notes |
|------|--------|
| `src/content/site.ts` | `HERO_SPA_ORIENTAL`, `videos.microStatement` |
| `src/app/page.tsx` | `videos.microStatement` on first micro strip |

### 38. Pinned micro — video not playing while scroll-locked (pin)

**Ask:** Video background did not play when the first micro section was **pinned** / scroll-locked.

**Done:** **`ArcMicroStatementSection`:** Removed **`IntersectionObserver`** with **`root: #main`** (pinned triggers use **`position: fixed`**, so intersection with a custom scroll root often goes false and **`pause()`** won). **Play/pause** is now driven by the same **`ScrollTrigger`** as the pin: **`onEnter` / `onEnterBack`** → **`play()`**, **`onLeave` / `onLeaveBack`** → **`pause()`**; trigger **`id: arc-micro-statement-pin`** + **`syncVideoIfPinned`** after refresh when **`isActive`**. **`preload="auto"`** on `<video>`. `npm run build` succeeded.

### 39. Seamless ambient loop (no loop seam glitch)

**Ask:** Avoid the visible glitch when an HTML5 video hits the end and restarts its loop.

**Done:** Added **`SeamlessLoopVideo`** (`src/components/arc/SeamlessLoopVideo.tsx`): two stacked `<video>` tags with the same **`src`**, no native **`loop`** — near the end of the leading clip, the other starts at **`currentTime = 0`**, opacity **crossfades** (~480ms), then the old layer resets to paused at 0. **`ArcMicroStatementSection`** uses it for **`pinnedBackgroundVideoSrc`**; **`play()` / `pause()`** on the ref affect only the active lead + reset the follower (same contract as before). `npm run build` succeeded.

### 40. About / welcome — immersive scroll-zoom gallery (Framer + GSAP)

**Ask:** Adapt a pasted **ImmersiveScrollGallery**-style experience (scroll-driven image scale collage + fading copy) for **`#about`**.

**Done:** Rebuilt **`ArcWelcomeSplitSection`** as a **client** component. **Default (motion):** **`h-[200vh]`** section with **`sticky` `100dvh`** stage; **7** `next/image` layers in a collage (slots from the reference, tuned for small screens with **`min()`** sizes); **Framer `useTransform`** on a **`useMotionValue`** progress driven by **GSAP `ScrollTrigger`** on **`#main`** (`start: top top`, `end: bottom bottom`, **`scrub: 0.65`**) so it matches **Locomotive** + the rest of the site. Images default to **`EDITORIAL_SPA_IMAGES` (5) + `images.heroMedia` + `images.whoWeAre`**; optional prop **`galleryImageSrcs`**. Collage opacity eases out by end; headline + body + CTA **fade/scale in** over scroll ~0.52–0.78. **`prefers-reduced-motion: reduce`:** previous **single-image + copy** layout inside **`PinnedSection`** (unchanged UX). **`npm run build`** succeeded.

### 41. Welcome gallery — swap image paths at slots 0 and 3 only

**Ask:** Trade **only** the image files at collage index **0** and **3**; no other changes.

**Done:** After `getWelcomeGallerySlots()` builds the 7-length array, it **swaps** `slots[0]` and `slots[3]` (temp variable). Layout, zoom/focal index mapping in UI, and **`ArcWelcomeSplitSection`** unchanged. `npm run build` succeeded.

| Path | Notes |
|------|--------|
| `src/content/welcomeGallery.ts` | Post-fill swap of `slots[0]` ↔ `slots[3]` |

### 42. Welcome gallery — dedicated image for slot 3 + distinct frame size vs slot 0

**Ask:** Use another image for collage index **3**, and make its **size** different from index **0**.

**Done:** Removed the **0↔3 path swap**. Exported **`WELCOME_GALLERY_SLOT_3_SRC`** (`images.whoWeAre` — consultation office) and **`getWelcomeGallerySlots()`** overwrites **`slots[3]`** after the pool fill. **`IMAGE_STYLES[3]`** in **`ArcWelcomeSplitSection`** is now **`w-[20vw] h-[46vh] left-[28vw] -top-[5vh]`** (portrait strip) vs index **0** **`w-[30vw] h-[30vh]`** (square). Focal zoom remains on index **`WELCOME_GALLERY_FOCAL_INDEX` (0)**. `npm run build` succeeded.

| Path | Notes |
|------|--------|
| `src/content/welcomeGallery.ts` | `WELCOME_GALLERY_SLOT_3_SRC`, slot 3 override |
| `src/components/arc/ArcWelcomeSplitSection.tsx` | `IMAGE_STYLES[3]` dimensions |

### 43. Welcome gallery — patient-experience assets for slots 3 and 6

**Ask:** Use images from **`patient-experience`** for collage index **3** and **6**.

**Done:** Imported **`PATIENT_EXPERIENCE_IMAGES`**, spread into **`WELCOME_GALLERY_IMAGE_SRCS`** (deduped). **`WELCOME_GALLERY_SLOT_3_SRC`** = **`PATIENT_EXPERIENCE_IMAGES[0]`** (`patient-experience-01.png`), **`WELCOME_GALLERY_SLOT_6_SRC`** = **`[1]`** (`patient-experience-02.png`); **`getWelcomeGallerySlots()`** assigns both after the pool fill. `npm run build` succeeded.

| Path | Notes |
|------|--------|
| `src/content/welcomeGallery.ts` | Pool + slot 3/6 overrides from `patientExperienceImages.ts` |

### 44. Welcome collage — smaller frames for tiles 3 and 6 on mobile

**Ask:** Make gallery indices **3** and **6** smaller so the layout fills more evenly (mobile collage balance).

**Done:** **`IMAGE_STYLES[3]`** and **`IMAGE_STYLES[6]`** in **`ArcWelcomeSplitSection`** use **mobile-first** smaller **`vw`/`vh`** + slightly inward **`left`/`top`** adjustments, with **`sm:`** stepping up and **`md:`** restoring prior desktop sizing (`20×46` / `18×18`). `npm run build` succeeded.

| Path | Notes |
|------|--------|
| `src/components/arc/ArcWelcomeSplitSection.tsx` | Responsive strings for collage indices 3 and 6 |

### 45. Welcome collage — mobile position pass for tiles 3 and 6

**Ask:** Fix **index 3** and **6** placement on **mobile** so they sit better (less empty right gutter / “floating” tile 6).

**Done:** **`IMAGE_STYLES[3]`** — nudged **right** on narrow widths (`left` **30vw → 32vw** at `sm`), slightly larger frame (**16–17vw**), mild vertical tweak; **`md:`** unchanged vs prior desktop. **`IMAGE_STYLES[6]`** — pulled **toward the center cluster** (`left` **10vw / 14vw** at `sm` vs **20vw** before), aligned vertically with the lower band (**`top` ~32–33vh**), slightly larger **13–15vw × 14–15vh**; **`md:`** unchanged. `npm run build` succeeded.

| Path | Notes |
|------|--------|
| `src/components/arc/ArcWelcomeSplitSection.tsx` | Base + `sm:` offsets for indices 3 and 6 |

### 46. Welcome collage — tile 6 nudged right on mobile

**Ask:** Move image index **6** to the **right** as well (not center-left).

**Done:** **`IMAGE_STYLES[6]`** base / `sm` **`left`** from **10vw / 14vw** → **24vw / 28vw**; **`md:`** unchanged. `npm run build` succeeded.
