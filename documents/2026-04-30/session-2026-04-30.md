# Session log – 2026-04-30

### 1. Connect local project to GitHub

**Ask:** Connect this repo to GitHub private repo `Ensemble-Digital-Labs/ArcWellnessWebsite` (`https://github.com/Ensemble-Digital-Labs/ArcWellnessWebsite.git`).

**Done:** Discovered `git` was resolving the repository root to `C:/Users/Chinh Mach` (accidental home-directory repo), not the website folder. Ran `git init` in `Arc Wellness Website` so the project has its own `.git`. Added `origin` → `https://github.com/Ensemble-Digital-Labs/ArcWellnessWebsite.git`. Staged sources; removed mistakenly staged paths under `arc-wellness/` (nested folder containing only `.next` and `node_modules`). Appended `/arc-wellness/` to `.gitignore` so that stray folder stays untracked. Initial commit on `main`, pushed with upstream: `git push -u origin main` (succeeded).

**Follow-ups:** Consider removing `C:\Users\Chinh Mach\.git` if that home-level repo was unintentional (do not delete without confirming—would un-git the whole user profile tree). Optionally delete local `arc-wellness/` folder to reclaim disk space; it is ignored and not on GitHub.

## Paths touched

| Path | Notes |
|------|--------|
| `.gitignore` | Ignore `/arc-wellness/` |
| `.git/` | New repo root inside project |
