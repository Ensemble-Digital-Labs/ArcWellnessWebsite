# Session log – 2026-05-04

### 1. Hero + nav responsiveness (1440px–2560px)

**Ask:** Improve responsiveness of the hero section and the navbar menu for higher resolutions from above 1440px up to 2560px.

**Done:**

- **`ScrollExpandHero.tsx`**
  - Replaced a binary mobile/desktop resize check with **`viewportTier`**: `mobile` (&lt;768), `desktop` (768–1439), `wide` (1440–1919), `ultra` (≥1920).
  - **Scroll-expand math** scales with tier: wider/taller final media frame and stronger headline **translateX** drift on wide and ultra viewports so the interaction still reads at large sizes.
  - **Hero content column**: fluid caps from `1400px` up through `xl` / `2xl` / `min-[1920px]` using `min(..., rem)` and `vw` so the block grows with the display instead of staying a narrow 1400px island.
  - **Intro block**: aligns to the same conceptual column as the hero via `left: max(0, (100vw − contentWidth) / 2)` at **`min-[1440px]`** (not Tailwind `xl` / 1280px) and `min-[1920px]`, wider `max-w`, larger body copy.
  - **Headlines**: `min-[1440px]` → `2xl` (1536px+) → `min-[1920px]:text-8xl` with wider `max-w` on the second line; keyword emphasis `em` sizes extended for the same breakpoints.
  - **“Watch our story”**: right inset matches column margin on large screens; slightly larger type at `min-[1920px]`.

- **`ArcSiteHeader.tsx`**
  - **Slide-in panel**: `max-w` steps from 28rem / 32rem to **38rem / 44rem / 48rem** at `xl`, `2xl`, and `min-[1920px]` so the menu is not a thin sliver on ultra-wide displays.
  - **Nav links**: larger type, padding, row gaps, and **hover preview** thumbnails at upper breakpoints; `sizes` on preview images updated for larger decoded sizes.
  - **Logo link + wordmark**: taller header hit area and higher `max-w` caps on very wide viewports (still bounded so the mark does not dominate).
  - **Menu/Close control**: `right` position uses the same **column-edge** `calc` pattern as the hero; larger tap target and type at `2xl` / `min-[1920px]`.

Build: `npm run build` completed successfully after changes.

## Paths / artifacts

| Path | Notes |
|------|--------|
| `src/components/arc/ScrollExpandHero.tsx` | Viewport tiers, hero layout/type/scroll-expand tuning |
| `src/components/arc/ArcSiteHeader.tsx` | Drawer width, nav typography, logo/menu alignment |

## Follow-ups

- Visual QA on a real **2560×1440** panel and **ultrawide** aspect ratios; column `min(…rem, …vw)` choices may need one pass of pixel tuning with design.
- Optional: scale **decorative SVG shapes** inside the nav panel at `xl+` if they feel small next to larger type.

### 2. Lock ultra-wide to 1440 look (slightly smaller at 2560)

**Ask:** Have everything look the same as at 1440px when at 2560px, just a little smaller.

**Done:**

- Introduced shared **`ARC_PAGE_RAIL_MAX`** in `src/lib/arc-layout.ts`: `max-w-[min(100%,90rem)]` (1440px rail) with **`min-[2200px]:max-w-[min(100%,86rem)]`** (~5% narrower) so very large monitors stay visually like 1440 with a modest shrink.
- **`ScrollExpandHero.tsx`**: Removed viewport-tier scale-up; hero foreground (intro, media expand, titles, CTA row, “Watch our story”) lives inside the rail; typography and scroll-expand math match the original **desktop** baseline.
- **`ArcSiteHeader.tsx`**: Wrapped logo + menu button in the same rail so alignment matches the hero without `calc()` gutters; reverted nav drawer and link row sizing to the pre–scale-up baseline.
- Removed unused **`ARC_RAIL_RIGHT_INSET`** after switching to the rail wrapper pattern.

| Path | Notes |
|------|--------|
| `src/lib/arc-layout.ts` | New shared `ARC_PAGE_RAIL_MAX` |
| `src/components/arc/ScrollExpandHero.tsx` | Rail + simplified resize logic |
| `src/components/arc/ArcSiteHeader.tsx` | Header rail wrapper, nav sizing revert |

### 3. Physician-founder homepage section

**Ask:** Add an **About**-style intro for the **doctor founder**, stating **what he can deliver**.

**Done:**

- **`ArcFounderIntroSection.tsx`**: Pinned full-viewport band (`bg-arc-cream`), split layout aligned with welcome — portrait (`object-top`), eyebrow, serif headline with **`TitleEmphasis`** on surname, role line, intro paragraph, **“What he delivers”** checklist with teal circular **`Check`** icons.
- **`homeFounder`** in **`src/content/homepage.ts`**: Placeholder name **Dr. Jordan Mercer**, role, intro copy, four deliverable lines (replace with approved biography).
- **`site.ts`**: **`founderPortrait`** (currently same asset as consultation photo until `public/assets/sections/founder/` headshot exists).
- **`page.tsx`**: Section **`id="founder"`** after **`ArcWelcomeSplitSection`**, before **`ArcTrustStripSection`**. **`public/assets/sections/README.md`** updated + **`founder/.gitkeep`**.

`npm run build` succeeded.

### 4. About section background after collage zoom

**Ask:** Use **`ChatGPT Image May 4, 2026, 01_22_23 PM.png`** as the background for the about / welcome immersive section **after** the image collage zooms in and the copy phase appears.

**Done:**

- Copied the file to **`public/assets/decoration/background/about-copy-stage.png`** (URL-friendly path for `next/image`).
- **`ArcWelcomeSplitSection.tsx`** (`WelcomeImmersiveScrollBody`): added a full-bleed **`motion.div`** at **`z-[5]`** (between collage at `z-0` and copy at `z-20`) with **`opacityCopyStageBg`** keyed to the same scroll progress as **`opacityCopy`** (`0.34` → `0.56` → `1`), so the art **crossfades in with the headline/body** as tiles fade out.
- **`next/image`** `fill`, `object-cover object-center`, `sizes="100vw"`; decorative layer uses **`aria-hidden`** and empty **`alt`**.
- Light **`bg-arc-cream/25`** overlay on top of the photo to align with the site cream field and keep type readable.

| Path | Notes |
|------|-------|
| `public/assets/decoration/background/about-copy-stage.png` | New static asset (source was repo-root ChatGPT export) |
| `src/components/arc/ArcWelcomeSplitSection.tsx` | `WELCOME_COPY_STAGE_BG` + copy-stage background layer |

### 5. About copy-stage background still not visible in browser

**Ask:** After the collage zoom, the decorative background still did not appear while scrolling (only solid cream).

**Done:**

- **`ArcWelcomeSplitSection.tsx` (`WelcomeImmersiveScrollBody`):** Drove copy-stage background opacity from the **same ScrollTrigger `onUpdate`** as the scrub timeline (direct **`element.style.opacity`**) instead of a second Framer `motion` opacity on that layer, so it cannot get out of sync with GSAP/Locomotive.
- Replaced **`next/image`** on that layer with a plain **`<img src={WELCOME_COPY_STAGE_BG}>`** (full-bleed `object-cover`) to avoid `fill`/layout edge cases.
- **`copyStageBgOpacity`:** ramps **0.28 → 0.52** (section progress) so the art **crossfades in while tiles are still fading**, not only after.
- After **`ScrollTrigger.refresh()`**, re-find the section’s trigger and **re-sync** background opacity (Locomotive refresh can leave one frame stale).
- Initial **`opacity-0`** utility on the layer; GSAP **inline opacity** overrides once scrub runs (inline beats the utility).

### 6. Organize new ambient mood PNGs into `/public` assets

**Ask:** User added more ChatGPT-export mood images (same kind as prior ambient art); organize them into the asset tree.

**Done:**

- Moved repo-root files **`ChatGPT Image May 4, 2026, 11_27_03 PM (1).png` … `(4).png`** → **`public/assets/decoration/background/ambient-05.png` … `ambient-08.png`** (stable order 1→5 … 4→8).
- Extended **`src/content/backgroundDecoration.ts`** → **`BACKGROUND_DECORATION_IMAGES`** with those four URLs.
- Updated **`public/assets/decoration/background/README.md`** (table includes `about-copy-stage` + `ambient-05`…`08`) and **`public/assets/decoration/README.md`** (range `ambient-01`…`ambient-08`).

| Path | Notes |
|------|-------|
| `public/assets/decoration/background/ambient-05.png` … `ambient-08.png` | Renamed in-place imports |
| `src/content/backgroundDecoration.ts` | Registry |
| `public/assets/decoration/background/README.md`, `public/assets/decoration/README.md` | Docs |

### 7. Welcome collage tiles 0, 3, 6 → ambient mood art

**Ask:** Use the new ambient-style assets for immersive collage image indices **0**, **3**, and **6** (replacing prior editorial / patient-experience shots in those positions).

**Done:**

- **`src/content/welcomeGallery.ts`:** Introduced **`WELCOME_COLLAGE_AMBIENT_TILE_SRC`** → **`/assets/decoration/background/ambient-05.png`** (same file as the first of the May 4 imported ChatGPT mood set). **`WELCOME_GALLERY_FOCAL_SRC`** (tile **0**, scroll-zoom focal), **`WELCOME_GALLERY_SLOT_3_SRC`**, and **`WELCOME_GALLERY_SLOT_6_SRC`** all point at that path; comments updated.

| Path | Notes |
|------|-------|
| `src/content/welcomeGallery.ts` | Tiles 0 / 3 / 6 use `ambient-05.png` |
| `public/assets/decoration/background/ambient-05.png` | Source asset (swap file or change constant for another ambient-NN) |

### 8. Distinct ambient per collage tile (0 / 3 / 6)

**Ask:** Use **`ambient-05`**, **`ambient-07`**, and **`ambient-08`** as different images for the three slots (not one shared file).

**Done:** **`welcomeGallery.ts`** — **`AMBIENT_COLL_TILE_0`** → `ambient-05.png`, **`AMBIENT_COLL_TILE_3`** → `ambient-07.png`, **`AMBIENT_COLL_TILE_6`** → `ambient-08.png`; exports **`WELCOME_GALLERY_FOCAL_SRC`** / **`_SLOT_3_`** / **`_SLOT_6_`** wired accordingly.

### 9. Hero CTAs: two buttons; remove “Watch our story”

**Ask:** Hero should show two CTAs — **Begin your Journey** and **See How it Works** — and remove **Watch our story**.

**Done:** **`ScrollExpandHero.tsx`** — intro block now has a flex row of links: primary teal **`#book`** (“Begin your Journey”), outline secondary **`#path`** (“See How it Works”); removed bottom-right play button and **`lucide-react`** **`Play`** import.

### 10. Premium `Button` (Base UI + CVA) + ARC brand variants

**Ask:** Hero CTAs should feel closer to a reference **Base UI** + **`class-variance-authority`** button (inset highlight, shadows, focus ring) while using **ARC** colors.

**Done:**

- **`npm install class-variance-authority @base-ui/react`**
- New **`src/components/ui/button.tsx`**: **`useRender`** + **`mergeProps`** polymorphic **`Button`**; **`buttonVariants`** with **`default`** (teal primary: inset highlight + teal glow shadow, hover/active), **`heroOutline`** (cream border + light glass on dark photography), **`outline`** (charcoal edge for light sections); sizes **`sm` / `default` / `lg`**; focus rings use **`arc-teal`** / **`arc-cream`** as appropriate.
- **`ScrollExpandHero.tsx`**: hero CTAs use **`<Button render={<Link … />} size="lg" variant="default" | "heroOutline">`** instead of raw **`Link`** classes.

### 11. Hero CTAs — reference split pill + round icon (dark cluster, ARC accent)

**Ask:** Hero buttons still did not match a **pill + circular arrow** reference (charcoal shells, accent text/icon, small gap); align layout and feel while keeping ARC colors.

**Done:**

- New **`src/components/arc/ArcSplitCta.tsx`**: one **`next/link`** wraps **`rounded-full` pill** (label) + **`rounded-full` disc** with **`ArrowUpRight`**, **`gap-1.5`**, shared **`#252525`**-style fill, light border, inset highlight + drop shadow; **`emphasis="primary"`** → **`arc-teal`** copy/icon + hover mint; **`emphasis="secondary"`** → **`arc-cream`** for the second CTA; focus ring **`arc-teal`** with dark offset.
- **`ScrollExpandHero.tsx`**: replaced **`Button`** pair with two **`ArcSplitCta`** rows (stacked on narrow, row on **`sm+`**); removed unused **`Link`** import.

### 12. `ArcSplitCta` — tighter pill/disc + motion

**Ask:** Match an **animated** reference: arrow disc **closer** to the pill; Base UI **`Button`** alone does not achieve the split layout.

**Done:** **`ArcSplitCta.tsx`** — row gap **`gap-px`**, disc **` -translate-x-px` / `sm:-translate-x-0.5`** so it sits nearer the pill; **Framer Motion** spring **`whileHover` / `whileTap`** on the cluster; **CSS** **`group-hover`** on disc (extra **`translate-x`**, scale) and arrow (nudge + rotate) so interaction works when hovering the label or the disc; **`motion-reduce`** disables transforms/transitions on disc/arrow and skips row scale.

### 13. `ArcSplitCta` — calm / elegant (less “buggy” motion)

**Ask:** Prior CTA motion felt **buggy**; want something **elegant and aesthetic**.

**Done:** **`ArcSplitCta.tsx`** — removed **Framer Motion** and all **scale / slide / arrow rotation**; **~500ms** transitions with **`cubic-bezier(0.4,0,0.2,1)`** on surfaces, borders, shadows, and label color only; hover adds a **soft lift** (shadow) and a **whisper of teal or cream glow** by emphasis; icon only **opacity** refines on hover; **active** is a light **opacity** dip on the link; component is a **server** module (no **`use client`**). **`motion-reduce`** still short-circuits transitions.

### 17. Hero headline: “Converge.” on its own line

**Ask:** Move **Converge.** to the next line so the stack reads **Where** / **Aesthetics…Longevity** / **Converge.**

**Done:** **`ScrollExpandHero.tsx`** — **`splitTitleRestForClosingLine`**: if the words after the first line end with **`*.`**, the last token is rendered as a third **`motion.span`** (same typography + scroll drift as line 2); script emphasis runs only on the middle line.

### 16. Hero text CTAs use brand teal

**Ask:** Hero **`ArcTextUnderlineCta`** row should use **branding (teal)** not champagne.

**Done:** **`ArcTextUnderlineCta`** — optional **`accent?: "teal" | "champagne"`**, default **`teal`** (`text-arc-teal`, hover **`arc-teal-hover`**, teal focus ring, soft teal text-shadow on dark); **`ScrollExpandHero`** passes **`accent="teal"`** explicitly on both links.

### 15. Brighter brand teal (site-wide)

**Ask:** Upgrade branding color for the whole site — **brighter teal**.

**Done:** **`globals.css`** — **`--arc-teal`** `#76b3a8` → **`#4ec4b0`**, **`--arc-teal-hover`** **`#369d88`**, **`--arc-teal-muted`** **`#e8f2ef`** → **`#e2f6f2`** (harmonizes with new teal). All **`text-arc-teal`**, **`bg-arc-teal`**, etc. pick this up via **`@theme`**. Hardcoded glows updated to match: **`ScrollExpandHero`** keyword shadow, **`ArcSplitCta`** primary glow, **`button.tsx`** shadows, **`ArcMicroStatementSection`** radial wash. **`.cursor/rules/project-context.mdc`** palette note updated.

### 14. Champagne text CTA (label + → + underline)

**Ask:** Reference control: **no box** — text + **thin arrow** + **hairline** under both, **muted gold/champagne**.

**Done:** **`globals.css`**: **`--arc-champagne`**, **`--arc-champagne-hover`** + **`@theme`** colors. New **`ArcTextUnderlineCta.tsx`**: **`next/link`**, flex row label + **`ArrowRight`** (thin stroke), **`mt-2.5`** then **`h-px`** full-width rule in **`currentColor`**; hover shifts arrow slightly and brightens color; focus ring on champagne. **`ScrollExpandHero.tsx`** hero links use **`ArcTextUnderlineCta`** ( **`ArcSplitCta`** left in repo, unused on homepage).

### 20. Hero CTA hover preview — left of links + tilt

**Ask:** Preview was overlapping CTAs; it should sit **to the left** of the stack and have a slight **rotation** for a more aesthetic look.

**Done:** **`HeroCtaCursorPreviews.tsx`** — **`itemRefs`** per row; position = **`linkRect.left - cardWidth - gap`** (clamped to viewport), vertical center on the **hovered row** + small pointer bias on Y; **`rotate(-5deg)`** on the same **`translate3d`**; **`rounded-2xl`**; preview **`hidden` below `sm`** to avoid narrow-viewport overlap; lerp unchanged for smooth tracking while scrolling.

### 19. Hero CTAs — right column + editorial stack

**Ask:** Move the **two** hero CTAs **to the right** and show them as an **aesthetic stack**.

**Done:** **`ScrollExpandHero.tsx`** — intro row is **`flex-col`** then **`sm:flex-row sm:justify-between`**: copy stays **`max-w-xl`** left; **`HeroCtaCursorPreviews`** sits **`items-end`**, always **column** stack (**`gap-6`–`gap-8`**), **`border-l border-white/18`** + padding for a calm editorial rail; each **`ArcTextUnderlineCta`** gets **`className="items-end"`** so labels, arrows, and underlines align flush right. **`HeroCtaCursorPreviews`** — each hover wrapper uses **`justify-end`**.

### 18. Hero CTAs — cursor-following preview cards

**Ask:** On hover over the two hero text CTAs, show a **preview image** that **tracks the cursor** (smooth / lerped), similar to a project-list reference.

**Done:** **`HeroCtaCursorPreviews.tsx`** — wraps both links; **`requestAnimationFrame`** + **lerp** drives **`translate3d`** on a **`fixed`** preview (**`z-[95]`**, under fullscreen nav); stacked **`next/image`** layers crossfade by active index; gradient veil; **`prefers-reduced-motion: reduce`** hides the preview. **`ScrollExpandHero`** uses **`images.investBanner`** and **`images.whoWeAre`** for the two slides.

### 21. Hero preview — irregular frame (not rounded rectangle)

**Ask:** Preview image can sit in an **irregular / aesthetic** shape; **no** plain **rounded rectangle**.

**Done:** **`HeroCtaCursorPreviews.tsx`** — replaced uniform rounded rect framing with **`PREVIEW_CLIP_CLASS`**: arbitrary **`clip-path: polygon(...)`** for an organic silhouette; **`drop-shadow`** for depth (box-shadow is clipped by **`clip-path`**). **`npm run build`** verified.

| Path | Notes |
|------|--------|
| `src/components/arc/HeroCtaCursorPreviews.tsx` | `PREVIEW_CLIP_CLASS`, polygon + drop-shadow |

### 22. Hero preview — SVG `<clipPath>` (gallery-style squiggle / rect / another)

**Ask:** Use the same **SVG `clipPath`** approach as a reference **`ClippedMediaGallery`** ( **`clip-squiggle`**, **`clip-rect`**, **`clip-another`** with **`objectBoundingBox`** paths).

**Done:** New **`heroCtaPreviewClipPaths.ts`** — path **`d`** strings + **`heroCtaPreviewClipIdForIndex`** (slide **0 → squiggle**, **1 → rect**, fallback **another**). **`HeroCtaCursorPreviews.tsx`** — hidden inline **`<svg><defs>`** with **`useId()`**-scoped ids (avoid collisions); each slide is an **absolute** wrapper with **`style={{ clipPath: \`url(#…)\` }}`**, **`next/image`** + gradient inside; **`drop-shadow`** on the clipped wrapper; removed CSS polygon **`PREVIEW_CLIP_CLASS`**. **`npm run build`** verified.

| Path | Notes |
|------|--------|
| `src/components/arc/heroCtaPreviewClipPaths.ts` | Path data + index → clip id |
| `src/components/arc/HeroCtaCursorPreviews.tsx` | Inline SVG defs + per-slide `clip-path: url(#…)` |

### 23. Hero preview — hide rectangular plate behind clip

**Ask:** Still seeing a **rectangle** outside the clip; only the **clipped** shape should read.

**Done:** **`HeroCtaCursorPreviews.tsx`** — removed **`bg-arc-charcoal/30`** from the **`aspect-[5/3]`** wrapper (it painted a full box behind the masks). **`drop-shadow`** applied **only** when that slide is active so **`opacity-0`** layers do not leave a boxy shadow. Optional **clipped** loading tint: **`bg-arc-charcoal/25`** on a layer **inside** each masked stack.

### 24. Testimonials below **Your path** (`#path`)

**Ask:** Add a **testimonial** section **below** **`PinnedSection` / `section#path`**.

**Done:** **`ArcTestimonialsSection.tsx`** — **`id="testimonials"`**, **`scroll-mt-28`**, cream surface + top hairline; centered header (eyebrow / H2 / optional intro); responsive **grid** (1 → 2 → 3 columns) of **`figure` / `blockquote` / `figcaption`** cards with teal border language consistent with **`YourPathSection`**. **`homepage.ts`** — **`homeTestimonials`** (three placeholder quotes + attribution + context; replace when approved). **`page.tsx`** — render **`ArcTestimonialsSection`** **after** **`YourPathSection`**, **before** **`InvestCTASection`**. **`npm run build`** verified.

| Path | Notes |
|------|--------|
| `src/components/arc/ArcTestimonialsSection.tsx` | New section |
| `src/content/homepage.ts` | `homeTestimonials` |
| `src/app/page.tsx` | Insertion order |

### 25. Testimonials — fullscreen pin + scroll reveal (like other stack sections)

**Ask:** Testimonials should be **full-screen**, **lock the view** while scrolling, with **animation on scroll** like other sections.

**Done:** **`ArcTestimonialsSection.tsx`** is now a **client** component wrapping **`PinnedSection`** ( **`useArcFullscreenPin`** — one viewport of scroll pinned inside **`#main`** / Locomotive). **`min-h-[100dvh]`**, **`flex flex-col justify-center`**, tightened vertical spacing so content fits the **100dvh** stage better. **`data-scroll-section`** on header + each card unchanged for **`initArcScrollReveal`** (fade / **`y`** in on scroll). **`npm run build`** verified.

### 26. Testimonials — 3D image sphere (left) + detail card (right)

**Ask:** Interactive **sphere of images** (Fibonacci layout, drag / momentum / auto-rotate) like a reference **`SphereImageGrid`**; sphere **left**, **no fullscreen modal** — a **card on the right** shows which person / their testimonial.

**Done:** **`TestimonialSphereGrid.tsx`** — deterministic sphere positions (no **`Math.random`**), ref + **`requestAnimationFrame`** momentum + optional auto-spin, document-level pointer/touch listeners, collision-aware scale, circular **`img`** cells with teal selection ring; **`prefers-reduced-motion`** → simple row of round **buttons**. **`ArcTestimonialsSection`** — **`lg`** row: sphere **left**, **`next/image`** + quote + attribution **right**; **`selectedId`** state ties sphere clicks to the card. **`homepage.ts`** — each testimonial gains **`id`**, **`imageSrc`** (from **`images.services`**), **`imageAlt`**. **`npm run build`** verified.

| Path | Notes |
|------|--------|
| `src/components/arc/TestimonialSphereGrid.tsx` | New |
| `src/components/arc/ArcTestimonialsSection.tsx` | Sphere + card layout |
| `src/content/homepage.ts` | `import { images }`; testimonial image fields |

### 27. Testimonial sphere — volumetric shell (not a “ball line”)

**Ask:** The testimonial **`SphereImageGrid`** read like a flat ring of same-sized discs (“ball line”) compared to a **21 dev** reference: true **3D shell** on black — **strong Z-driven scale and opacity** (large/bright in front, tiny/faded in back), **sparse hollow center**, **overlap as depth**, not an equator belt.

**Done:**

- **`src/components/ui/img-sphere.tsx` (`SphereImageGrid`)**
  - **`generateSpherePositions`:** Fibonacci **`t`** uses **`(i + 0.5) / count`**; removed **poleBonus / phi compression** that bunched points on the equator (ring look).
  - **`theme === "dark"`:** Scale and **`depthOpacity`** driven primarily by **normalized Z** (**`depthT`** from **`depthScale`**), not XY distance from center — avoids a belt of equal-sized circles. Aggressive curves so back tiles shrink and fade into black; back-face fade zone tweaked for a longer dissolve.
  - **Collision pass disabled in dark mode** (still on for light): collision was shrinking overlapping tiles toward similar sizes and flattening depth.
  - Default **initial rotation** **`{ x: 22, y: -14, z: 0 }`** so the equator is less edge-on.
  - Dark mode: stronger CSS **`perspective`** (~**1.28×** prop); **selected** tile gets extra scale (~**1.42×**); hover divisor floor adjusted (e.g. min scale **0.22**).
- **`src/components/arc/ArcTestimonialsSection.tsx`:** Tuned **`SphereImageGrid`** — **`sphereRadius={268}`**, **`baseImageScale={0.108}`**, **`hoverScale={1.35}`**, **`perspective={1280}`** (still **`theme="dark"`**, **`fitContainer`**, **`showModal={false}`**).

**Verify:** `npm run build` succeeded after changes.

| Path | Notes |
|------|--------|
| `src/components/ui/img-sphere.tsx` | Fibonacci spacing, Z-first dark math, no dark collision, perspective/rotation |
| `src/components/arc/ArcTestimonialsSection.tsx` | Sphere prop tuning |

**Follow-ups:** Visual QA in the pinned testimonials section (Locomotive); optional further tuning of **`baseImageScale`**, **`sphereRadius`**, depth exponents, **selected** boost, or tile count (**`TESTIMONIAL_SPHERE_TILE_COUNT`** in **`testimonialSphereBaseImages.ts`**).

### 28. Sphere hydration mismatch (Next.js overlay)

**Ask:** Hydration error pointing at **`SphereImageGrid`** tile wrapper (`className="absolute transition-transform duration-200 ease-out"`).

**Done:** **`src/components/ui/img-sphere.tsx`** — Server and the client’s first paint now match by **not rendering 3D tiles (or reduced-motion grid) until after `useLayoutEffect`**: a minimal placeholder shell (`layoutReady` gate) matches on SSR + hydration; tiles mount on the next commit. **`prefers-reduced-motion`** uses **`useSyncExternalStore`** with **`getServerSnapshot: () => false`** instead of **`useEffect`**, so the post-hydration branch respects motion prefs without relying on a late effect. **`npm run build`** OK.

### 29. **DemoOne**-style sphere (`layoutMode="demo"`) + `/sphere-demo`

**Ask:** Port the pasted **SphereImageGrid** / **DemoOne** snippet (Fibonacci + pole spread, collision, fade **-10 / -30**, CONFIG + 60 images) into this repo.

**Done:** Did **not** copy **`Math.random`** or **`setState` in `requestAnimationFrame`** (hydration + stale-velocity bugs). **`img-sphere.tsx`**: new **`layoutMode?: "arc" | "demo"`** (default **`arc`** for testimonials), **`initialRotation`**, **`generateDemoSpherePositions`** (deterministic jitter), demo branch in world positions + collision, demo starts at **15° / 15°** like the reference. **`src/content/sphereImageGridDemo.ts`**: **`SPHERE_DEMO_IMAGES`** (60 tiles) + **`SPHERE_DEMO_GRID_PROPS`** (same numbers as user CONFIG). **`src/app/sphere-demo/page.tsx`**: **`/sphere-demo`** with **`layoutMode="demo"`** + **`theme="light"`**. **`npm run build`** OK.

### 30. Volumetric sphere closer to reference img 1 (vs split layout img 2)

**Ask:** Same code but look differs — want **img 1** (deep black shell, strong Z scale/opacity, sparse, glowing teal selection), not **img 2** (current site).

**Done:** Clarified: **img 2** is the real **split** section (cream card + logo chrome); **img 1** is a tighter “hero” of the shell. Visual gap was mostly **tuning**, not a different component. **`img-sphere.tsx` (dark / `arc`)**: steeper **`depthT`** exponents for scale + **`depthOpacity`**, adjusted back-face fade band, stronger CSS **`perspective`** (**×1.38**), **`tileSelectedDark`** (thick ring + teal glow), selected tile scale **~1.58**. **`testimonialSphereBaseImages.ts`**: **`TESTIMONIAL_SPHERE_TILE_COUNT` 60 → 32** for sparser shell. **`ArcTestimonialsSection.tsx`**: **`sphereRadius`**, **`baseImageScale`**, **`perspective`** nudged up. Note: **`/sphere-demo`** still uses **`layoutMode="demo"`** (DemoOne / gray) — that path will **not** match img 1; testimonials use default **`arc`** + **`theme="dark"`**.
