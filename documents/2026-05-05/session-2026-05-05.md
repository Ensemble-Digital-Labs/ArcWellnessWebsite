# Session log – 2026-05-05

### 1. Fibonacci sequence mismatch in testimonial sphere

**Ask:** The image order/distribution in the testimonial sphere still did not match the target product’s Fibonacci sequencing.

**Done:** In `src/components/ui/img-sphere.tsx`, updated `generateSpherePositions` to use canonical Fibonacci azimuth ordering only, removing the extra custom theta offset (`+ i * 37.417 + (i % 7) * 13.1`) that changed image sequence. Theta is now normalized with a stable positive modulo. Verified with `npm run build` and lint diagnostics (no errors).

### 2. Slow down testimonial sphere auto-spin

**Ask:** Reduce the auto-spin speed of the testimonial sphere.

**Done:** In `src/components/arc/ArcTestimonialsSection.tsx`, reduced `autoRotateSpeed` from `0.2` to `0.08` on `SphereImageGrid` for a calmer idle motion. Verified with `npm run build` and lints (no errors).

### 3. Match sphere panel background to light-gray reference

**Ask:** Make the sphere background match the provided screenshot (light gray panel rather than black).

**Done:** In `src/components/arc/ArcTestimonialsSection.tsx`, changed the left sphere panel background from `bg-black` to `bg-[#ececec]` and switched `SphereImageGrid` from `theme="dark"` to `theme="light"` so tile rendering and shadows match a light surface. Verified with `npm run build` and lints (no errors).

### 4. Use exact section background color

**Ask:** Not light gray — make sphere background the same color as the section.

**Done:** In `src/components/arc/ArcTestimonialsSection.tsx`, changed the left sphere panel background from `bg-[#ececec]` to `bg-arc-cream` so both sides share the same section background token.

### 5. Increase sphere tile size after light-theme switch

**Ask:** Sphere images suddenly looked smaller.

**Done:** In `src/components/arc/ArcTestimonialsSection.tsx`, increased `baseImageScale` from `0.124` to `0.16` on `SphereImageGrid` to restore larger tile size on the light theme background.

### 6. Slightly larger sphere tiles again

**Ask:** Make sphere images a bit larger.

**Done:** In `src/components/arc/ArcTestimonialsSection.tsx`, `baseImageScale` `0.16` → `0.182`.

### 7. Hero CTA font + arrow refinement

**Ask:** Make the hero links (`Begin your Journey`, `See How it Works`) use the elegant serif font from reference and make the arrow longer.

**Done:** In `src/components/arc/ArcTextUnderlineCta.tsx`, updated shared CTA row typography to use `font-serif` with refined tracking and adjusted `ArrowRight` sizing from a square icon to a wider horizontal proportion (`w-[1.55em]`, `sm:w-6`) so the arrow reads longer while preserving current spacing, hover motion, and focus styles. Verified with IDE lints (no errors).

### 8. Uppercase CTA labels + extra-long arrows

**Ask:** Make the button/link labels all caps and make arrows longer.

**Done:** In `src/components/arc/ArcTextUnderlineCta.tsx`, set CTA row text to `uppercase` with wider tracking (`tracking-[0.1em]`) for both size variants, and lengthened the right-arrow footprint again (`w-[2.05em]`, `sm:w-7`) while keeping stroke, alignment, and hover transition behavior unchanged. Verified with IDE lints (no errors).

### 9. Curved underline aesthetic for CTA links

**Ask:** Make the CTA underline curved / more aesthetic.

**Done:** In `src/components/arc/ArcTextUnderlineCta.tsx`, replaced the flat 1px underline span with a responsive SVG arc (`Q` bezier path) so each CTA now has a subtle curved underline while preserving `currentColor`, opacity transitions, and reduced-motion compatibility. Verified with IDE lints (no errors).

### 10. Botanical wave underline style

**Ask:** Make the underline look like the provided decorative vine/wave reference.

**Done:** In `src/components/arc/ArcTextUnderlineCta.tsx`, replaced the single arc with a wider botanical SVG treatment: flowing wave baseline plus hand-drawn-style leaf ellipses above/below the line. Kept `currentColor` strokes so the ornament follows CTA accent color, and retained existing hover opacity transition behavior. Verified with IDE lints (no errors).

### 11. Refined underline pass (cleaner / less busy)

**Ask:** Underline should be more refined.

**Done:** In `src/components/arc/ArcTextUnderlineCta.tsx`, refined the decorative underline to a lighter premium look: reduced overall height/opacity, thinned line weight, simplified wave rhythm, and cut leaf count for a cleaner motif that reads elegant rather than ornate. Verified with IDE lints (no errors).

### 12. Ornamental center-flower underline variant

**Ask:** Create an underline like the new reference image (long line, center flower ornament, heart-style terminals).

**Done:** In `src/components/arc/ArcTextUnderlineCta.tsx`, replaced the vine motif with a new responsive SVG ornament: straight horizontal rails, small heart-like end caps, connector stems with side buds, and a stylized central flower emblem. Preserved `currentColor` stroke inheritance and hover opacity transitions so the ornament follows CTA accent color and interaction behavior. Verified with IDE lints (no errors).

### 13. Near-exact vine flourish replica pass

**Ask:** Build something as exact as possible to the latest black-background vine reference.

**Done:** In `src/components/arc/ArcTextUnderlineCta.tsx`, replaced the previous ornament with a mirrored vine-flourish SVG: curled end strokes, sweeping branch lines toward center, nested leaf clusters, small circular buds, and a compact 5-petal center flower with inner dot. Kept stroke-only rendering in `currentColor` and retained hover opacity transition behavior for consistency with existing CTA interactions. Verified with IDE lints (no errors).

### 14. Emphasize center flower size

**Ask:** The flower in the middle should be bigger and more emphasized.

**Done:** In `src/components/arc/ArcTextUnderlineCta.tsx`, increased the center flower presence by enlarging all five petal ellipses and the center dot, and slightly spreading petal anchor positions so the blossom reads more dominant relative to surrounding vines while preserving the same line style. Verified with IDE lints (no errors).

### 15. Increase center flower again

**Ask:** Bigger.

**Done:** In `src/components/arc/ArcTextUnderlineCta.tsx`, performed a second flower scale-up pass by increasing the center-dot radius and all five petal radii/positions again so the blossom now stands out decisively as the focal ornament. Verified with IDE lints (no errors).

### 16. Rotate top flower petal upward

**Ask:** Rotate the top petal upward.

**Done:** In `src/components/arc/ArcTextUnderlineCta.tsx`, adjusted the top petal ellipse proportions to a more vertical orientation (`rx` reduced, `ry` increased) and nudged it slightly upward so the top petal reads as pointing up. Verified with IDE lints (no errors).

### 17. Move center flower upward slightly

**Ask:** Move the flower up a little bit.

**Done:** In `src/components/arc/ArcTextUnderlineCta.tsx`, shifted the full center flower cluster upward by reducing the `cy` values of the center dot and all five petals uniformly, preserving the same petal shape and orientation while raising overall placement on the vine. Verified with IDE lints (no errors).

### 18. Move center flower up a bit more

**Ask:** Up a bit more.

**Done:** In `src/components/arc/ArcTextUnderlineCta.tsx`, applied a second upward nudge to the same center flower cluster (center dot + all petals) by lowering all related `cy` coordinates again, keeping scale and shape unchanged. Verified with IDE lints (no errors).

### 19. Increase CTA link font weight

**Ask:** Make the hero CTA font bolder.

**Done:** In `src/components/arc/ArcTextUnderlineCta.tsx`, increased shared CTA row typography weight from `font-medium` to `font-semibold` for both `sm` and `md` variants so `BEGIN YOUR JOURNEY` and `SEE HOW IT WORKS` read stronger. Verified with IDE lints (no errors).

### 20. Slightly more CTA weight for visibility

**Ask:** Make it a little bit bolder to improve visibility.

**Done:** In `src/components/arc/ArcTextUnderlineCta.tsx`, bumped CTA row typography from `font-semibold` to `font-bold` (both size variants) for stronger legibility against photography while preserving uppercase styling and spacing. Verified with IDE lints (no errors).

### 21. Right-side CTA group with shared left start

**Ask:** Keep both hero CTAs on the right side, but align them so both labels start from the same left position.

**Done:** Updated hero CTA alignment to preserve right-side placement while making link starts consistent: in `src/components/arc/ScrollExpandHero.tsx`, changed the CTA stack and CTA component alignment from `items-end` to `items-start`; in `src/components/arc/HeroCtaCursorPreviews.tsx`, changed per-row wrapper alignment from `justify-end` to `justify-start`. Result: both links share a clean common left edge inside the right rail. Verified with IDE lints (no errors).

### 22. Lengthen CTA arrow again

**Ask:** Make the arrow longer.

**Done:** In `src/components/arc/ArcTextUnderlineCta.tsx`, increased the `ArrowRight` width footprint again from `w-[2.05em]` to `w-[2.45em]` (and `sm:w-7` to `sm:w-8`) so the arrow reads longer while preserving hover motion and stroke weight. Verified with IDE lints (no errors).

### 23. Longer arrow tail (custom glyph)

**Ask:** Make the hero CTA arrows have a longer tail.

**Done:** In `src/components/arc/ArcTextUnderlineCta.tsx`, replaced the `lucide-react` `ArrowRight` icon with a custom inline SVG arrow (long horizontal stem + compact chevron head) and widened its container sizing (`w-[2.8em]`, `sm:w-9`) so the tail visibly extends while preserving existing hover shift, opacity transition, and stroke styling. Verified with IDE lints (no errors).

### 24. Remove stray thin left line on CTA block

**Ask:** There is a thin line on the left side of the CTA block.

**Done:** In `src/components/arc/ScrollExpandHero.tsx`, removed the CTA rail `border-l border-white/18` from the `HeroCtaCursorPreviews` wrapper class so the visual divider line no longer appears, while preserving right-side placement, spacing, and alignment. Verified with IDE lints (no errors).

### 25. Create standardized text CTA component from micro style

**Ask:** Use the `EXPLORE THE PRACTICE` button style as the standardized button style for the website.

**Done:** Added new reusable component `src/components/arc/ArcStandardCta.tsx` encapsulating that exact visual pattern (uppercase sans, spaced tracking, teal hover shift, subtle arrow motion, accessible focus ring). Updated `src/components/arc/ArcMicroStatementSection.tsx` to use `ArcStandardCta` for its link CTA instead of local inline `Link` + `ArrowRight` classes. This establishes a single shared implementation to reuse across other sections for consistency. Verified with IDE lints (no errors).

### 26. Apply standardized CTA to chapter intro section

**Ask:** The shown CTA still looked unchanged.

**Done:** Located remaining local CTA markup in `src/components/arc/ScrollChapterIntroSection.tsx` (`Continue to who we are`) and replaced it with `ArcStandardCta`, removing section-local `Link` and `ArrowRight` usage. Preserved the existing reveal animation via inline style (`opacity` + `translateY`) and spacing (`mt-8 sm:mt-10`) so behavior remains the same while visual style now comes from the shared standardized component. Verified with IDE lints (no errors).

### 27. Switch micro statement CTA to hero-styled underline CTA

**Ask:** The micro statement `EXPLORE THE PRACTICE` CTA was still not visually changed.

**Done:** In `src/components/arc/ArcMicroStatementSection.tsx`, replaced `ArcStandardCta` with `ArcTextUnderlineCta` (`accent="teal"`) so this CTA now uses the same styled underline treatment as hero CTAs. Kept pinned reveal animation by wrapping the CTA with a motion-style container that still applies the existing `linkMotion` (`opacity` + `translateY`). Verified with IDE lints (no errors).

### 28. Apply same CTA style to additional sections

**Ask:** Use the same button style for three additional CTAs (`SEE OUR PILLARS OF CARE`, `START YOUR JOURNEY`, `BOOK NOW`).

**Done:** Replaced local CTA link implementations with `ArcTextUnderlineCta` in:
- `src/components/arc/ArcWelcomeSplitSection.tsx` (both reduced-motion and immersive variants for `ctaLabel`, which includes `SEE OUR PILLARS OF CARE`)
- `src/components/arc/YourPathSection.tsx` (`START YOUR JOURNEY`)
- `src/components/arc/InvestCTASection.tsx` (`BOOK NOW`)

Removed section-local `Link`/`ArrowRight` imports where no longer needed, preserved layout intent with `items-start` / `items-center` class alignment per section, and kept existing spacing. Verified with IDE lints (no errors).

### 29. Bolder CTA label weight

**Ask:** Make the button font bolder.

**Done:** In `src/components/arc/ArcTextUnderlineCta.tsx`, increased label weight from `font-bold` to `font-extrabold` for both size variants. In `src/components/arc/ArcStandardCta.tsx`, increased from `font-semibold` to `font-bold` for consistency where that component is used. Verified with IDE lints (no errors).

### 30. CTA weight not visible — load Playfair heavy weights

**Ask:** Why the bolder CTA change was not visible.

**Done:** Next.js was only configuring `Playfair_Display` without explicit `weight`, so heavier utilities could map to the same rendered face as `font-bold`. Updated `src/app/layout.tsx` to pass `weight: ["400","500","600","700","800","900"]` so 800/900 glyphs load. In `src/components/arc/ArcTextUnderlineCta.tsx`, bumped CTA labels to `font-black` (900) for a clearer step up from body serif. Verified with `npm run build` and IDE lints (no errors).

### 31. Stronger visibility for all CTAs / buttons

**Ask:** Further increase visibility for all the buttons.

**Done:** Broad pass for contrast and legibility: `ArcTextUnderlineCta` — larger type scale, stronger layered `text-shadow` for teal and champagne, thicker arrow and ornament strokes, higher default opacity. `ArcStandardCta` — larger type, teal glow shadow, bigger arrow. `layout.tsx` — `DM_Sans` now loads explicit weights through 700 for real bold. `Button` (`button.tsx`) — `font-bold`, wider tracking, per-variant label shadows (teal fill, cream hero outline, charcoal outline), stronger teal glow on default variant. Inline teal pills — `WhoWeAreSection` and `ArcFooter` book links: `font-bold`, larger text, teal drop shadow + hover shadow. Verified with `npm run build` and IDE lints (no errors).

### 32. Teal readability on light backgrounds (ink vs vivid)

**Ask:** Signature teal feels too low-contrast on white / light grey sections.

**Done:** Added `--arc-teal-ink` / `--arc-teal-ink-hover` in `globals.css` plus `@theme` colors for Tailwind. `ArcTextUnderlineCta` default accent is now ink teal with a light-surface-friendly shadow; new `accent="tealBright"` keeps vivid `--arc-teal` for dark/photo contexts — applied in `ScrollExpandHero.tsx` and `InvestCTASection.tsx`. Migrated light-section accents to ink: `ArcMicroStatementSection` (eyebrow, script emphasis, CTA default), `ArcWelcomeSplitSection`, `ArcStandardCta`, `WhoWeAreSection` eyebrow, `YourPathSection` icons, `ArcFounderIntroSection`, `ArcTestimonialsSection` label, `ArcFooter` links/icons/marquee. Verified with `npm run build` and IDE lints (no errors).

### 33. Larger, bolder Birthstone / `TitleEmphasis` everywhere

**Ask:** All emphasized script words (hero keywords, micro statements, welcome, founder) should be bigger and bolder.

**Done:** `globals.css` `.font-title-emphasis` — added subtle `-webkit-text-stroke` + `paint-order` so single-weight Birthstone reads heavier. `TitleEmphasis.tsx` — increased default scale (`1.12em` → `1.2em`). `ScrollExpandHero.tsx` — larger keyword scale at all breakpoints, faux-bold shadow on vivid + blend variants, hero title rows + first line `font-semibold` → `font-bold`. `ArcMicroStatementSection`, `ArcWelcomeSplitSection` (both layouts), `ArcFounderIntroSection` — larger `TitleEmphasis` em steps, light-surface text-shadow for legibility, parent `h2` `font-bold` where applicable. Verified with `npm run build` and IDE lints (no errors).

### 34. Hero title — keep keyword row on one line (large screens)

**Ask:** On bigger screens, the hero line with Aesthetics / Wellness / Longevity should not break across two lines.

**Done:** In `ScrollExpandHero.tsx`, updated `titleLine2and3Class` so from **`lg` and up** the flex row uses **`lg:flex-nowrap`**, **`lg:max-w-none`** (use full rail width), and **`lg:gap-y-0`**; below `lg`, **`flex-wrap`** remains for narrow viewports. Verified with `npm run build` and IDE lints (no errors).

### 35. Hero intro — bold key phrases

**Ask:** Hero blurb should keep the same message but bold key words for attention.

**Done:** `ScrollExpandHero` `intro` prop type **`string` → `ReactNode`**. Intro paragraph classes include **`[&_strong]:font-bold`**, full-opacity cream on **`strong`**, and extra **`drop-shadow`**. **`page.tsx`** passes JSX wrapping **ARC Wellness**, **thoughtful aesthetics**, **whole-body wellness**, **age intentionally**, and **confidence** in **`<strong>`**. Verified with `npm run build` and IDE lints (no errors).

### 36. Hero intro — ornamental frame (corner flourishes)

**Ask:** Place the hero statement in a decorative frame like the provided corner-flourish reference.

**Done:** In `ScrollExpandHero.tsx`, added **`HeroIntroCornerOrnament`** (multi-stroke SVG corner) and **`HeroIntroOrnamentFrame`** wrapper: light **`border-white/22`**, **`bg-black/12`**, inset highlight, **`backdrop-blur-[2px]`**, four mirrored corner SVGs with **`text-[#f7f4ef]/72`** and drop shadow; intro copy stays inside **`relative z-10`**. Preserved existing max-width rail and strong styling on inner **`<p>`**. Verified with `npm run build` and IDE lints (no errors).

### 37. Hero intro frame — restyle toward reference (no glass panel)

**Ask:** Frame did not match the ornate corner reference; looked wrong.

**Done:** Replaced the frosted panel with a **certificate-style** treatment: **no fill / no backdrop-blur**, only **`border-white/14` hairline rectangle** inset between corners and **larger** corner SVGs (double-line L, parallel inner L, scroll finials on each arm, small vertex knot). Corners use **`drop-shadow`** for legibility on photography; padding/inset tuned so type clears ornament. Verified with `npm run build` and IDE lints (no errors).

### 38. Hero intro — two corners, more air around copy

**Ask:** Use only two corners for elegance; push ornament further from the text.

**Done:** **`HeroIntroOrnamentFrame`** now uses **two corners only** (pair updated to **top-right + bottom-left** per preference); removed the connecting hairline rectangle. **Inner padding** increased (`px-7`–`md:px-10`, `pt`/`pb` stepped up through `md`) so flourishes read as distant bookends; **max-width** nudged up slightly so line length stays comfortable. Verified with IDE lints (no errors).

### 39. Hero intro — Presidio copy + script “you”

**Ask:** Replace hero blurb with clinical-sanctuary / Presidio / plan copy; make **you** larger with the emphasis (Birthstone) font.

**Done:** **`page.tsx`** — new intro paragraph; final **you** wrapped in **`TitleEmphasis`** with **`text-[1.48em]` / `md:text-[1.62em]`**, full cream, and photo legibility **`drop-shadow`**. Verified with IDE lints (no errors).

### 40. Hero intro — put “you” on its own line

**Ask:** Separate the word **you** into its own individual line in the hero intro.

**Done:** In **`src/app/page.tsx`**, inserted a `<br />` before the emphasized **you** token so it breaks to a dedicated line while keeping existing hero intro styling. Verified with IDE lints (no errors).

### 41. Hero intro — make “you” much larger

**Ask:** Make **you** way bigger than the current size.

**Done:** In **`src/app/page.tsx`**, increased responsive `TitleEmphasis` sizing for **you** to `text-[2.1em] sm:text-[2.4em] md:text-[2.7em] lg:text-[3.1em]` with stronger shadow for readability on the hero image. Verified with IDE lints (no errors).

### 42. Hero intro — revert “you” to normal text

**Ask:** Keep the word **you** normal (not specially styled).

**Done:** In **`src/app/page.tsx`**, removed `TitleEmphasis` usage for the final word and simplified the sentence to plain text: `for one patient: you.`; also removed the now-unused `TitleEmphasis` import. Verified with IDE lints (no errors).

### 43. Add concerns list section below first micro statement

**Ask:** Add a new section directly below the pinned micro statement containing:
Low Energy & Burnout, Hormonal Imbalance & Weight Gain, Poor Sleep & Recovery, Aging Skin & Body Changes, and Brain Fog & Focus Issues.

**Done:** In **`src/app/page.tsx`**, inserted a new cream-background section immediately after the first `ArcMicroStatementSection`, using a centered rounded container and responsive list layout (`md:grid-cols-2`) for those five bullet items. Verified with IDE lints (no errors).

### 44. Add unique selling points to new concerns section

**Ask:** Also mention the unique selling points shown in the reference strip.

**Done:** In **`src/app/page.tsx`**, added a compact stat/USP row beneath the five concerns list with four items: **`12+` Treatment Modalities**, **`FDA` Cleared Technology**, **`0` Membership Required**, and **`Free` Initial Consultation**. Styled as a responsive 2x2 (mobile) / 4-column (desktop) bordered grid to match the existing premium cream/charcoal visual language. Verified with IDE lints (no errors).

### 45. Convert concerns/USP block into pinned full-screen section

**Ask:** Make this section a lock-screen full-view scrolling animation section as well.

**Done:** Created **`src/components/arc/ArcConcernsPinnedSection.tsx`** as a client component with GSAP `ScrollTrigger` pin/scrub behavior on `#main` (same locomotive-ready lifecycle pattern used by existing pinned sections), including `prefers-reduced-motion` fallback. Moved the concerns list + USP grid into this component, set the wrapper to `min-h-[100dvh]`, and added progress-based fade/translate reveal for the content card. Replaced the inline section in **`src/app/page.tsx`** with `<ArcConcernsPinnedSection />` and added its import. Verified with IDE lints (no errors).

### 46. Restyle pinned concerns section to expanding-card layout

**Ask:** Use the provided design pattern (horizontal interactive gallery style) for this section.

**Done:** In **`src/components/arc/ArcConcernsPinnedSection.tsx`**, redesigned the section into a premium expanding-card gallery while preserving existing concerns and USP content. Added a heading/subcopy, mapped five concern cards to existing local ARC imagery (`images.services` / `images.whoWeAre`), and implemented desktop hover/focus expansion (`flex` growth) with gradient overlays and anchored labels. Kept the section pinned full-screen behavior and retained the 4-item USP strip below the gallery. Verified with IDE lints (no errors).

### 47. Remove boxed container from pinned concerns section

**Ask:** Make this section full-screen and remove the inner container card.

**Done:** In **`src/components/arc/ArcConcernsPinnedSection.tsx`**, removed the rounded bordered white card wrapper (`max-w-6xl`, background, border, shadow, card padding) so the section reads as a true full-screen composition. Also removed card rounding/borders from the gallery row and switched the USP strip to lighter full-width divider lines (`border-y`) instead of boxed panel styling. Pinned lock-screen animation behavior remains unchanged. Verified with IDE lints (no errors).

### 48. Remove concerns subtitle and add top-safe spacing

**Ask:** Remove the subtitle text and ensure the section title never touches the logo across screen sizes.

**Done:** In **`src/components/arc/ArcConcernsPinnedSection.tsx`**, removed the subtitle paragraph below the heading. Updated layout from vertical centering to top-aligned content (`justify-start`) and introduced stronger responsive top spacing (`pt-28 sm:pt-32 md:pt-36 lg:pt-40`) plus preserved bottom padding so the heading consistently clears the logo/header zone across viewport sizes. Verified with IDE lints (no errors).

### 49. Organize new concerns images and wire section to them

**Ask:** Newly added 5 images should be organized in assets and used for this concerns section.

**Done:** Moved five root-level PNGs into a dedicated folder **`public/assets/sections/concerns/`** and normalized filenames:
`concern-low-energy-burnout.png`, `concern-hormonal-imbalance-weight-gain.png`, `concern-poor-sleep-recovery.png`, `concern-aging-skin-body-changes.png`, `concern-brain-fog-focus-issues.png`.
Updated **`src/components/arc/ArcConcernsPinnedSection.tsx`** `CONCERN_IMAGES` to point to these new local `/assets/sections/concerns/...` paths and removed the now-unused `images` import. Verified with IDE lints (no errors).

### 50. Keep last hovered concern card open

**Ask:** One image should be open on scroll, and when cursor leaves the 5-image area it should remember and keep the last selected image open.

**Done:** In **`src/components/arc/ArcConcernsPinnedSection.tsx`**, removed the gallery `onMouseLeave={() => setActiveIndex(0)}` reset behavior. Since `activeIndex` already initializes to `0`, one card is open by default when the section appears; after hover/focus/click changes, the last selected card now remains open even when the cursor leaves the gallery. Verified with IDE lints (no errors).

### 51. Add emphasized keyword styling to concerns title

**Ask:** Use the same emphasized keyword style from the micro section title (`TitleEmphasis`) for this section heading.

**Done:** In **`src/components/arc/ArcConcernsPinnedSection.tsx`**, imported `TitleEmphasis` and updated the heading to include an emphasized keyword phrase: **`every step`**. Applied the same script size, teal-ink color, and text-shadow treatment used in `ArcMicroStatementSection` (`text-[1.52em]` up to `lg:text-[1.82em]`) and aligned heading typography with the micro section (`leading`, `md` size steps). Verified with IDE lints (no errors).

### 52. Adjust emphasized keyword to match section meaning

**Ask:** Confirm whether `every step` is the right wording and match the title to the section content.

**Done:** In **`src/components/arc/ArcConcernsPinnedSection.tsx`**, changed the emphasized phrase from **`every step`** to **`thoughtfully mapped`** so the title aligns with the concerns-selection content rather than process language. Verified with IDE lints (no errors).

### 53. Add dedicated background image for concerns section

**Ask:** Add the provided image as this section background, rename it, organize it in assets, and use it.

**Done:** Moved and renamed the new root image to **`public/assets/sections/concerns/concerns-section-background.png`**. In **`src/components/arc/ArcConcernsPinnedSection.tsx`**, added an absolute background layer using `next/image` (`CONCERNS_SECTION_BG`) plus a cream overlay for readability, and kept content above it with `relative z-10`. Verified with IDE lints (no errors).

### 54. Concerns section background actually visible

**Ask:** Background still not showing in the section (looked like flat cream only).

**Done:** The image had **`opacity-[0.14]`** plus **`bg-arc-cream/84`**, which hid the art. Replaced with full-strength cover image and a **vertical cream gradient** (`from-arc-cream/80` → `to-arc-cream/45`) so the texture reads while copy and cards stay legible. Set the background wrapper to **`z-0 min-h-[100dvh]`** for reliable fill under the pinned viewport. Verified with IDE lints (no errors).

### 55. Ambient texture on concerns USP stat cells

**Ask:** Use `ambient-01.png`-style background on the four USP boxes (`12+`, `FDA`, `0`, `Free`).

**Done:** In **`src/components/arc/ArcConcernsPinnedSection.tsx`**, imported **`BACKGROUND_DECORATION_IMAGES`** from **`src/content/backgroundDecoration.ts`** and used **`ambient-01`** (`BACKGROUND_DECORATION_IMAGES[0]`) as a **`next/image` fill** layer behind each of the four grid cells, with **`bg-arc-cream/72`** wash and **`relative z-10`** on the stat copy for readability. Preserved existing borders and grid layout. Verified with IDE lints (no errors).

### 56. USP bar: no cream wash, centered copy, full-bleed bottom

**Ask:** For the four stat boxes, remove the cream overlay, middle-align the text, and run the bar edge-to-edge along the bottom of the section.

**Done:** In **`src/components/arc/ArcConcernsPinnedSection.tsx`**, removed the per-cell **`bg-arc-cream/72`** layer. Each cell now uses **`flex items-center justify-center text-center`** with a slightly taller **`min-h`**. Restructured layout: title + gallery stay in the padded **`max-w-7xl`** column; the USP grid is a sibling **`w-full`** row with **`mt-auto`** inside a **`min-h-[100dvh] flex-col`** stack so the bar spans the full viewport width and sits at the bottom of the pinned section. Verified with IDE lints (no errors).

### 57. Scroll-scrubbed fade-in on USP bar

**Ask:** Add the same kind of faded-in scroll animation to the bottom stat bar.

**Done:** In **`src/components/arc/ArcConcernsPinnedSection.tsx`**, derived **`uspReveal`** from the existing pinned scrub progress **`p`** with a later start (`(p - 0.28) / 0.52` clamped) and applied **`opacity`** plus **`translateY`** on the full-bleed bar wrapper so it eases up into place while scrolling through the pin. **`prefers-reduced-motion`** still forces **`p === 1`**, so the bar renders fully visible. Verified with IDE lints (no errors).

### 58. Welcome (#about) body copy refresh

**Ask:** Replace the two welcome paragraphs with new positioning copy about accessible pricing, medical-grade treatments, and inclusive self-care; split for nicer layout.

**Done:** In **`src/content/homepage.ts`** (`homeWelcome`), replaced **`paragraph1`** and **`paragraph2`** with the supplied messaging split into two blocks: first paragraph covers belief + treatments + pricing for lasting wellness; second paragraph is the short “Indulgent, yes…” closer. **`ArcWelcomeSplitSection`** already renders each as its own `<p>` with spacing. Verified with IDE lints (no errors).

### 59. Remove founder “Your physician” eyebrow

**Ask:** Remove the uppercase eyebrow row above the founder headline (`YOUR PHYSICIAN`).

**Done:** **`eyebrow`** is now optional on **`ArcFounderIntroSection`**; the label + hairline row renders only when **`eyebrow?.trim()`** is set. Removed **`eyebrow`** from **`homeFounder`** in **`src/content/homepage.ts`** and dropped the prop from **`src/app/page.tsx`**. Verified with IDE lints (no errors).

### 60. Wire custom image for “SEE HOW IT WORKS” hover preview

**Ask:** Use the newly added image for the pop/preview tied to the `SEE HOW IT WORKS` hero CTA, and organize/rename it in assets.

**Done:** Moved and renamed `ChatGPT Image May 5, 2026, 12_34_16 PM.png` to **`public/assets/hero/hero-cta-see-how-it-works-preview.png`**. Added `images.heroCtaSeeHowItWorksPreview` in **`src/content/site.ts`** and updated the second preview slide in **`src/components/arc/ScrollExpandHero.tsx`** to use this new asset (the first preview remains `images.investBanner`). Verified with IDE lints (no errors).

### 61. Widen #about copy blocks on laptop+ screens

**Ask:** Increase max width of the #about text boxes on larger laptop screens so lines break less aggressively.

**Done:** In **`src/components/arc/ArcWelcomeSplitSection.tsx`**, widened copy containers at larger breakpoints:
- static body text block: `md:max-w-2xl` and `lg:max-w-[44rem]`
- immersive center copy wrapper: `md:max-w-4xl lg:max-w-5xl`
- immersive paragraph block: added `lg:max-w-[54rem]`
This keeps mobile sizing intact while reducing early wraps on laptop/desktop layouts. Verified with IDE lints (no errors).

### 62. Micro statement — real ornamental frame (replace missing PNG)

**Ask:** Add a frame around the pinned micro statement copy block similar to the hero ornamental / certificate style.

**Done:** In **`src/components/arc/ArcMicroStatementSection.tsx`**, replaced the **`MicroStatementCopyFrame`** implementation that referenced a missing **`micro-statement-sketch-frame.png`**. New frame uses **four mirrored SVG corners** (same structural language as hero flourishes) with **`stroke-arc-charcoal`** + **`stroke-arc-teal-ink`** accents for legibility on the cream scrim, plus a subtle **hairline border**, **light cream glass fill** (`bg-arc-cream/25`), **backdrop blur**, and **soft shadow**. Inner padding uses responsive `clamp()` so copy clears the ornaments. Verified with IDE lints (no errors).

### 63. Remove micro statement frame box

**Ask:** Remove the framed box around the micro statement copy.

**Done:** In **`src/components/arc/ArcMicroStatementSection.tsx`**, removed the ornamental corner SVGs, border, panel fill, blur, and shadow. **`MicroStatementCopyFrame`** is now a minimal **`flex flex-col items-center text-center`** wrapper only. Verified with IDE lints (no errors).

### 64. Tibbixel SVG frame on micro statement copy

**Ask:** Use uploaded vector `tibbixel-dot-com-26602-svg.svg` as the frame around the pinned micro statement text block.

**Done:** Moved file to **`public/assets/decoration/micro-statement-tibbixel-frame.svg`**. Updated **`MicroStatementCopyFrame`** in **`src/components/arc/ArcMicroStatementSection.tsx`** to paint it as a **`background-image`** with **`backgroundSize: 100% 100%`**, responsive **`clamp()`** padding so copy sits inside the art, and inner **`relative z-10`** stack. Documented path in **`public/assets/decoration/README.md`**. Verified with IDE lints (no errors).

### 65. Rotate + scale Tibbixel frame to fit copy width

**Ask:** Frame was too tall/narrow for the text; rotate and expand so it fits the text block.

**Done:** In **`MicroStatementCopyFrame`**, moved the art to an absolutely centered layer sized with the SVG’s **portrait aspect** (`aspect-[267/354]`) and wide **`w-[min(118vw,76rem)]`**, then applied **`transform: translate(-50%,-50%) rotate(90deg) scale(1.14)`** so the frame reads **landscape** and wraps headline + subline + CTA. Increased inner **`clamp()`** padding so type sits inside the opening. Verified with IDE lints (no errors).

### 66. Micro statement — Tibbixel frame fades in with pinned scroll

**Ask:** Make the frame fade in with scroll like the other elements in the pinned micro statement section.

**Done:** In **`src/components/arc/ArcMicroStatementSection.tsx`**, added optional **`ornamentMotion`** on **`MicroStatementCopyFrame`** (opacity + small vertical drift driven by scrub progress). When **`pinnedScrollMotion`** is true, the parent computes **`frameOrnamentMotion`** from the same **`progress`** used for headline/subline/CTA so the Tibbixel layer eases in with the section; non-pinned and reduced-motion paths keep the frame at full visibility. Removed redundant **`ornamentMotion={undefined}`** on the non-pinned branch. Verified with IDE lints (no errors).

### 67. Welcome `#about` — same Tibbixel frame as micro statement

**Ask:** Use the same ornamental Tibbixel frame around the immersive about copy block (the centered **`motion.div`** stack under **`section#about`**).

**Done:** Extracted shared **`ArcTibbixelCopyFrame`** to **`src/components/arc/ArcTibbixelCopyFrame.tsx`** (SVG background, rotated/scaled ornament, responsive padding, optional **`ornamentMotion`** / **`ornamentClassName`** / **`innerClassName`**). **`ArcMicroStatementSection`** now imports it instead of an inline frame component. **`ArcWelcomeSplitSection`**: wrapped immersive copy (**`WelcomeImmersiveScrollBody`**) in **`ArcTibbixelCopyFrame`** with the prior max-width + centered text classes and a wider **`ornamentClassName`** (`w-[min(98vw,68rem)]`) so the art clears **`lg:max-w-5xl`** body type; the parent **`motion.div`** still applies **`opacityCopy`** / **`copyScale`**, so the frame fades and scales with the copy. Reduced-motion **`WelcomeSplitStaticBody`** uses the same frame around headline + paragraphs + CTA with **`items-start text-left`** and a column-sized ornament. Verified with **`npm run build`** and IDE lints (no errors).

### 68. Header frost — blur frame (and other layers) behind the logo

**Ask:** The Tibbixel frame reaches the fixed wordmark; blur the frame behind the logo for separation / legibility.

**Done:** In **`src/components/arc/ArcSiteHeader.tsx`**, added a **full-bleed absolute band** under the logo rail (matching logo stack heights **`h-32` → `lg:h-48`**) with **`backdrop-blur`**, **`backdrop-saturate`**, a **cream gradient scrim**, and a **bottom fade mask** so content scrolling beneath (including ornate SVG frames) reads softer behind the wordmark without a solid bar. Wrapped the logo + menu row in **`relative z-10`** so the wordmark and controls stay above the frost. Verified with **`npm run build`** and IDE lints (no errors).

### 69. Slide-in nav — frosted glass (blur behind drawer)

**Ask:** The blur should be **under the nav menu** (the open drawer), not only behind the logo.

**Done:** In **`src/components/arc/ArcSiteHeader.tsx`**, replaced the drawer’s solid **`bg-arc-cream`** with a **semi-transparent cream fill** plus **`backdrop-blur-2xl`** / **`backdrop-saturate-150`** (and a slightly lighter cream when **`backdrop-filter`** is supported). **Lightened** the three stacked **`arc-nav-backdrop-strip`** washes (**`/90` → `/45`**, **`/25` → `/18`**, **`/80` → `/38`**) so the frosted effect reads through while keeping teal/cream tint. Added a subtle **`border-l border-white/30`** on the panel edge. Verified with **`npm run build`** and IDE lints (no errors).

### 70. Nav drawer stacking — menu above page, chrome above drawer

**Ask:** The nav menu should be **on top of everything** (not covered by the header / frost).

**Done:** The open state had **`header z-[110]`** while **overlay `z-[100]`** and **drawer `z-[101]`**, so the **full-width header** (including the top frost) painted **over** the slide-in panel. **Split** the top treatment: **frost** is a standalone **`fixed`** band at **`z-[60]`**; **overlay** uses **`z-[1000]`**, **drawer** **`z-[1001]`**, and **`<header>`** (logo + Menu/Close only) **`z-[1002]`** so the dimmer and drawer sit above all page content while **Close** and the wordmark remain usable on top. Module-level **`NAV_STACK_*`** constants document the stack. Verified with **`npm run build`** and IDE lints (no errors).

### 71. Remove frost “front panels” from header

**Ask:** Remove the front / frost panel(s).

**Done:** In **`src/components/arc/ArcSiteHeader.tsx`**, removed the **fixed top frosted scrim** behind the wordmark (gradient + **`backdrop-blur`**). Restored the **slide-in nav** to **opaque **`bg-arc-cream`** with the original **teal/cream backdrop strip** opacities (**`/90`**, **`/25`**, **`/80`**), dropping **`backdrop-blur`**, semi-transparent fills, and the glass **`border-l`**. **Overlay / drawer / chrome z-index stack** unchanged. Verified with **`npm run build`** and IDE lints (no errors).

### 72. Founder `#founder` — horizontal image accordion layout

**Ask:** Redesign **`PinnedSection` `#founder`** using a reference layout: **left copy**, **right horizontal image accordion** (expand on hover).

**Done:** **`ArcFounderIntroSection`** is now a **client** component with **`useState`** active strip, **`FounderAccordionStrip`** using **`next/image`**, ARC **cream / charcoal / teal** styling, **vertical-rl** captions on collapsed strips, gradient overlay, **focus rings**, and **`prefers-reduced-motion`** (no width/hover scale transitions). **Tap + hover + focus** activate a panel (mobile-friendly). **`homeFounder.accordionPanels`** added in **`homepage.ts`** — five entries (**founder portrait** + **`images.services[0–3]`**) with short labels; **`page.tsx`** passes **`accordionPanels`**. Layout: **copy left / accordion right** from **`md:`** up; narrow viewports stack **accordion above** copy. **`imageSrc`** retained as fallback when **`accordionPanels`** omitted. Verified with **`npm run build`** and IDE lints (no errors).

### 73. Founder accordion — tablet width overlap / adaptability

**Ask:** Around **~936px** the accordion overlapped the text; layout not adaptable on smaller screens.

**Done:** **Two-column layout deferred to `lg:`** (`1024px+`) so tablet widths stay **stacked** (full-width copy + full-width accordion) instead of squeezing **~680px** of strips beside **~26rem** copy. **Accordion scroller**: outer **`w-full min-w-0 max-w-full overflow-x-auto overscroll-x-contain`**, inner **`flex w-max`** row so extra width **scrolls inside the right column** instead of painting over the left. Removed **`md:overflow-visible`** (it let strips escape). **Text column** **`lg:shrink-0`**; **accordion column** **`lg:flex-1`** with **`min-w-0`**. Active strip width **`min(400px, 100vw − padding)`** below **`lg`**, fixed **`400px`** at **`lg`**. Slightly shorter strip heights below **`lg`**. Thin scrollbar styling for discoverability. Verified with **`npm run build`** and IDE lints (no errors).

### 74. Founder section — mosaic grid (replace horizontal accordion)

**Ask:** Horizontal accordion layout still not working well; switch to a **2-column masonry-style gallery grid** with staggered blur/slide animation (reference using **`motion/react`**).

**Done:** Added **`src/components/arc/FounderGalleryMosaic.tsx`**: **`framer-motion`** (repo dependency; not **`motion/react`**) **`whileInView`** on a **`motion.div`** grid with **`staggerChildren`**, per-cell **`variants`** (**opacity / y / blur**), spring transitions, and **`useReducedMotion`** (opacity-only). Grid matches reference **`grid-cols-2`** + row template + **`areaClasses`** placement. **`next/image`** in each cell, gradient + label. **`useFourPanels`** fills four slots from **`accordionPanels`**. **`ArcFounderIntroSection`** now uses **`FounderGalleryMosaic`** only (removed accordion strip / **`useState`**); right column **`max-w`** caps for pin width. Verified with **`npm run build`** and IDE lints (no errors).

### 75. Founder mosaic — auto-rotating images in two tiles

**Ask:** Two boxes should cycle through **3–4 images** automatically and keep changing.

**Done:** In **`FounderGalleryMosaic.tsx`**, **tiles 1 and 3** (grid positions **`areaClasses[1]`** / **`[3]`**) are **`RotatingMosaicCell`** components with **`useRotatingIndex`** timers (**~3.2s** and **~4.1s**), advancing only when **`document.visibilityState === 'visible'`**. **`useRotatePools`** builds two **deduped** sequences from **`panels`** (e.g. indices **1–4** vs **2,4,0,3**). **`AnimatePresence` `mode="wait"`** crossfades slides; **`prefers-reduced-motion`** disables the interval and shows one slide. **Tiles 0 and 2** stay **static** (portrait + one still). Verified with **`npm run build`** and IDE lints (no errors).

### 76. Homepage — remove trust strip + second micro statement

**Ask:** Remove the teal **trust strip** (“Evidence-informed protocols…”) and the **unpinned** **Why ARC** **`ArcMicroStatementSection`** block.

**Done:** In **`src/app/page.tsx`**, removed **`ArcTrustStripSection`** and the second **`ArcMicroStatementSection`** ( **`homeMicro2`** ). Dropped unused imports **`ArcTrustStripSection`**, **`homeMicro2`**, **`homeTrustStrip`**. **`homeMicro2`** / **`homeTrustStrip`** data remain in **`homepage.ts`** for reuse. Verified with **`npm run build`** and IDE lints (no errors).

### 77. Founder mosaic — images only from `/assets/sections/founder/`

**Ask:** **`#founder`** section should use **only founder-folder** photography, not service / clinic stock.

**Done:** **`src/content/site.ts`**: added **`founderGallery`** (five PNG paths under **`public/assets/sections/founder/`** — professional **02–04** + smiling **01–02**). **`homeFounder.accordionPanels`** in **`homepage.ts`** now uses **`images.founderPortrait`** plus **`images.founderGallery[0..3]`** (removed **`images.services`**). Mosaic / rotation behavior unchanged. Verified with **`npm run build`** and IDE lints (no errors).

### 78. Founder `#founder` — zoom / parallax collage (scroll-scrubbed)

**Ask:** Make the doctor / founder block feel like the reference **zoom parallax** collage (multi-image grid with cream gutters, strong central focal), with **motion tied to scrolling**.

**Done:** Extended **`useArcFullscreenPin`** in **`src/lib/arcSectionPins.ts`** with optional **`ArcFullscreenPinOptions.onProgress`** (ScrollTrigger **`onUpdate`**, stable via ref) so pinned sections can drive scrubbed UI without a second trigger. Added **`src/components/arc/FounderZoomParallaxCollage.tsx`**: seven absolutely positioned tiles (percent-based layout), **`imageSrc`** as focal center, peripheral slots filled from **`accordionPanels`**; Framer **`useTransform`** maps pin progress (~first **0.52** of scroll) to **focal scale up** + **peripheral scale down** + **directional drift**. **`ArcFounderIntroSection`** now uses **`section` + `useRef`** + **`useMotionValue`** + **`useArcFullscreenPin(..., { onProgress })`** instead of **`PinnedSection`**; **`prefers-reduced-motion`** keeps **`FounderGalleryMosaic`** (static / rotating). Slightly widened right column (**`lg:max-w-[min(580px,54vw)]`**). Verified with **`npm run build`**.

### 79. Founder `#founder` — full-viewport immersive collage (match `#about`)

**Ask:** Founder collage should **fill the screen** like **`WelcomeImmersiveScrollBody`** (sticky **`h-[100dvh]`** stage, vw/vh tile layout), not a small column grid.

**Done:** Added **`src/lib/immersiveCollageFrames.ts`** with shared **`IMMERSIVE_COLLAGE_FRAME_CLASSES`**; **`ArcWelcomeSplitSection`** now imports it (removed duplicate **`IMAGE_STYLES`**). Replaced **`FounderZoomParallaxCollage`** with **`FounderImmersiveCollageLayer`**: same DOM/scaling/opacity pattern as welcome (**`scale` 1→4.25 focal**, peripherals **1→1.08**, images fade **0→GALLERY_PROGRESS_END**), portrait at **slot 0**. **`ArcFounderIntroSection`** immersive path: **`h-[240vh]`** section, sticky full-viewport stack, **no** ensemble **`useArcFullscreenPin`**; GSAP **`ScrollTrigger`** **`scrub`** on **`#main`** (**`ARC_LOCOMOTIVE_READY_EVENT`** lifecycle) drives progress; **`about-copy-stage.png`** + opacity handoff + **`ArcTibbixelCopyFrame`** for copy ( **`ARC_PINNED_CLEAR_BELOW_LOGO`**, scrollable copy stack). **`prefers-reduced-motion`**: restored **`PinnedSection`** two-column + **`FounderGalleryMosaic`**. Verified with **`npm run build`**.

### 80. Founder hero — editorial reference (no opening collage)

**Ask:** The doctor **opening** should **not** be a multi-tile collage; aim for the **reference** layout: **one main portrait**, **overlapping vertical inset**, **bottom-left typography** (sans lead + large italic serif name + small caps role), **dark contained frame**, light parallax; keep scroll handoff to detail copy.

**Done:** Removed **`FounderZoomParallaxCollage.tsx`**. Added **`src/components/arc/FounderEditorialHero.tsx`** (**`FounderEditorialHeroLayer`** scrubbed: main **scale**, inset **x/y** drift, type **y**, whole hero **opacity** out; **`FounderEditorialHeroStatic`** for reduced motion). **`ArcFounderIntroSection`** immersive path swaps collage for **`FounderEditorialHeroLayer`**; inset image = first **`accordionPanels`** entry whose **`imageSrc`** ≠ portrait (e.g. **In practice**). **`homeFounder.heroMeetLead`** / **`heroNameItalic`** in **`homepage.ts`**; **`page.tsx`** passes them. Reduced motion: **`PinnedSection`** stacks **static editorial hero** + copy + **`FounderGalleryMosaic`**. Verified with **`npm run build`**.

### 81. Founder editorial hero — shorten ambient-only phase 1

**Ask:** Reduce the first phase a bit (ambient-only opening).

**Done:** In **`src/components/arc/FounderEditorialHero.tsx`**, lowered **`AMBIENT_ONLY_END`** from **`0.2`** to **`0.15`** so the portrait/type reveal starts slightly earlier; **`heroRevealEnd`** remains **`AMBIENT_ONLY_END + HERO_REVEAL_BLEND`** (**0.37**). Verified with **`npm run build`**.

### 82. Founder immersive — reference PNG as detail-phase backdrop

**Ask:** Use **`ChatGPT Image May 5, 2026, 03_02_57 PM.png`** as the new background for the section in the detail/copy phase.

**Done:** Copied asset to **`public/assets/decoration/background/founder-section-ambient.png`**. **`src/content/backgroundDecoration.ts`**: exported **`FOUNDER_SECTION_AMBIENT_SRC`**. **`ArcFounderIntroSection`** (`FounderImmersiveScrollBody`): full-bleed **`next/image`** layer (`z-0`) on the sticky viewport stack; **`FounderEditorialHeroLayer`** raised to **`z-[1]`** so the editorial hero still paints above it until its opacity scrubs out, revealing the new plate behind the scrolling copy. **`FounderEditorialHero.tsx`**: restored semicolons on **`AMBIENT_ONLY_END`** / **`HERO_REVEAL_BLEND`**. Verified with **`npm run build`**.

### 83. Founder — shift text blocks right

**Ask:** Move the text to the right more.

**Done:** **`FounderEditorialHero.tsx`**: **`textPositionClass`** uses larger **left** padding (asymmetric vs right) so hero type sits farther right; role line **`translate-x`** steps increased. **`ArcFounderIntroSection`**: immersive detail copy wrapper gets **`ml-6 sm:ml-10 md:ml-16 lg:ml-24`**; reduced-motion copy column gets matching **`sm`–`lg`** **`ml-*`**. Verified with **`npm run build`**.

### 84. Founder — text farther right (follow-up)

**Ask:** More to the right (typo: “theteight”).

**Done:** Increased **`textPositionClass`** left padding again (**`pl-14` → `lg:pl-44`** etc.); role line **`translate-x`** stepped up through **`2xl:translate-x-80`**. Detail copy margins **`ml-10 sm:ml-16 md:ml-24 lg:ml-32`** (immersive + reduced-motion). Verified **`npm run build`**.

### 86. Services showcase — first three slides + per-slide eyebrow / nav label

**Ask:** Replace the **first three** service blocks with new copy (**Aesthetic Optimization** / **Internal Health** / **Functional Performance** and detail lines); keep **six** slides; uppercase label + nav aligned to content.

**Done:** **`servicesShowcaseSlides.ts`**: optional **`eyebrow`** / **`navLabel`** on **`ServicesShowcaseSlide`**; **`servicesShowcaseEyebrow`**, **`servicesShowcaseNavLabel`**, **`SERVICES_SHOWCASE_DEFAULT_EYEBROW`**. Slides **1–3** updated; **4–6** unchanged. **`ArcServicesShowcaseSlider`**: **`eyebrowRef`** with updates on slide change (WebGL + initial); nav chips use **`navLabel`**. Verified **`npm run build`**.

### 87. `#path` — full-viewport scroll-locked journey

**Ask:** Redesign **Your Path** so it’s **full screen**, **locked** while scrolling, with **scroll-driven animation** (not the old short pin + static row only).

**Done:** **`YourPathSection.tsx`** is now **`"use client"`** with **`YourPathImmersive`**: section height **`320vh`**, **`sticky`** **`h-[100dvh]`** stage, **`ScrollTrigger`** on **`#main`** (**`ARC_LOCOMOTIVE_READY_EVENT`** + fallback) with **`scrub: 0.75`** driving Framer **`progress`**; headline uses subtle **y / opacity**; three steps **crossfade + scale** in a shared card stack; **progress bar** at bottom; mobile **dot** indicators; **`ARC_PINNED_CLEAR_BELOW_LOGO`** for header clearance. **`prefers-reduced-motion`**: prior **`PinnedSection`** two-column + three cards (**`YourPathReduced`**). Verified **`npm run build`**.

### 88. `#path` — sticky stacking tabs (reference pattern)

**Ask:** Implement the provided **`StickyTabs`** reference (sticky nav spacer + per-section sticky headers + content) for **Your Path**.

**Done:** Added **`src/components/arc/ArcStickyTabs.tsx`**: **`overflow-clip`** root, **sticky** spacer matching **wordmark** clearance (same **`max(..., env(safe-area-inset-top))`** ladder as pinned sections), optional **`lead`**, then **`ArcStickyTabs.Item`** sections each with **sticky** sub-header (**`h3`**) and scrollable content. Styling uses **ARC** cream / teal / charcoal. **`YourPathSection`** immersive path now uses **`ArcStickyTabs`** with **lead** (H2 + CTA) and three items (numbered title + card body); removed prior **GSAP scrub / Framer crossfade** stack. **`YourPathReduced`** unchanged. Verified **`npm run build`**.

### 89. `ArcStickyTabs` — sticky lead (path intro + CTA)

**Ask:** The small intro block (headline + **Start your journey** / underline) should **stick a bit** while scrolling.

**Done:** **`ArcStickyTabs`**: optional **`leadSticky`**, **`leadStickyTrackClassName`** (default **`min-h`** runway ~**`42–52dvh`**), **`leadStickyBarClassName`**; lead wraps in a **sticky** strip (**`z-[25]`**, same **`STICKY_HEADER_TOP`** as step bars) with light **border / blur / wash**. **`YourPathSection`** sets **`leadSticky`**. Verified **`npm run build`**.

### 90. Sticky lead — no dead band before step 1

**Ask:** Keep lead sticky but **remove the gap** before step 1.

**Done:** Replaced large **`min-h`** lead track with a short in-flow **`leadStickyScrollPadClassName`** spacer (default ~**`12–18dvh`**) so scrolling isn’t a tall empty band; tightened lead bar **`pb`**; **first** tab item content uses **`!pt-5`–`!pt-7`** to pull step 1 body under its header. Renamed prop from **`leadStickyTrackClassName`** to **`leadStickyScrollPadClassName`**. Verified **`npm run build`**.

### 91. Path sticky lead — scroll hint instead of empty pad

**Ask:** Remove the **empty gap** spacer; add an **indicator** that users should keep scrolling for the wellness journey.

**Done:** **`ArcStickyTabs`**: removed fixed-height blank **`aria-hidden`** pad; added a **scroll hint** strip (gradient wash, two-line default copy + **`ChevronDown`** with **`motion-safe:animate-bounce`** / **`motion-reduce:animate-none`**, **`sr-only`** assistive line). Optional **`leadScrollHint`** + **`leadScrollHintClassName`**; dropped **`leadStickyScrollPadClassName`**. Verified **`npm run build`**.

### 92. Path lead — fill first viewport (hero)

**Ask:** Intro block (headline, CTA, scroll cue) should **fill the initial view** of the section.

**Done:** **`ArcStickyTabs`**: **`leadStickyFillViewport`** (default **`true`**) applies **`LEAD_FILL_MIN_H`** = **`min-h: calc(100dvh − wordmark band)`** per breakpoint (matches nav spacer ladder + **`env(safe-area-inset-top,0px)`**); single sticky column uses **`justify-between`** with **`lead`** top and scroll hint **`mt-auto`** + **`border-t`** band; bottom padding respects **`safe-area-inset-bottom`**. Set **`leadStickyFillViewport={false}`** for a compact stack. Verified **`npm run build`**.

### 93. Path lead — longer sticky lock

**Ask:** **Longer screen lock** for the initial path hero before steps advance.

**Done:** **`ArcStickyTabs`**: wrapper around the sticky lead uses **`leadStickyLockTrackClassName`** (default **`min-h-[135dvh]`** after follow-up — was **`180dvh`** / **`280dvh`**) so the hero stays pinned briefly; tune per section. Verified **`npm run build`**.

### 94. `ArcStickyTabs` — remove hairline rules

**Ask:** Dislike the **line on top** (and related rules).

**Done:** Dropped **`border-b`** on nav spacer and lead sticky bar; removed scroll-hint **`border-t`**; step sticky header wrapper now only **`border-b`** (lighter **`/12`**), no **`border-t`**; removed **`-mt-px`** overlap hacks. Verified **`npm run build`**.

### 95. Path lead hero — center in viewport

**Ask:** Put the path intro block (headline, **Start your journey**, decorative underline) **in the middle of the screen**; keep the scroll cue toward the bottom.

**Done:** **`ArcStickyTabs`**: when **`leadStickyFillViewport`**, the lead column uses **`flex flex-col`** with the **`{lead}`** region in **`flex-1 min-h-0 flex-col items-center justify-center`** plus horizontal padding and **`text-center`** so the hero is centered in the sticky viewport; scroll hint stays **`shrink-0`** below (no **`mt-auto`** on the hint). **`YourPathSection`**: lead uses **`items-center text-center`**, constrained width (**`max-w-2xl` / `lg:max-w-3xl`**), H2 **`text-balance`**, **`ArcTextUnderlineCta`** with **`items-center`**. Fixed invalid HTML in default scroll hint (two **`<p>`**s). Verified **`npm run build`**.

### 96. Path H2 — emphasize key words

**Ask:** In **“Your Path to Feeling and Living at Your Best”**, emphasize **Feeling**, **Living**, and **Best**; make the emphasized text **bigger**.

**Done:** **`YourPathSection.tsx`**: added **`YourPathHeadlineTitle`** with **`pathHeadlineEmphasisClass`** (**`font-bold`**, **`text-arc-teal-ink`**, responsive **`em`** scale **`~1.38 / 1.34 / 1.3`**) on those three words; used in immersive sticky lead and **`YourPathReduced`** H2. Verified **`npm run build`**.

### 97. Path H2 — same `TitleEmphasis` as founder

**Ask:** Use the site’s standard emphasized headline treatment (**`TitleEmphasis` / Birthstone**, same classes as founder **“Jabbar”**).

**Done:** **`TitleEmphasis.tsx`**: exported **`ARC_HEADLINE_TITLE_EMPHASIS_CLASS`** (founder’s responsive **`em`** scale, **`text-arc-teal-ink`**, light-surface **`text-shadow`**). **`ArcFounderIntroSection`** now imports that constant instead of a local string. **`YourPathSection`**: **`Feeling` / `Living` / `Best`** wrapped in **`TitleEmphasis`** with **`ARC_HEADLINE_TITLE_EMPHASIS_CLASS`**. Verified **`npm run build`**.

### 98. `#path` intro — full-bleed background asset

**Ask:** Use provided reference image as the **intro** background for the path section; organize/rename under **`public` assets**; per-step backgrounds **later**.

**Done:** Copied image to **`public/assets/sections/your-path/path-intro-background.png`**; removed duplicate **`ChatGPT Image May 5, 2026, 03_50_57 PM.png`** from repo root. **`backgroundDecoration.ts`**: **`PATH_SECTION_INTRO_BACKGROUND_SRC`**. **`ArcStickyTabs`**: optional **`leadStickyBackgroundSrc`** + **`leadStickyBackgroundOverlayClassName`** (default cream/teal gradient scrim); **`next/image`** **`fill`** **`object-cover`** under sticky lead only; lead + scroll hint use **`relative z-[2]`**. **`YourPathSection`**: passes intro src + lighter **`leadStickyBarClassName`** so the photo reads through. **`public/assets/sections/README.md`** + **`your-path/README.md`**. Verified **`npm run build`**.

### 99. `#path` intro — restore scroll “lock” (sticky lead)

**Ask:** The intro **screen lock** (sticky hold while scrolling) stopped working.

**Done:** **`ArcStickyTabs`**: **`min-h-0`** was merged on the same node as **`LEAD_FILL_MIN_H`** via `cn` / tailwind-merge, so **`min-h-0` won** and the viewport **`min-h-[calc(100dvh−…)]`** was dropped — sticky band collapsed and the lock felt gone. Removed **`min-h-0`** from that sticky container (inner lead column still uses **`min-h-0`** for flex overflow). Moved **`overflow-hidden`** off the sticky node onto the **absolute background** wrapper only so clip doesn’t fight **`position: sticky`**. Verified **`npm run build`**.

### 100. `#path` intro background — fill top (wordmark band)

**Ask:** Textured intro background should reach the **top of the screen**; a flat teal band sat above it (wordmark / nav spacer strip).

**Done:** **`ArcStickyTabs`**: when **`leadStickyBackgroundSrc`** + sticky lead, render a **`100dvh`** **`absolute`** image + scrim at the **component root** (behind in-flow content); make the **nav spacer** **`bg-transparent`** (drop backdrop tint) so texture shows through under the fixed transparent header. Removed the duplicate image layer from the sticky lead panel only (single source). Root **`relative`**; lock track **`z-[1]`** above the bg layer. Verified **`npm run build`**.

### 101. `#path` intro background — follow screen lock

**Ask:** Intro background did not **follow the screen lock** (pinned intro): art scrolled off while the sticky hero stayed.

**Done:** Removed **root-anchored** **`100dvh`** bg (it moved with section scroll, not the sticky lead). Re-mounted image + scrim **inside** the **`position: sticky`** lead panel with **`LEAD_BG_VIEWPORT_PULL`** (negative top matching the wordmark / nav spacer ladder) + **`h-[100dvh]`** so the texture still reaches the viewport top. Transparent nav spacer unchanged. Verified **`npm run build`**.

### 102. `#path` steps — restore “hide previous step” while scrolling

**Ask:** Multiple path steps visible at once; **each step should hide / cover** the prior one while scrolling.

**Done:** **`ArcStickyTabs`**: each **`ArcStickyTabs.Item`** `<section>` gets an inline **`zIndex: 40 + index * 10`** so later steps paint **above** earlier steps and their **opaque** mint surface covers the previous card + band. **`YourPathSection`**: **`sectionClassName="… bg-arc-teal-muted"`** (solid token, not **`/85`**) so layers aren’t see-through; slightly **taller** step body **`min-h`** (**`88dvh` / `92dvh`** caps) so the next header enters later. Verified **`npm run build`**.

### 103. `#path` step card — hide after passing step header (stacking)

**Ask:** Step **card** stayed visible (even over the header / logo) after scrolling past the step’s cream **header** bar.

**Done:** **`initArcScrollReveal`** applies **`transform` + `opacity`** to every **`[data-scroll-section]`**; that compositor layer + Lenis scroll broke **section z-index “cover”** ordering. **`YourPathSection`**: removed **`data-scroll-section`** from each step’s body wrapper; switched **`items-center`** → **`justify-start`** + top **`pt-*`** so the card sits under the bar instead of vertically centered in a tall band. **`sectionClassName`**: added **`isolate`**. **`ArcStickyTabs`**: step **sticky bar** **`z-20` → `z-30`**, content column **`relative z-0`**; card wrapper **`relative z-0`**. Verified **`npm run build`**.

### 104. `#path` steps — hard-cover previous step (Lenis / sticky)

**Ask:** Prior **step header + card** still visible after scrolling into the next step.

**Done:** **`ArcStickyTabs`**: for **`itemIndex > 0`**, prepend an **`absolute`** full-width **`bg-arc-teal-muted`** slab with **negative top** (**`~68–76dvh`** capped) so this step’s paint **physically overlaps** the prior step’s viewport; wider **`zIndex`** steps (**`60 + index * 45`**). **`!overflow-visible`** on those sections so **`overflow-clip`** doesn’t clip the bleed. Content column **`z-0` → `z-10`** above the slab. Verified **`npm run build`**.

### 105. `#path` steps — reference **StickyTabs** reimplementation (post-intro)

**Ask:** Reimplement steps **1–3** after the intro using the provided **`StickyTabs`** reference (plain sections, sticky bar + content, no z-index/bleed hacks).

**Done:** **`ArcStickyTabs`**: step blocks now use **`Children.map`** (dev **`console.warn`** for non-**`Item`** children); **`STEP_STICKY_HEADER_TOP`** = **`calc(max(wordmark band) - 1px)`** per breakpoint (hairline stack like reference **`calc(mainNavHeight - 1px)`**); sticky bar **`z-10`**, **`-mt-px`**, **`flex flex-col`**; sections are **`relative overflow-clip`** + **`sectionClassName`** only — removed upward bleed, inline **`zIndex`**, **`!overflow-visible`**, and extra content **`z-10`**. In-flow nav spacer **`z-30` → `z-20`** (reference parity). **`YourPathSection`**: **`sectionClassName`** drops **`isolate`**; step card wrapper drops extra **`relative z-0`**; slightly shorter body **`min-h`** band. Verified **`npm run build`**.

### 106. `#path` — five journey steps + timeline layout

**Ask:** Use the provided **five-step** reference (Listen → Measure → Author → Practice → Revise) as **all** path steps.

**Done:** Added **`src/content/pathSteps.ts`** (**`PATH_JOURNEY_STEPS`**, **`PathJourneyStep`**). **`YourPathSection`**: **`PathJourneyStepContent`** — 3-column grid (Roman **champagne** circle, short vertical connector **`lg`**, title + meta, body), forest panel **`#1e322e`** / sticky strip **`#182925`**, cream type; immersive maps **5** **`ArcStickyTabs.Item`**s; reduced-motion stacks the same rows beside the hero. **`site-section-content-inventory.md`** path table updated. Verified **`npm run build`**.

### 107. `#path` — keep prior UI, new copy only

**Ask:** Restore the **previous** path design; only change **text** to the five-step narrative.

**Done:** **`YourPathSection`**: removed dark timeline / **`PathJourneyStepContent`**; restored **mint** **`sectionClassName`**, default **`ArcStickyTabs`** chrome, **white rounded cards** with **Lucide** icons (**`MessageCircle`**, **`Activity`**, **`PenLine`**, **`HeartHandshake`**, **`RefreshCw`**); sticky titles **`{id} {title}`**; card eyebrow uses **`step.meta`**, body **`step.body`** from **`PATH_JOURNEY_STEPS`**. Reduced-motion: **5** light cards in the horizontal row again. **`site-section-content-inventory.md`** note updated. Verified **`npm run build`**.

### 108. `#path` steps — card must not ride over sticky header

**Ask:** Step body (white card) was appearing **on top of / above** the cream **“1 Listen”** bar while scrolling — should stay **under** the sticky title strip.

**Done:** **`ArcStickyTabs`**: step sticky bar **`z-10` → `z-30`**; default **`headerContentWrapperClassName`** **`bg-arc-cream/95` → `bg-arc-cream`** (opaque wash); content column wraps with **`relative z-0`**. **`YourPathSection`**: step body + card **`relative z-0`**, slightly shorter **`min-h`** runway, card **`bg-white`** (full opacity). Verified **`npm run build`**.

### 109. `#path` steps — nothing visible “through” sticky header

**Ask:** Step **content** and **mint background** should not appear to pass **through** the cream sticky step header (no blur / semi-transparent seam).

**Done:** **`ArcStickyTabs`**: sticky step shell is **`w-full bg-arc-cream`** with **`backdrop-blur-none`** (solid plate, not frosted inner wrapper); default **`headerContentWrapperClassName`** is **border only** (removed **`backdrop-blur-md`**); sticky **`z-30` → `z-40`**; each step **`<section>`** uses **`isolate`**; content column **`bg-inherit`** + **`min-h-0`** so the mint fill is explicit under scroll. **`YourPathSection`**: removed **`backdrop-blur-sm`** from white cards (opaque **`bg-white`** only); dropped unused **`cn`** import.

### 110. Testimonials sphere — interaction hint copy

**Ask:** Add an on-page indicator that the portrait sphere can be **spun** and **clicked** to change testimonials (e.g. “spin me and click me…”).

**Done:** **`ArcTestimonialsSection.tsx`**: wrapped **`SphereImageGrid`** in **`role="region"`** with **`aria-labelledby`** tied to visible hint text; hint below the sphere (**`text-balance`**, cream-friendly charcoal at **`/72`**). Default copy: **“Spin me with a drag, then tap a portrait to read the testimonials.”** When **`useReducedMotion() === true`** (grid fallback): **“Tap a portrait to read the testimonials.”** — avoids hydration mismatch by branching only on strict **`true`**. Moved **`max-w`** to outer wrapper; **`SphereImageGrid`** **`className="w-full"`**. Verified **`npm run build`**.

### 111. Testimonials — heading above right-hand card

**Ask:** Add a **“Testimonials”** header above the quote card on the right column.

**Done:** **`ArcTestimonialsSection.tsx`**: **`h2`** **“Testimonials”** above **`figure`**, serif scale (**`text-2xl` → `sm:text-3xl`**), **`text-center`** on small stacks and **`lg:text-left`** with spacing **`mb-5` / `sm:mb-6` / `lg:mb-6`**. Verified **`npm run build`**.

### 112. Testimonials — larger, richer card layout

**Ask:** **Bigger** and **better** testimonial card design on the right.

**Done:** **`ArcTestimonialsSection.tsx`**: widened column (**`max-w-lg` → `xl:max-w-[min(100%,36rem)]`**, more **`lg/xl`** horizontal padding); **`h2`** slightly larger. Card: **`rounded-2xl`**, solid **`bg-white`**, **`ring-1`**, deeper shadow (**`shadow-[0_24px_70px_…]`**), generous **`p-8`–`lg:p-12`**, thin **teal gradient** top accent bar. **Larger** portrait (**`size-20` → `md:size-[7.25rem]`**), stronger name / context type, **`border-t`** before blockquote, **larger** serif quote (**`~1.125rem` → `md:1.35rem`**, **`text-balance`**). Decorative typographic quotes in **`text-arc-teal-ink`** with **`aria-hidden`**. Verified **`npm run build`**.

### 113. Testimonials — sphere + card closer (less gutter)

**Ask:** Bring the **portrait sphere** and **testimonial card** closer horizontally.

**Done:** **`ArcTestimonialsSection.tsx`**: row **`lg:items-stretch`**. **Left** column **`lg:justify-end`** with modest outer **`lg:pl-8` / `xl:pl-12`** and **tight inner seam** **`lg:pr-3` / `xl:pr-5`** so the sphere hugs the center. **Right** column **`lg:justify-start`** and **reduced** inner seam padding (**`lg:pl-4` / `xl:pl-6`**) vs previous **`pl-12`–`xl:pl-16`**; outer **`lg:pr-8` / `xl:pr-12`**. Verified **`npm run build`**.

### 114. Testimonials — centered (“middle”) column

**Ask:** **Testimonials middle** — center the testimonials stack in the right column.

**Done:** **`ArcTestimonialsSection.tsx`**: right pane **`flex-col items-center justify-center`**, symmetric **`lg:px-6` / `xl:px-10` / `2xl:px-14`**; inner wrapper **`mx-auto`** + **`text-center`** (removed **`lg:text-left`** on **`h2`**). Card body: **`text-center`**, avatar row **`flex-col items-center`** then **`sm:flex-row sm:justify-center`** so portrait + attribution stay visually centered. Verified **`npm run build`**.

### 115. Tibbixel copy frame — disabled site-wide

**Ask:** Remove the **black / floral Tibbixel frame** (**`ArcTibbixelCopyFrame`**) from the site for now.

**Done:** **`ArcTibbixelCopyFrame.tsx`**: removed the absolute **`background-image`** ornament layer; component is now **padding + layout wrapper** only (same responsive **`clamp()`** insets). **`TIBBIXEL_COPY_FRAME_SVG`** export kept for easy restore. **`ArcMicroStatementSection.tsx`**: dropped **`frameOrnamentMotion`** + **`ornamentMotion`** prop wiring (pinned scrub). **`ArcWelcomeSplitSection`** unchanged at call sites — **`ornamentClassName`** props are no-ops until art returns. Verified **`npm run build`**.

### 116. Concerns section — USP lower bar on short laptop viewports

**Ask:** The dark **full-bleed USP bar** (stats grid below the concern panels) did not show correctly on shorter laptop screens.

**Done:** **`ArcConcernsPinnedSection.tsx`**: root section locked to **`h-[100dvh] max-h-[100dvh] min-h-0`** (same idea as other pinned viewport sections) instead of **`min-h-[100dvh]`** only, so total layout cannot exceed one viewport while pinned. Flex chain uses **`min-h-0`** so children can shrink; headline **`shrink-0`**; concern cards row uses **`flex-1`** instead of fixed **`h-[360px]`–`420px`** so it absorbs remaining height; panel buttons get **`min-h-0`**. USP grid **`shrink-0`** with slightly smaller default cell **`min-height`** / padding plus **`@media (max-height: 780px)`** compaction so the bar stays on-screen. Background layer uses **`inset-0`** only (no separate **`min-h-[100dvh]`**). Verified **`npm run build`**.

### 117. Header logo — delayed hide while scrolling, settle when idle

**Ask:** Logo should stay fixed but **lag / stagger** relative to scroll: **hide while scrolling** (including horizontal/trackpad “right”), then **ease back** to the normal top placement when scrolling stops.

**Done:** **`ArcSiteHeader.tsx`**: Lenis **`velocity`** from **`window.locomotiveScroll.lenisInstance`** (listen for **`ARC_LOCOMOTIVE_READY_EVENT`** + retries). **`useMotionValue`** + **`useSpring`** drive **`translateY`** (lift) and **opacity** while **`|velocity|`** is above a small epsilon; target resets to **0** when idle so the logo **springs** back. **`virtual-scroll`** adds light **`translateX`** drift for horizontal wheel/trackpad. **`prefers-reduced-motion`** and **menu open** force targets to **0** (static logo). Verified **`npm run build`**.

### 118. Header logo — gradual fade on scroll (no “pop” off)

**Ask:** On scroll, the logo fade should feel **gradual**, not like a straight “puff” / instant disappear.

**Done:** **`ArcSiteHeader.tsx`**: Logo motion is **opacity-only** (no translate bobbing). **Fade-out while scrolling** (past **`LOGO_SHOW_BELOW_SCROLL_Y`**, Lenis not “stopped”) now runs over **`LOGO_FADE_OUT_DURATION_MS` (480ms)** with **`easeInOutQuad`**, using **`logoFadeOutSessionRef`** (`start` + **`from`** opacity) instead of a fast exponential lerp toward 0. **Fade-in after scroll stops** remains a fixed-duration **`easeOutCubic`** (**`LOGO_FADE_IN_DURATION_MS`**) via **`logoFadeInSessionRef`**. Sessions clear on menu open, missing Lenis, and when switching between hide/show branches. Verified **`npm run build`**.

### 119. Header logo — smooth fade-in when scrolling back to top

**Ask:** Scrolling back to the top, the logo **snapped** visible instead of **fading in**.

**Done:** **`ArcSiteHeader.tsx`**: The **`scroll < LOGO_SHOW_BELOW_SCROLL_Y`** branch had been clearing **`logoFadeInSessionRef`** and lerping opacity with **`LOGO_OPACITY_TOP_SNAP_LERP`**, which reached ~full opacity in a few frames (felt instant). **Near-top** is now merged with the **“show logo”** path: same **`LOGO_FADE_IN_DURATION_MS` + `easeOutCubic`** session as when past the threshold but idle. **`!lenis`** fallback uses that timed fade-in as well instead of only a per-frame lerp. Verified **`npm run build`**.

### 120. Hero — left headline + CTAs lower on screen

**Ask:** Move the **left block** (headline + CTAs) **farther down** the viewport.

**Done:** **`ScrollExpandHero.tsx`**: increased absolute **`top`** on the left stack from **`top-36` / `sm:top-40` / `md:top-44` / `lg:top-[11.25rem]`** to **`top-48` / `sm:top-52` / `md:top-56` / `lg:top-[15.25rem]`** (~3rem more at each step). Verified **`npm run build`**.

### 121. Hero — left stack lower again

**Ask:** Move the left headline + CTAs **down more**.

**Done:** **`ScrollExpandHero.tsx`**: **`top`** stepped again — **`top-60` / `sm:top-64` / `md:top-72` / `lg:top-[18.25rem]`** (from **`top-48` … `lg:top-[15.25rem]`**). Verified **`npm run build`**.

### 122. Hero — bottom keyword marquee (sliding bar)

**Ask:** At the **bottom of the hero**, add an **animated sliding bar** with **keywords**, like the reference ticker (bullet-separated list).

**Done:** **`ScrollExpandHero.tsx`**: **`HeroKeywordMarquee`** + **`HERO_KEYWORD_MARQUEE_ITEMS`** (ARC-focused phrases). Full-bleed bar **`absolute bottom-0`** (**`z-[30]`**) so it sits on the hero viewport, not below it. Styling: **`bg-arc-teal`**, white uppercase sans, **`·`** separators, duplicated row + existing **`animate-arc-marquee`** / **`arc-marquee`** keyframes (same pattern as **`ArcFooter`**). **`globals.css`**: **`prefers-reduced-motion: reduce`** disables marquee animation. Ticker **`aria-hidden`** (decorative). Verified **`npm run build`**.

### 123. Hero marquee — concerns copy

**Ask:** Use these phrases in the sliding bar: Low Energy & Burnout; Hormonal Imbalance & Weight Gain; Poor Sleep & Recovery; Aging Skin & Body Changes; Brain Fog & Focus Issues.

**Done:** **`ScrollExpandHero.tsx`**: **`HERO_KEYWORD_MARQUEE_ITEMS`** set to that list. Marquee row typography switched from **`uppercase`** to **title-style** (**`tracking-wide`**, slightly larger base size) so the longer lines stay readable. Verified **`npm run build`**.

### 124. Hero marquee — all caps + TitleEmphasis (Birthstone)

**Ask:** Marquee text **all caps** and use the same **emphasis / TitleEmphasis** font as hero keywords.

**Done:** **`ScrollExpandHero.tsx`**: Each item wrapped in **`TitleEmphasis`** with **`uppercase`** and **`HERO_MARQUEE_EMPHASIS_CLASS`** (**`text-arc-teal-ink`** + layered **`text-shadow`** so script stays legible on **`bg-arc-teal`**; hero headline uses vivid teal on dark photo). Row base size **`text-sm` / `sm:text-base` / `md:text-lg`**; separators **`font-sans`** · in **`text-arc-teal-ink/75`**. **`animate-arc-marquee`** row includes **`items-center`**. Verified **`npm run build`**.

### 125. Hero marquee — white text

**Ask:** Make the text in the sliding bar white.

**Done:** **`ScrollExpandHero.tsx`**: marquee emphasis class switched from **`text-arc-teal-ink`** to **`text-white`** (kept all-caps **`TitleEmphasis`** + shadow stack), and dot separators changed to **`text-white/80`**. Verified **`npm run build`**.

### 126. Concerns panels — bottom-right info box on expand

**Ask:** In **`ArcConcernsPinnedSection`**, each column should show a **useful text box** about the keyword that **only fades in when that panel is expanded**, positioned **bottom-right**.

**Done:** **`ArcConcernsPinnedSection.tsx`**: Replaced parallel **`CONCERNS` / `CONCERN_IMAGES`** arrays with **`CONCERN_PANELS`** (**`title`**, **`image`**, **`blurb`**). Title block **bottom-left** given **`max-w`** so it doesn’t collide with the new box. **Bottom-right** container: frosted **`bg-black/55`**, **`backdrop-blur-md`**, border; **`opacity` + `translate-y`** transition when **`isActive`**, **`pointer-events-none`** + hidden from SR when inactive (**`aria-hidden`**). **`prefers-reduced-motion`**: opacity-only transition, no vertical nudge. **`onClick`** sets **`activeIndex`** for touch; **`aria-expanded`** on buttons. Verified **`npm run build`**.

### 127. Concerns panels — stop text reflow while expanding

**Ask:** Text was **running / reflowing** awkwardly while panels **expand and collapse**.

**Done:** **`ArcConcernsPinnedSection.tsx`**: Title and info overlays used **`max-w`** with **percentage of panel width**, which tracks the **`flex` transition** and forces continuous re-wrapping. Replaced with **fixed `rem` caps** (**`~10.5–12.5rem`** title, **`~15–16rem`** info), **`left-0` / `right-0`** alignment, **`min-w-0`**, **`text-pretty`**, and **removed the active-only larger title size** so typography stays stable during the animation. Verified **`npm run build`**.

### 128. Concerns info box — hide fully when panel closes

**Ask:** When **closing** a panel, the info box was **still visible** (ghost / lag).

**Done:** **`ArcConcernsPinnedSection.tsx`**: **Closed** state uses **`invisible`** + **`opacity-0`** with **no exit transition** (open still **300ms** opacity/transform). **`backdrop-blur-md`** / **`bg-black/55`** apply only while **`isActive`** so a semi-transparent blur doesn’t linger over the image during collapse. Verified **`npm run build`**.

### 129. About / immersive collage — intro line on focal tile (index 0)

**Ask:** At the **start** of the about collage section, on **image index 0**, show **“Wellness. made personal”** over the image so visitors **don’t see the full collage** at first.

**Done:** **`ArcWelcomeSplitSection.tsx`** (**`WelcomeImmersiveScrollBody`**): Focal tile (**`WELCOME_GALLERY_FOCAL_INDEX`**, currently **0**) gets a **cream veil** + centered **“Wellness. Made personal.”** (**serif** + **`TitleEmphasis`** on “Made personal”), opacity scrubbed via **`introVeilOpacity`** (**`COLLAGE_INTRO_VEIL_FADE`**). **Non-focal** images use **`opacityPeripheralReveal`** (**`COLLAGE_PERIPHERAL_REVEAL`**) so they stay **opacity 0** at rest and ease in as the veil lifts. Image **`motion.div`** wrapper preserves scroll **`scale`** + gallery **`opacityImage`**. Verified **`npm run build`**.

### 130. About collage — focal text only, full grid unchanged

**Ask:** **Revert** hiding peripheral collage tiles / intro veil; **keep the original collage behavior** and only add the **“Wellness. Made personal.”** line on **focal index 0**.

**Done:** **`ArcWelcomeSplitSection.tsx`**: Removed **`COLLAGE_INTRO_VEIL_FADE`**, **`COLLAGE_PERIPHERAL_REVEAL`**, **`introVeilOpacity`**, **`opacityPeripheralReveal`**, and the extra **`motion.div`** around **`Image`**. All seven frames behave as before. **Focal tile only**: light **`bg-gradient-to-t`** for legibility + centered **Wellness. / Made personal.** (**`TitleEmphasis`**, hero-style teal glow on photo). Verified **`npm run build`**.

### 131. About immersive — larger focal line + scroll hold before parallax

**Ask:** **Bigger** “Wellness. Made personal.” on the focal collage, and a **screen lock** so **parallax doesn’t start** until after some scroll.

**Done:** **`ArcWelcomeSplitSection.tsx`**: Added **`WELCOME_PARALLAX_HOLD` (0.14)** and **`parallaxProgress`** (**`useTransform`** so raw section progress stays at **0** until the hold, then maps **0→1** over the remainder). **`scaleFocal`**, **`scalePeripheral`**, **`opacityImage`** now use **`parallaxProgress`**; copy fade / stage bg still use **raw** **`progress`**. Section height **`240vh` → `280vh`** for a bit more runway. Focal overlay type scaled up (**`text-3xl` … `xl:text-[3.5rem]`**, wider **`max-w`**). Verified **`npm run build`**.

### 132. About immersive — shorter “lock” at end of section

**Ask:** The **lock at the end** of the about / immersive section felt **too long** (sticky stage lingers before the next section).

**Done:** **`ArcWelcomeSplitSection.tsx`**: Reduced immersive section height **`280vh` → `220vh`** so total ScrollTrigger travel is shorter; the **post-copy** tail (full copy + background already visible) scales down proportionally. Verified **`npm run build`**.

### 133. Welcome copy-stage background — new asset + naming

**Ask:** Use the new **`ChatGPT Image May 5, 2026, 09_28_56 PM.png`** for the section background; **rename and organize** under **`public/assets`**.

**Done:** Copied to **`public/assets/decoration/background/welcome-copy-stage-cream.png`**. Renamed prior art to **`about-copy-stage--previous.png`**. **`ArcWelcomeSplitSection.tsx`**: **`WELCOME_COPY_STAGE_BG`** → new path. Updated **`public/assets/decoration/background/README.md`** and **`public/assets/decoration/README.md`**. Removed repo-root **`ChatGPT Image May 5, 2026, 09_28_56 PM.png`**. Verified **`npm run build`**.

### 134. Welcome copy-stage — headline/body colors on dark marble

**Ask:** **Text color change** so **“Wellness.”** reads in **white**, **“Made Personal”** and the **final period** in **teal**, with **body** and **CTA** legible on the dark photography.

**Done:** **`src/content/homepage.ts`**: **`homeWelcome.headline`** **`"Wellness, Made Personal"` → `"Wellness. Made Personal"`** so the split places the period on **“Wellness.”** and **emphasis** on **“Made Personal”**. **`ArcWelcomeSplitSection.tsx`** (**`WelcomeImmersiveScrollBody`**): copy-stage tint **`bg-arc-cream/10` → `bg-black/[0.12]`**; **`h2`** **`before`** **`text-white`** + shadow; **`TitleEmphasis`** **`text-arc-teal`** with hero-style glow; **trailing period** rendered **teal** when **`after`** is empty or only **`.`**; body **`text-[#f7f4ef]/90`**, **`strong`** **`text-white`**; **`ArcTextUnderlineCta`** **`accent="tealBright"`**. Verified **`npm run build`** and IDE lints (no errors).

### 135. Footer — editorial layout aligned with site chrome

**Ask:** Redesign **`ArcFooter`** layout aesthetically so it fits the design throughout the site.

**Done:** **`src/components/arc/ArcFooter.tsx`**: Marquee band uses a subtle **teal-muted gradient** wash. Main area adds a soft **radial cream/teal glow** and an eyebrow row (**“Visit & connect”** + hairline) matching **`WhoWeAreSection`**. Replaced plain **“ARC WELLNESS”** text with **`next/image`** wordmark from **`images.logo`** (links home). Tagline uses **serif italic**; **`max-w-7xl`** rail. Link columns use a **`2×2` / four-column** nav grid with uppercase micro labels consistent with other sections; shared **`footerLinkClass`** for underline + **teal-ink** hover + focus rings. **Stay connected** + phone + address + book CTA live in a **rounded frosted card** (border, inset highlight, light blur). Legal row uses **`border-arc-teal/12`** and the same link treatment; **Back to top** is an unstyled button with matching focus/hover. Verified **`npm run build`**.

### 136. Footer — luxury marble reference layout (So Vous–style)

**Ask:** Make the footer **something like** the provided reference (marble field, centered logo, address / contact columns, ratings + social + legal row, dark accessibility bar).

**Done:** **`src/components/arc/ArcFooter.tsx`**: Root **`bg-arc-charcoal`** so the bottom compliance strip can sit flush. **CSS marble plate** (`footerMarbleClass` — layered radial/linear gradients on warm off-white, no new image asset). **Tier 1** — `md:grid-cols-3`: left **Visit** + address (`FOOTER_ADDRESS` placeholders), center **wordmark** + serif tagline, right **Contact** + phone + **`FOOTER_HOURS`** + teal **Book here** pill. **Hairline dividers** between blocks. **Sitemap** — existing four link columns preserved. **Tier 2** — **5.0** + gold **`Star`** row (`arc-champagne`), centered **social** icons in champagne, pipe-separated **legal** micro line (Sitemap / Privacy / Terms / Accessibility / Back to top), right rail **Schedule your consultation** (champagne italic). **Bottom strip** — full-width **`bg-arc-charcoal`**: accessibility statement, **skip-to-`#main`** circle (**`Accessibility`** icon), **book** circle (**`MessageCircle`**, champagne). Marquee strip above marble uses neutral tint + champagne separators. Removed unused **`MapPin`** / **`TitleEmphasis`**. Verified **`npm run build`**.

### 137. Footer — `ambient-04.png` background

**Ask:** Use **`ambient-04.png`** for the footer background instead of the CSS-only marble gradient.

**Done:** **`src/components/arc/ArcFooter.tsx`**: Replaced **`footerMarbleClass`** gradient stack with **`FOOTER_AMBIENT_BG`** (`/assets/decoration/background/ambient-04.png`). Added **`next/image`** **`fill`** + **`object-cover`** in an **`absolute inset-0`** layer behind content; layered a **cream gradient wash** (`from/via/to` with high opacity) so charcoal copy and the wordmark stay readable. Main column + charcoal strip use **`relative z-[1]`**. Fallback base color **`bg-[#f0ede6]`** on the flex shell. Verified **`npm run build`**.

### 138. Footer — no logo image, no ambient overlay

**Ask:** Remove the footer **PNG wordmark**; use **only** the ambient background **without** an overlay wash.

**Done:** **`src/components/arc/ArcFooter.tsx`**: Removed **`images.logo`** **`Image`** and **`@/content/site`** import. Center column uses a **text** home link **“ARC WELLNESS”** (serif, tracking) plus the existing tagline. Removed the **gradient scrim** div above **`ambient-04`** so the texture shows at full strength. Verified **`npm run build`**.

### 139. Footer — light text on dark marble (legibility)

**Ask:** Change footer **text colors** so type reads on a **dark marble** background (charcoal links were invisible).

**Done:** **`src/components/arc/ArcFooter.tsx`**: Footer shell **`bg-zinc-950 text-white`** under the image so letterforms read if art loads slowly. **Body copy** — white / cream opacities (**`/92`**, **`/78`**, **`/55`** for labels) plus light **`text-shadow`** on busy texture. **Sitemap links** — **`footerLinkClass`** → **`text-white/88`**, hover **`text-arc-teal`**. **Legal row** — **`legalLinkClass`** → **`text-white/55`**, brighter hover; pipe separators **`white/30`**. **Dividers** — **`via-white/20`**. **Focus rings** — **`ring-offset-zinc-950`**. **Social** hovers **`bg-white/10`**. **Teal Book** CTA unchanged; champagne accents kept for stars / script CTA / icons. Verified **`npm run build`**.

### 140. Footer — larger type + more top offset

**Ask:** Make footer **text bigger** and move the **top block down** a bit.

**Done:** **`src/components/arc/ArcFooter.tsx`**: Inner rail **`pt-24 md:pt-32 lg:pt-36`** (was uniform **`py-14`/`16`**) with preserved bottom padding. **Tier 1** — eyebrows **`text-xs`**, address **`text-base`/`md:text-lg`**, **ARC WELLNESS** up to **`lg:text-4xl`**, tagline **`text-lg`→`md:text-2xl`**, phone/hours larger, **Book here** **`text-sm`** + **`min-h-[48px]`**. **Sitemap** — headings **`text-xs`**, links **`text-sm`/`md:text-base`**. **Rating** — larger **5.0** / stars; **Patient experience** **`text-xs`**. **Legal line** — **`sm:text-xs`**, **`md` ~13px**. **Schedule** link scaled to **`sm:text-2xl`**. **Social** icons **`size-6`**. Slightly larger tier gaps and divider margins. Verified **`npm run build`**.

### 141. Footer — remove center “ARC WELLNESS” link

**Ask:** Remove the **`ARC WELLNESS`** home **`Link`** in the footer center column.

**Done:** **`src/components/arc/ArcFooter.tsx`**: Dropped the serif **`Link`**; center column is only the italic tagline, with **`justify-center`** on the flex stack. Verified IDE lints (no errors).

### 142. Footer — spread rating vs. new-patient CTA to edges

**Ask:** **Patient experience** (5.0 block) toward the **left edge**, **New patients / Schedule your consultation** toward the **right**, with social + legal centered.

**Done:** **`src/components/arc/ArcFooter.tsx`**: Moved **Tier 2** out of **`max-w-5xl`** into a full-width rail with **`px-5`→`xl:px-20`**. Desktop grid **`md:grid-cols-[minmax(0,1fr)_auto_minmax(0,1fr)]`** so side columns absorb space and **middle stays `auto`**. Left: **`md:justify-start`**; center: **`justify-self-center`**; right: **`md:justify-self-end`** + **`md:items-end`**. **`StarRatingBlock`**: **`items-center text-center`** on small screens, **`md:items-start md:text-left`** for alignment with the left rail. Verified **`npm run build`**.

### 144. Footer — remove Tier 1 “Book here” CTA

**Ask:** Remove the teal **Book here** pill from the footer **Contact** column.

**Done:** **`src/components/arc/ArcFooter.tsx`**: Removed the **`Link`** to **`#book`** under hours; Contact column is phone + hours only; dropped **`gap-5`** on the column wrapper. Other **`#book`** entry points (e.g. **Schedule your consultation**, bottom bar chat) unchanged. Verified **`npm run build`**.

### 145. Git — track hero / micro-statement `.mov`

**Ask:** Include **`spa-background-oriental-therapy.mov`** in the Git upload (it was ignored).

**Done:** **`.gitignore`**: kept **`/public/assets/videos/**/*.mov`** but added negation **`!/public/assets/videos/hero/spa-background-oriental-therapy.mov`** with a short comment (~42MB, under GitHub’s 100MB limit). Staged **`.gitignore`** + that video for commit.

## Paths / artifacts (optional)

| Path | Notes |
|------|--------|
| `src/components/arc/ArcFooter.tsx` | Footer layout + tiers; no Tier 1 Book pill (§144) |
| `.gitignore` | Exception for tracked hero `.mov` (§145) |
| `public/assets/videos/hero/spa-background-oriental-therapy.mov` | Now trackable (§145) |
